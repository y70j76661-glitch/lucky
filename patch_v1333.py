# -*- coding: utf-8 -*-
"""patch_v1333.py — v1332(2e776920) main.py 에 v13.33 적용. (v1332 적용 후 실행)
  [mini14 실측 — 원인과 수정]
   M2 빈 항목('3. 세제 혜택을 … 경우:'): 항목 안 문장이 세제 규칙으로 제거되고 머리말만 남음 → 목록 정합 패스(K0): 본문 없는
       번호 머리말은 제거하고 재번호(번호·상품·근거는 한 묶음).
   M2 '선호하는 분들께 적합합니다' 잔존: 투자자 조건 예외('선호하')가 상품명이 든 문장까지 면제했음 → 상품명이 있는 문장은 예외 없이 계약.
       '장기 성장 가능성이 큽니다'류 성과 단정도 계약 대상. 높은위험 표준 문장: '장기 투자자가 검토할 수 있으나 … 손실 가능성이 있습니다'.
   M2b 3번 상품 비보장 고지 누락: 고지 문장이 상품마다 동일해 전역 중복 제거(K5)에 지워짐 → 고지·표준 문장에 상품명을 넣어 고유화.
   G11: '원리금보장형 상품이 가장 적합' → '자료상 원금 보장을 중시할 때 검토할 수 있는 후보'; 재예치·만기상환 문장은 사용 근거 문서에
       그 단어가 있을 때만 유지.
자동백업·자가검증. 이미 v13.33이면 스킵. 검증 md5==f2627a3aaf51665be3cf890a8908819b"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='2e776920d280a5bbe7447ee89ffcc27a'
EXPECT_AFTER='f2627a3aaf51665be3cf890a8908819b'
HUNKS=[["            if re.search(r\"중도\\s*해지[^.!?]{0,20}이자[^.!?]{0,10}손실\", st) and not any((\"중도해지\" in t or \"중도상환\" in t) and \"이자\" in t for t in _texts):\n                n += 1\n                continue                                          # 근거 문서에 없는 중도해지 이자 손실 서술 제거\n            kp.append(st)\n        if ln.strip() and not \"\".join(kp).strip():\n            continue\n", "            if re.search(r\"중도\\s*해지[^.!?]{0,20}이자[^.!?]{0,10}손실\", st) and not any((\"중도해지\" in t or \"중도상환\" in t) and \"이자\" in t for t in _texts):\n                n += 1\n                continue                                          # 근거 문서에 없는 중도해지 이자 손실 서술 제거\n            # v13.33(G11): '원리금보장형 상품이 가장 적합할 것입니다' → 후보 표현; 재예치·만기상환 서술은 근거 문서에 그 단어가 있을 때만\n            if re.search(r\"원리금\\s*보장형?\\s*상품이\\s*가장\\s*적합\", st):\n                st = re.sub(r\"원리금\\s*보장형?\\s*상품이\\s*가장\\s*적합할?\\s*(?:것입니다|합니다|수\\s*있습니다)\",\n                            \"자료상 원금 보장을 중시할 때 검토할 수 있는 후보는 원리금보장형 상품입니다\", st)\n                n += 1\n            if re.search(r\"재예치|만기상환\", st) and not any((\"재예치\" in t or \"만기상환\" in t) for t in _texts):\n                n += 1\n                continue\n            kp.append(st)\n        if ln.strip() and not \"\".join(kp).strip():\n            continue\n"], ["                             \"안정형 후보로 보기 어렵습니다(참고용 후보).\")\n            _added += 1\n        if not re.search(r\"보장되지\\s*않|보장은\\s*아님|보장되는\\s*상품은\\s*아닙\", _p):\n            _lines.append(\" - 유의: 이 상품은 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n            _added += 1\n        _paras[_pi] = \"\\n\".join(_lines)\n    if _added:\n", "                             \"안정형 후보로 보기 어렵습니다(참고용 후보).\")\n            _added += 1\n        if not re.search(r\"보장되지\\s*않|보장은\\s*아님|보장되는\\s*상품은\\s*아닙\", _p):\n            _nm = _pm.group(0).strip(\"·-\")\n            _lines.append(f\" - 유의: {_nm}은(는) 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")   # 상품명 포함(중복 제거 회피)\n            _added += 1\n        _paras[_pi] = \"\\n\".join(_lines)\n    if _added:\n"], ["    CLAIM = re.compile(r\"원금(?:이|을|은|의)?\\s*(?:보장|보전|보호|유지)|거의\\s*확실|가장\\s*안전|안전성이\\s*(?:가장\\s*)?높|안전한?\\s*(?:옵션|선택|상품|자산)|무위험|\"\n                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_HI = \"실적배당형이며 변동성이 큰 편으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    HI_RISK = re.compile(r\"높은\\s*위험|매우\\s*높은|고위험|(?:위험\\s*등급\\s*)?\\(?\\s*[12]\\s*등급|주식형|주식에\\s*주로\")\n    _para_text = {}\n", "    CLAIM = re.compile(r\"원금(?:이|을|은|의)?\\s*(?:보장|보전|보호|유지)|거의\\s*확실|가장\\s*안전|안전성이\\s*(?:가장\\s*)?높|안전한?\\s*(?:옵션|선택|상품|자산)|무위험|\"\n                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자|장기\\s*성장\\s*가능성이\\s*큽|장기적으로\\s*(?:더\\s*)?큰\\s*수익|\"\n                       r\"성장\\s*잠재력이\\s*큽\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_HI = \"실적배당형이며 변동성이 큰 편으로, 장기 투자자가 검토할 수 있으나 투자원금과 수익(분배금)은 보장되지 않으며 손실 가능성이 있습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    HI_RISK = re.compile(r\"높은\\s*위험|매우\\s*높은|고위험|(?:위험\\s*등급\\s*)?\\(?\\s*[12]\\s*등급|주식형|주식에\\s*주로\")\n    _para_text = {}\n"], ["        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _canon_done = False\n        for st in sents:\n            if CRITERIA.search(st) or re.search(r\"어렵|않|아니|없습니다\", st) and not CLAIM.search(st):\n                fixed.append(st); continue                       # 투자자 조건 서술·부정문은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n", "        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _canon_done = False\n        for st in sents:\n            _has_prod = bool(_PROD_CITE.search(st) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,}\", st))\n            if (CRITERIA.search(st) and not _has_prod) or (re.search(r\"어렵|않|아니|없습니다\", st) and not CLAIM.search(st)):\n                fixed.append(st); continue                       # 투자자 조건 서술(상품명 없음)·부정문은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n"], ["            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                new = \"이 상품은 \" + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            n += 1\n", "            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                _ppm = _PROD_CITE.search(_para_text.get(_line_para[_li], \"\")) or \\\n                       re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _para_text.get(_line_para[_li], \"\"))\n                _sj = (_ppm.group(0).strip(\"·-\") + \"은(는) \") if _ppm else \"이 상품은 \"   # 문단의 상품명을 주어로(전역 중복 제거 회피)\n                new = _sj + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            n += 1\n"], ["    # K) v13.9(골든 전수 판독 3건 — 전부 결정적 판정):\n    #    K1 (G11) 최상위 번호 목록이 1부터 연속이 아니면(예: [2], [1,3]) 1부터 재번호.\n    #       앞 항목이 다른 필터에 지워져 '2.'만 남는 고아 번호. 이미 연속([1,2,3] 또는 [1,2,3,1,2] 두 목록)이면 불변.\n    _lines = ans.split(\"\\n\")\n    _idx = [(i, int(m.group(1))) for i, ln in enumerate(_lines)\n            for m in [re.match(r\"^(\\d{1,2})\\.\\s\", ln)] if m]\n", "    # K) v13.9(골든 전수 판독 3건 — 전부 결정적 판정):\n    #    K1 (G11) 최상위 번호 목록이 1부터 연속이 아니면(예: [2], [1,3]) 1부터 재번호.\n    #       앞 항목이 다른 필터에 지워져 '2.'만 남는 고아 번호. 이미 연속([1,2,3] 또는 [1,2,3,1,2] 두 목록)이면 불변.\n    # K0 v13.33(M2 실측): 내용 없는 번호 항목('3. 세제 혜택을 … 경우:' 뒤에 불릿·문장이 없이 빈 줄/다음 항목) 제거 —\n    #    항목 안의 문장이 다른 규칙으로 제거되면 머리말도 함께 없앤다(번호·상품·근거는 한 묶음).\n    _lines = ans.split(\"\\n\")\n    _keep_idx = []\n    for _i, _ln in enumerate(_lines):\n        if re.match(r\"^\\s*\\d{1,2}[.)]\\s*\\**[^\\n]{0,80}[:：]\\s*\\**\\s*$\", _ln) or re.match(r\"^\\s*\\d{1,2}[.)]\\s*$\", _ln):\n            _nxt = next((x for x in _lines[_i + 1:] if x.strip()), \"\")\n            if not _nxt or re.match(r\"^\\s*\\d{1,2}[.)]\\s\", _nxt) or _nxt.lstrip().startswith(\"※\") or _nxt.lstrip().startswith(\"[참고 문서]\") \\\n                    or (not re.match(r\"^\\s*[-•·*]|^\\s{1,}\", _nxt) and _lines[_i + 1:_i + 2] == [\"\"]):\n                continue                                        # 머리말만 있고 본문 없음\n        _keep_idx.append(_i)\n    ans = \"\\n\".join(_lines[_i] for _i in _keep_idx)\n    ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", ans)\n    _lines = ans.split(\"\\n\")\n    _idx = [(i, int(m.group(1))) for i, ln in enumerate(_lines)\n            for m in [re.match(r\"^(\\d{1,2})\\.\\s\", ln)] if m]\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "K0 v13.33(M2 실측)" in src: print("[스킵] 이미 v13.33 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1332과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1332_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
