# -*- coding: utf-8 -*-
"""patch_v1366.py — v1365(7ec65eb6) main.py 에 v13.66 적용. (v1365 적용 후 실행)
  [T11 리뷰 반영] ① 구조화 대체 목록: '하나' 요구면 '현재 정보로는 확정하지 않는다'를 먼저, 등급 체계 출처 명시(투자설명서 1~6등급)
   ② trace에 '구조화 대체 목록(투자설명서 직접 판독)' 명시, 출처 줄을 목록이 인용한 투자설명서+비R2 문서로 정밀화
   ③ 조사 정합(은/는) 최종 패스(전 유형) ④ B12 판정 문장 '요건 충족 시 환급·과세이연' ⑤ 추천 표현 '안정적인 수익 기대'·'가장 안전한' 완화
자동백업·자가검증. 이미 v13.66이면 스킵. 검증 md5==8f832807b9d89a766357f99f8077d414"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='7ec65eb61995f570ed4f759a4425122e'
EXPECT_AFTER='8f832807b9d89a766357f99f8077d414'
HUNKS=[["    _lines_ = []\n    for _n, _r in _items[:4]:\n        _n = re.sub(r\"(\\d)\\s+호\", r\"\\1호\", _n)                       # v13.62: 원문 띄어쓰기('1 호') 정리\n        _lab = (_r.get(\"label\") or _LABEL_BY_GRADE.get(_r[\"grade\"], \"\")).replace(\" \", \"\")\n        _lines_.append(f\"- {_n}: 위험등급 {_r['grade']}등급({_lab}) — 근거 문서: {_r['src']}\\n - 유의: {_n}은(는) 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n    if _lines_:\n        return (\"문의하신 조건에 맞는 상품을 제공된 자료 안에서 확정하지 못했습니다. 검색된 투자설명서에서 확인되는 상품과 위험등급은 다음과 같으며, \"\n                \"후보로 검토하실 수 있습니다(1등급이 가장 높은 위험, 6등급이 가장 낮은 위험):\\n\\n\" + \"\\n\".join(_lines_) +\n                (\"\\n\\n\" + (\"질문하신 대로 하나를 고르려면 \" if re.search(r\"하나|한\\s*개|한\\s*가지|1개\", _hint_q) else \"후보를 좁히려면 \") +\n                 \"연령·투자성향 외에 투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)가 더 필요합니다. 알려주시면 제공된 자료 범위에서 후보를 좁혀 드리겠습니다.\"))   # v13.62(T11): 역질문 마무리\n", "    _lines_ = []\n    _RC_FB_SRCS.clear()\n    for _n, _r in _items[:4]:\n        _n = re.sub(r\"(\\d)\\s+호\", r\"\\1호\", _n)                       # v13.62: 원문 띄어쓰기('1 호') 정리\n        _lab = (_r.get(\"label\") or _LABEL_BY_GRADE.get(_r[\"grade\"], \"\")).replace(\" \", \"\")\n        _lines_.append(f\"- {_n}: 위험등급 {_r['grade']}등급({_lab}) — 근거 문서: {_r['src']}\\n - 유의: {_n}{_josa(_n)} 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n        _RC_FB_SRCS.append(_r[\"src\"])\n    if _lines_:\n        _one = bool(re.search(r\"하나|한\\s*개|한\\s*가지|1개\", _hint_q))\n        _head = ((\"질문하신 대로 '하나'를 고르기에는 현재 정보(연령·투자성향)만으로는 부족해 특정 상품을 확정하지 않습니다. \" if _one else\n                  \"문의하신 조건에 맞는 상품을 제공된 자료 안에서 확정하지 못했습니다. \") +\n                 \"검색된 투자설명서에서 코드가 직접 확인한 상품과 위험등급은 다음과 같으며, 후보로 검토하실 수 있습니다\"\n                 \"(투자설명서의 위험등급 체계: 1등급 매우 높은 위험 ~ 6등급 매우 낮은 위험):\\n\\n\")          # v13.66(T11): 확정 불가를 먼저, 등급 체계 출처 명시\n        return (_head + \"\\n\".join(_lines_) +\n                (\"\\n\\n\" + (\"하나로 좁히려면 \" if _one else \"후보를 좁히려면 \") +\n                 \"투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)가 더 필요합니다. 알려주시면 제공된 자료 범위에서 후보를 좁혀 드리겠습니다.\"))   # v13.62(T11): 역질문 마무리\n"], ["def _rc_fallback_body(used, _hint_q):\n", "_RC_FB_SRCS = []                                                  # v13.66: 구조화 대체 목록이 인용한 출처(출처 줄 정밀화용)\n\n\ndef _josa(nm):\n    \"\"\"v13.66: 상품명 뒤 주격 조사 — 마지막 글자의 받침으로 '은'/'는' 결정(한글·숫자), 판정 불가(영문 끝)는 '은(는)'.\"\"\"\n    _s = re.sub(r\"[\\s)\\]'\\\"」*]+$\", \"\", nm)\n    if not _s:\n        return \"은(는)\"\n    _c = _s[-1]\n    if \"가\" <= _c <= \"힣\":\n        return \"은\" if (ord(_c) - 0xAC00) % 28 else \"는\"\n    if _c.isdigit():\n        return \"은\" if _c in \"013678\" else \"는\"\n    return \"은(는)\"\n\n\ndef _rc_fallback_body(used, _hint_q):\n"], ["    note = (f\" 추천 렌더러(블록 {st['blk']}개 구조화, 교정 {st['fix']}건\" + (f\", 등급 미확인 블록 {_drop}개 제외\" if _drop else \"\") + \")\") if (st[\"blk\"] or st[\"fix\"] or _drop) else \"\"\n", "    note = (f\" 추천 렌더러(블록 {st['blk']}개 구조화, 교정 {st['fix']}건\" + (f\", 등급 미확인 블록 {_drop}개 제외\" if _drop else \"\") +\n            (\" → 구조화 대체 목록(투자설명서 직접 판독)\" if st.get(\"fallback\") else \"\") + \")\") if (st[\"blk\"] or st[\"fix\"] or _drop) else \"\"\n"], ["            _notes = [l for l in body.split(\"\\n\") if l.lstrip().startswith(\"※\")]\n            body = _rc_fallback_body(used, _question_hint[0] if _question_hint else \"\")\n", "            _notes = [l for l in body.split(\"\\n\") if l.lstrip().startswith(\"※\")]\n            body = _rc_fallback_body(used, _question_hint[0] if _question_hint else \"\")\n            st[\"fallback\"] = True\n            for s_ in _RC_FB_SRCS:\n                if s_ not in cited:\n                    cited.append(s_)\n"], ["            ans, _rr, _rr_cited = render_recommend(ans, used, _rr_allow)\n            clean_note += _rr\n", "            ans, _rr, _rr_cited = render_recommend(ans, used, _rr_allow)\n            clean_note += _rr\n            if \"구조화 대체 목록\" in _rr and src_tail:                # v13.66(T11): 목록이 인용하지 않은 투자설명서는 출처 줄에서 제외(근거 정밀도)\n                _keep = list(dict.fromkeys(list(_RC_FB_SRCS) + [s for s in _rr_allow if not s.startswith(\"R2_\")]))\n                if _keep:\n                    src_tail = \"[참고 문서] \" + \", \".join(_keep)\n                    clean_note += \" 출처 줄 정밀화\"\n"], ["        if src_tail:                      # v9.25: 출처 줄을 맨 끝으로 되돌린다\n", "        ans = re.sub(r\"([가-힣0-9])([)\\]]?)\\s*은\\(는\\)\", lambda m_: m_.group(1) + m_.group(2) + _josa(m_.group(1)), ans)   # v13.66: 조사 정합\n        if src_tail:                      # v9.25: 출처 줄을 맨 끝으로 되돌린다\n"], ["                           \"60일 이내면 IRP 입금 후 퇴직소득세 환급 대상이 되고, 지났으면 환급·과세이연 절차를 적용받기 어려울 수 있습니다.\").format((_mel.group(2) + \" 달\") if _mel.group(2) else f\"{_mon}개월\")\n", "                           \"60일 이내에 입금하면 요건 충족 시 퇴직소득세 환급·과세이연을 적용받을 수 있고, 지났으면 환급·과세이연 절차를 적용받기 어려울 수 있습니다.\").format((_mel.group(2) + \" 달\") if _mel.group(2) else f\"{_mon}개월\")   # v13.66\n"], ["    (re.compile(r\"(?:에|에게)\\s*(?:잘\\s*)?어울리는\\s*(상품|펀드)입니다\"), r\"에서 검토할 수 있는 \\1입니다\"),\n", "    (re.compile(r\"(?:에|에게)\\s*(?:잘\\s*)?어울리는\\s*(상품|펀드)입니다\"), r\"에서 검토할 수 있는 \\1입니다\"),\n    (re.compile(r\"안정적인\\s*수익(?:을|이)?\\s*(?:기대할\\s*수\\s*있|추구할\\s*수\\s*있|제공할\\s*수\\s*있|얻을\\s*수\\s*있|올릴\\s*수\\s*있)\"), \"상대적으로 낮은 변동성을 목표로 하지만 수익이 보장되지는 않\"),   # v13.66\n    (re.compile(r\"가장\\s*안전한\\s*(상품|펀드|선택)\"), r\"상대적으로 위험등급이 낮은 \\1\"),\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def _josa(" in src: print("[스킵] 이미 v13.66 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[중단] 적용전 md5가 예상 v1365와 다름 → 서버 파일이 갈라짐. main_v1366_full.py 로 통째 교체하세요."); sys.exit(4)
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1365_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
