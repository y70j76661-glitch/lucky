# -*- coding: utf-8 -*-
"""patch_v1350.py — v1349(5d6986d2) main.py 에 v13.50 적용. (v1349 적용 후 실행)
  [계약 5종 — mini27 실측] ① ComparisonContract: 요약 문장의 근거 밖 수치 제거, 비교표 위험등급 행을 근거(정답표→투자설명서)로 확정,
   '보다 높/낮' 우열 문장은 근거 등급으로 검증(불일치·검증불가 제거), 우열 결론은 코드 생성(같으면 '동일'), 펀드 비교엔 비보장 고지
   ② EntityConsistencyContract: 추천 블록의 머리말 상품과 다른 상품 줄 제거, 블록마다 비보장 고지, 브랜드(에셋플러스 등) 인식, 번호 항목 문단 경계
   ③ PremiseSubjectContract: 전제 교정문의 주어를 질문의 계좌명으로 ④ PartialSafetyContract: 공격 구문만 제거하고 남은 정상 질문 처리(부분 거절)
   ⑤ QuestionRelevanceContract: 과세제외금액 고지는 질문이 인출·세제 문맥일 때만 ⑥ 개인정보 절 제거 후 검색, 접근 로그 비활성 ⑦ 명퇴수당 절세 근거 청크
자동백업·자가검증. 이미 v13.50이면 스킵. 검증 md5==8dd1cf6ffabd87b7f9a815de759a1db4"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='5d6986d2a70a45224158427b9ff491d5'
EXPECT_AFTER='8dd1cf6ffabd87b7f9a815de759a1db4'
HUNKS=[["def _grade_hint(nm):\n    \"\"\"상품명 → 'hi'(1·2등급/높은위험) | 'lo' | None\"\"\"", "def _grade_evidence(nm):\n    \"\"\"v13.50: 상품명 → 'N등급(라벨)' 또는 None. 정답표 → 코퍼스 R2_ 투자설명서(전체 표기, 없으면 브랜드·법적형태를 뗀 핵심 토큰으로 재대조).\n    문서마다 등급이 다르면 None(단정하지 않는다).\"\"\"\n    fact = next((f for f in PRODUCT_FACTS if re.search(f[\"key\"], nm)), None)\n    if fact and re.match(r\"\\d\", fact[\"grade\"]):\n        return fact[\"grade\"]\n    key = re.sub(r\"\\s+\", \"\", nm).strip(\"·-'\\\"\")\n    if len(key) < 4:\n        return None\n    core = re.sub(r\"미래에셋|삼성|자산운용|증권자?투자신탁|자투자신탁|투자신탁|모투자신탁|제?\\d+호|\\[[^\\]]*\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\)|[·\\s\\-']\", \"\", key)\n    for k in ([key] + ([core] if len(core) >= 4 and core != key else [])):\n        grades = set()\n        for s, t in _src_norm_items():\n            if k in t:\n                g, lab = _read_grade(_src_text(s))\n                if g is not None:\n                    grades.add((g, (lab or _LABEL_BY_GRADE.get(g, \"\")).replace(\" \", \"\")))\n        if len(grades) == 1:\n            g, lab = next(iter(grades))\n            return f\"{g}등급({lab})\" if lab else f\"{g}등급\"\n        if grades:\n            return None\n    return None\n\n\ndef _cmp_header_products(ans):\n    \"\"\"비교표 머리말의 상품명 목록: '| 항목 | A | B |' 또는 '(비교 항목 — A vs B)'\"\"\"\n    for ln in ans.splitlines():\n        if ln.lstrip().startswith(\"|\") and re.search(r\"\\|\\s*(?:비교\\s*)?항목\\s*\\|\", ln):\n            cells = [c.strip() for c in ln.strip().strip(\"|\").split(\"|\")]\n            return [c for c in cells[1:] if c]\n        m = re.match(r\"^\\s*\\((?:비교\\s*)?항목\\s*[—-]\\s*(.+)\\)\\s*$\", ln)\n        if m:\n            return [c.strip() for c in re.split(r\"\\s+vs\\s+\", m.group(1)) if c.strip()]\n    return []\n\n\ndef _grade_hint(nm):\n    \"\"\"상품명 → 'hi'(1·2등급/높은위험) | 'lo' | None\"\"\""], ["        if n_scrub:\n            ans = \"\\n\".join(_sl)\n            clean_note += f\" 근거 밖 수치 칸 {n_scrub}개 정리\"\n", "        if n_scrub:\n            ans = \"\\n\".join(_sl)\n            clean_note += f\" 근거 밖 수치 칸 {n_scrub}개 정리\"\n        # v13.50(T6 실측): 비교 답변의 요약 문장(표 밖)에 든 소수 수치가 사용 근거 문서에 없으면 그 문장을 지운다\n        #   ('삼성클래식연금은 0.47%입니다' — 표는 자료 없음인데 요약이 창작). 표 행·※·카드·항목 줄은 제외.\n        if system is COMPARE_SYSTEM:\n            _sl2, _nsum = [], 0\n            for _ln in ans.splitlines():\n                _st = _ln.strip()\n                if not _st or _st.startswith((\"|\", \"※\", \"[\", \"(\", \"-\")) or \"|\" in _ln:\n                    _sl2.append(_ln); continue\n                _lead = re.match(r\"^\\s*(?:\\d+[.)]\\s*)?\", _ln).group(0)\n                _ss = re.split(r\"(?<=[.!?])\\s+\", _ln[len(_lead):])\n                _kp = []\n                for _s in _ss:\n                    _nums = re.findall(r\"\\d+\\.\\d+\", _s.replace(\",\", \"\"))\n                    if _nums and any(not re.search(r\"(?<![\\d.])\" + re.escape(x) + r\"(?![\\d])\", _ctxnum) for x in _nums):\n                        _nsum += 1; continue\n                    _kp.append(_s)\n                if _kp:\n                    _sl2.append(_lead + \" \".join(_kp))\n            if _nsum:\n                ans = \"\\n\".join(_sl2)\n                clean_note += f\" 근거 밖 수치 요약문장 {_nsum}건 제거\"\n"], ["        # v9.99f: 카테고리 비교(TDF·원리금보장형 등)라 원문 상품을 못 찾으면\n        #   risk_prof가 비어 위 블록이 통째로 건너뛴다.", "        # v13.50(T6 실측): 비교표 머리말의 상품마다 근거 등급(정답표 → 코퍼스 투자설명서)을 코드가 읽어 위험등급 행을 확정한다.\n        #   셀의 숫자가 근거와 다르거나 숫자가 없을 때만 바꾸고, 모든 상품이 확정되면 '원문 대조 불가' 고지를 제거한다.\n        #   요약의 '보다 높/낮' 문장은 근거 등급과 어긋나거나(등급이 같은데 우열) 검증 불가면 제거한다.\n        if system is COMPARE_SYSTEM:\n            _hp = _cmp_header_products(ans)\n            _ge = [(_nm, _grade_evidence(_nm)) for _nm in _hp] if 2 <= len(_hp) <= 4 else []\n            if _ge:\n                _lines = ans.split(\"\\n\"); _nfix = 0\n                for _i, _ln in enumerate(_lines):\n                    _is_md = _ln.lstrip().startswith(\"|\") and re.search(r\"\\|\\s*위험\\s*등급\\s*\\|\", _ln)\n                    _is_ls = re.match(r\"^\\s*-\\s*위험\\s*등급\\s*[:：]\", _ln)\n                    if not (_is_md or _is_ls):\n                        continue\n                    if _is_md:\n                        _cells = [c.strip() for c in _ln.strip().strip(\"|\").split(\"|\")]\n                        _vals = _cells[1:]\n                    else:\n                        _vals = [c.strip() for c in re.sub(r\"^\\s*-\\s*위험\\s*등급\\s*[:：]\\s*\", \"\", _ln).split(\";\")]\n                    if len(_vals) != len(_ge):\n                        break\n                    for _j, (_nm, _g) in enumerate(_ge):\n                        if not _g:\n                            continue\n                        _d = re.search(r\"(?<![\\d.])([1-6])\\s*등급\", _vals[_j])\n                        if not _d or _d.group(1) != _g[0] or re.search(r\"\\d\\s*등급[^;]*\\d\\s*등급\", _vals[_j]):\n                            _vals[_j] = _g; _nfix += 1\n                    _lines[_i] = (\"| 위험등급 | \" + \" | \".join(_vals) + \" |\") if _is_md else (\"- 위험등급: \" + \"; \".join(_vals))\n                    break\n                if _nfix:\n                    ans = \"\\n\".join(_lines)\n                    clean_note += f\" 위험등급 행 근거 확정 {_nfix}칸\"\n                if all(g for _, g in _ge) and RISK_WARN_MARK in ans:\n                    ans = re.sub(r\"\\n*\\[위험등급 원문 대조[^\\n]*\", \"\", ans)\n                    clean_note += \" (근거 등급 확정으로 대조 불가 고지 제거)\"\n                # 요약의 우열 문장 검증: 주어 상품이 '더 높/낮'이라 주장 — 근거 등급으로 참·거짓 판정(1등급이 가장 높은 위험)\n                _known = {_nm: int(_g[0]) for _nm, _g in _ge if _g}\n                if len(_known) >= 2:\n                    _out, _ndrop = [], 0\n                    def _mention_pos(_s, _k):\n                        _kk = re.sub(r\"\\s+\", \"\", _k)\n                        _ss_ = re.sub(r\"\\s+\", \"\", _s)\n                        for _L in (len(_kk), 12, 8, 6):\n                            _p = _ss_.find(_kk[:_L])\n                            if _p >= 0:\n                                return _p\n                        return -1\n                    for _ln in ans.split(\"\\n\"):\n                        if _ln.lstrip().startswith((\"|\", \"※\", \"[\", \"(\", \"-\")) or not re.search(r\"등급|위험\", _ln):\n                            _out.append(_ln); continue\n                        _lead = re.match(r\"^\\s*(?:\\d+[.)]\\s*)?\", _ln).group(0)\n                        _kp = []\n                        for _s in re.split(r\"(?<=[.!?])\\s+\", _ln[len(_lead):]):\n                            _m = re.search(r\"보다\\s*(?:위험(?:등급|도)?이\\s*)?(?:더\\s*)?(높|낮)\", _s)\n                            if _m and re.search(r\"등급|위험\", _s):\n                                _ments = sorted([(_mention_pos(_s, _k), _k) for _k in _hp if _mention_pos(_s, _k) >= 0])\n                                _subj = _ments[0][1] if _ments else None\n                                _oth = [_k for _k in _hp if _k != _subj]\n                                if not _subj or _subj not in _known or any(_o not in _known for _o in _oth):\n                                    _ndrop += 1; continue                 # 검증 불가 → 단정 문장 제거\n                                _hi = _m.group(1) == \"높\"\n                                _truth = all((_known[_subj] < _known[_o]) if _hi else (_known[_subj] > _known[_o]) for _o in _oth)\n                                if not _truth:\n                                    _ndrop += 1; continue\n                            _kp.append(_s)\n                        if _kp:\n                            _out.append(_lead + \" \".join(_kp))\n                    if _ndrop:\n                        ans = \"\\n\".join(_out)\n                        clean_note += f\" 등급 우열 단정 {_ndrop}건 제거(근거 등급과 불일치·검증 불가)\"\n                    # 우열 결론은 코드가 근거 등급으로 만든다(질문이 등급 우열을 물었을 때)\n                    if re.search(r\"위험\\s*등급|등급\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"등급 비교(근거 문서 기준)\" not in ans:\n                        _gs = sorted(_known.items(), key=lambda kv: kv[1])\n                        if len({v for _, v in _known.items()}) == 1:\n                            _concl = \"등급 비교(근거 문서 기준): \" + \", \".join(f\"{k} {v}등급\" for k, v in _gs) + \" — 위험등급이 같으므로 등급만으로는 우열을 가릴 수 없습니다.\"\n                        else:\n                            _lo = [k for k, v in _gs if v == _gs[-1][1]]\n                            _concl = (\"등급 비교(근거 문서 기준): \" + \", \".join(f\"{k} {v}등급\" for k, v in _gs) +\n                                      f\" — 숫자가 클수록 위험이 낮으므로, 위험등급이 더 낮은 상품은 {' / '.join(_lo)}입니다.\")\n                        _body_, _tail_ = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n                        ans = _body_.rstrip() + \"\\n\\n\" + _concl + ((\"\\n\\n[참고 문서]\" + _tail_) if \"[참고 문서]\" in ans else \"\")\n                        clean_note += \" 등급 우열 결론 코드 생성\"\n            # 비교한 상품이 모두 펀드(투자신탁·펀드·ETF·TDF)인데 비보장 고지가 없으면 한 줄 붙인다(안정형 질문 대응)\n            if _hp and all(re.search(r\"투자신탁|펀드|ETF|TDF\", _n) for _n in _hp) and not re.search(r\"보장되지\\s*않|보장하지\\s*않\", ans):\n                ans = ans.rstrip() + \"\\n\\n※ 비교한 상품은 모두 실적배당형(펀드)으로, 투자원금과 수익(분배금)은 보장되지 않으며 운용 결과에 따라 손실이 발생할 수 있습니다.\"\n                clean_note += \" 펀드 비보장 고지\"\n        # v9.99f: 카테고리 비교(TDF·원리금보장형 등)라 원문 상품을 못 찾으면\n        #   risk_prof가 비어 위 블록이 통째로 건너뛴다."], ["        if _fire and not any(k in question for k in _OPER_Q) \\\n                and not re.search(r\"받지\\s*않은\\s*납입\\s*(?:금|원금)\", ans) \\\n                and not re.search(r\"과세\\s*제외\", ans):", "        if _fire and not re.search(r\"인출|해지|출금|환매|중도|세액공제|과세|세금|기타소득|절세|공제\", question):\n            _fire = False                                         # v13.50(T13 실측): DB/DC 제도 질문에 기타소득세 고지가 붙던 무관 정보 차단\n        if _fire and not any(k in question for k in _OPER_Q) \\\n                and not re.search(r\"받지\\s*않은\\s*납입\\s*(?:금|원금)\", ans) \\\n                and not re.search(r\"과세\\s*제외\", ans):"], ["            _f01 = (\"IRP(연금계좌) 자체가 원금을 보장하는 것은 아닙니다. 계좌 안에서 예금 등 원리금보장상품을 \"", "            _acct = next((k for k in (\"IRP\", \"연금저축\", \"퇴직연금\", \"DC\") if k in question), \"연금계좌\")   # v13.50: 질문의 계좌명으로\n            _f01 = (f\"{_acct}(연금계좌) 자체가 원금을 보장하는 것은 아닙니다. 계좌 안에서 예금 등 원리금보장상품을 \""], ["    # v11.1 흔들림 고정: ISA 연간 납입한도(doc23) — 실측 G14 flaky", "    # v13.50(T14 과제소개서 예시): 명퇴수당·퇴직금을 연금계좌로 → 퇴직소득세 감면(30%) 근거 청크(doc26 절세 예시) 보강\n    ((\"명퇴\", \"명예퇴직\", \"퇴직금\", \"퇴직급여\", \"퇴직수당\", \"퇴직소득\"),\n     (\"절세\", \"세금\", \"세제\", \"감면\", \"혜택\", \"아끼\", \"줄이\", \"덜\"),\n     (\"30% 절세\", \"30%가 감면\", \"30% 감면\", \"30%를 감면\", \"퇴직소득세의 30%\", \"7만 9,200원\", \"30%가 절세\"), 1),\n    # v11.1 흔들림 고정: ISA 연간 납입한도(doc23) — 실측 G14 flaky"], ["    q_in, _pii = mask_pii(question or \"\")\n    out = _answer_core(question_id, q_in, _retried)", "    q_in, _pii = mask_pii(question or \"\")\n    if _pii:\n        # v13.50(T17 실측): 가린 개인정보 절('제 [주민등록번호 비공개]는 …이고')은 검색·생성을 흐린다 → 질문 절만 남긴다\n        _segs = [s_ for s_ in re.split(r\"(?<=[,.!?])\\s+|(?<=인데|이고|이며|인데요)[,\\s]+\", q_in) if s_.strip()]\n        _keep = [s_ for s_ in _segs if \"비공개]\" not in s_]\n        if _keep and len(re.sub(r\"\\s+\", \"\", \" \".join(_keep))) >= 6:\n            q_in = \" \".join(_keep).strip()\n    out = _answer_core(question_id, q_in, _retried)"], ["    _line_para, _pi = [], 0\n    for _l in _all_lines:\n        if not _l.strip():\n            _pi += 1\n        _line_para.append(_pi)\n    _para_fund = {}", "    _line_para, _pi = [], 0\n    for _li0, _l in enumerate(_all_lines):\n        if not _l.strip() or (_li0 and re.match(r\"^\\s*\\d+[.)]\\s\", _l)):    # v13.50(T11 실측): 빈 줄 없이 이어진 번호 항목도 문단 경계\n            _pi += 1\n        _line_para.append(_pi)\n    _para_fund = {}"], ["_PROD_SPAN = re.compile(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|NH-Amundi|NH-아문디|한국밸류|미래에셋|삼성|KB|신한|하나|키움|교보|IBK|한화|우리)\"", "_PROD_SPAN = re.compile(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|NH-Amundi|NH-아문디|한국밸류|미래에셋|삼성|KB|신한|하나|키움|교보|IBK|한화|우리|에셋플러스|한국투자|신영|피델리티|슈로더|블랙록)\""], ["                        r\"|(?:TIGER|KODEX|ACE|KBSTAR|SOL|HANARO|ARIRANG|RISE)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}(?:\\s*ETF)?\")   # ETF 브랜드는 접미어 없이도", "                        r\"|(?:TIGER|KODEX|ACE|KBSTAR|SOL|HANARO|ARIRANG|RISE)\\s*(?:(?!은\\(는\\))[가-힣A-Za-z0-9()·\\-]){2,40}(?:\\s*ETF)?\")   # ETF 브랜드는 접미어 없이도(v13.50: '은(는)' 꼬리 제외)"], ["    _has_nm = lambda ls: any(core in re.sub(r\"\\s+\", \"\", l) for l in ls)\n    if not _has_nm(([head] if head is not None else []) + risk + others):\n        others.insert(0, ind + \"상품 예시: \" + nm)                    # 상품명이 유의 문장에만 남았으면 이름 줄을 세운다", "    _has_nm = lambda ls: any(core in re.sub(r\"\\s+\", \"\", l) for l in ls)\n    if not _has_nm(([head] if head is not None else []) + risk + others) \\\n            and not (head is not None and re.search(r\"투자신탁|펀드|ETF\", head)):   # v13.50: 머리말이 이미 상품명이면(정규식 밖 브랜드) 삽입 안 함\n        others.insert(0, ind + \"상품 예시: \" + nm)                    # 상품명이 유의 문장에만 남았으면 이름 줄을 세운다"], ["    for m in list(_PROD_SPAN.finditer(text)) + list(_PROD_CITE.finditer(text)):\n        raw = m.group(0).strip(\"·-'\\\"\")\n        c = _rr_core(raw)\n        if _rr_generic(c):\n            continue\n        g_ = next((g for g in groups if _rr_same(g[0][0], c)), None)", "    for m in list(_PROD_SPAN.finditer(text)) + list(_PROD_CITE.finditer(text)):\n        raw = re.sub(r\"은\\(는\\)$\", \"\", m.group(0).strip(\"·-'\\\"\"))\n        c = _rr_core(raw)\n        if _rr_generic(c):\n            continue\n        g_ = next((g for g in groups if _rr_same(g[0][0], c)), None)"], ["app = FastAPI(default_response_class=UTF8JSONResponse)", "app = FastAPI(default_response_class=UTF8JSONResponse)\ntry:                                                              # v13.50: 접근 로그(질의 URL 원문)가 서버 로그에 남지 않게 — 개인정보 보호\n    import logging as _lg\n    _lg.getLogger(\"uvicorn.access\").disabled = True\nexcept Exception:\n    pass"], ["    out = _answer_core(question_id, q_in, _retried)\n    out[\"question\"] = question                                    # 평가 질의 원문은 그대로 돌려준다", "    _partial = \"\"\n    if _ATTACK.search(q_in):\n        # v13.50(T16 실측) [부분 거절]: 공격 구문(문장/절)만 떼고, 남은 정상 연금 질문이 있으면 그것만 처리한다\n        _cl = [c_ for c_ in re.split(r\"(?<=[.!?])\\s+|\\s*(?:그리고|그럼|이제|또한|and)\\s+\", q_in) if c_.strip()]\n        _safe = [c_ for c_ in _cl if not _ATTACK.search(c_)]\n        _rest = \" \".join(_safe).strip()\n        if _rest and len(re.sub(r\"\\s+\", \"\", _rest)) >= 5 and re.search(r\"연금|IRP|퇴직|세액|공제|DC|DB|ISA|상품|펀드|ETF|한도|수령|세금|과세|인출|가입|보수|등급\", _rest):\n            q_in = _rest\n            _partial = (\"내부 설정이나 시스템 지시문은 공개할 수 없어 그 요청은 처리하지 않았습니다. \"\n                        \"함께 문의하신 연금 관련 내용은 아래에 제공된 자료를 근거로 안내해 드립니다.\\n\\n\")\n    out = _answer_core(question_id, q_in, _retried)\n    if _partial and out.get(\"answer\") != SECURITY_REFUSAL:\n        out[\"answer\"] = _partial + (out.get(\"answer\", \"\") or \"\").lstrip()\n        out[\"think_trace\"] = \"0.9) 프롬프트 공격 구문 제거(부분 거절) → 남은 질문만 처리 \" + (out.get(\"think_trace\", \"\") or \"\")\n    out[\"question\"] = question                                    # 평가 질의 원문은 그대로 돌려준다"], ["    head = None if _RR_BUL.match(lines[0]) else lines[0]\n    rest = lines[1:] if head is not None else lines\n    risk, others, caveats = [], [], []\n    for l in rest:\n        if _RR_RISKNOTE.match(l):\n            risk.append(l)\n        elif _RR_BUL.match(l) and _RR_CANON.search(l) and len(re.split(r\"(?<=[.!?])\\s+\", l.strip())) == 1:\n            caveats.append(l)\n        else:\n            others.append(l)\n    st[\"done\"] = True", "    head = None if _RR_BUL.match(lines[0]) else lines[0]\n    rest = lines[1:] if head is not None else lines\n    risk, others, caveats = [], [], []\n    for l in rest:\n        if _RR_RISKNOTE.match(l):\n            risk.append(l)\n        elif _RR_BUL.match(l) and _RR_CANON.search(l) and len(re.split(r\"(?<=[.!?])\\s+\", l.strip())) == 1:\n            caveats.append(l)\n        else:\n            others.append(l)\n    st[\"done\"] = True\n    # v13.50(T11 실측) [엔티티 일관성]: 머리말이 상품 A인데 유의 줄·'상품 예시' 줄이 다른 상품 B를 주어로 하면 그 줄은 다른 블록에서\n    #   샌 것 → 제거. (한 블록 = 한 상품)\n    if head is not None:\n        _hg = _rr_groups(head)\n        if len(_hg) == 1:\n            _hk = _hg[0][0][0]\n            def _other_entity(l_):\n                _g2 = _rr_groups(l_)\n                return bool(_g2) and not any(_rr_same(_hk, g[0][0]) for g in _g2)\n            _n0 = len(caveats) + len(others)\n            caveats = [l_ for l_ in caveats if not _other_entity(l_)]\n            others = [l_ for l_ in others if not (re.match(r\"^\\s*[-•·*]\\s*상품\\s*예시\\s*[:：]\", l_) and _other_entity(l_))]\n            st[\"fix\"] += _n0 - len(caveats) - len(others)\n            groups = [g for g in groups if _rr_same(_hk, g[0][0])] or groups"], ["    if caveats:\n        new.append(caveats[0])                                     # 표준 유의 문장은 블록당 하나, 맨 끝\n        st[\"fix\"] += len(caveats) - 1", "    if caveats:\n        new.append(caveats[0])                                     # 표준 유의 문장은 블록당 하나, 맨 끝\n        st[\"fix\"] += len(caveats) - 1\n    elif not any(_RR_CANON.search(l) for l in new) and not re.search(r\"예금|원리금\\s*보장|ELB|DLB\", p):\n        new.append(ind + f\"유의: {nm}은(는) 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")   # v13.50: 상품 블록마다 비보장 고지 보장\n        st[\"fix\"] += 1"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.50(T6 실측): 비교 답변의 요약" in src: print("[스킵] 이미 v13.50 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1349와 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1349_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
