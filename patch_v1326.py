# -*- coding: utf-8 -*-
"""patch_v1326.py — v1325(aea56ab9) main.py 에 v13.26 적용. (v1325 적용 후 실행)
  [원문 대조(x2_check·mini7) 결과 — 의미 오류 1 + 과대 서술 1 + 출처 연결 1 + 표현 완화]
   X2(doc39): '세액공제 받지 않은 납입금 … 가장 마지막에 인출' — 문서는 ①세액공제 받지 않은 돈 → ②퇴직금 → ③세액공제 받은 돈·수익
       순서. 반대 서술 문장을 문서 순서 문장으로 교체. 'ISA 전환금액은 세액공제 대상이 아님'(과대) → '세액공제를 받지 않은
       납입금(ISA전환금 포함) 범위에서 과세제외 재원'으로 교체.
   M2: 한 상품을 가리키는 문장의 '5등급 및 6등급' → 가장 가까이 언급된 정답표 상품의 등급으로 교정. '원금 손실 가능성을
       최소화합니다'·'원금 보전에 초점'·'투자자에게 알맞으며'·'상품이 좋습니다'·'목적에 부합' → 실적배당형(원금 비보장) 기준 완화
       (같은 줄에 펀드·투자신탁이 있으면 뒤 문장도 펀드 문맥으로 봄).
   N6: 연령-세율 근거 문서 탐색을 완화('연금소득세'+70세+5.5/4.4/3.3)하고 기동 시 '연령세율 근거 문서: …'를 출력. 답변에
       그 구간이 있으면 표기 문구와 무관하게 출처에 포함, 근거 문서를 못 찾은 경우엔 '문서 기준' 표현을 지운다.
자동백업·자가검증. 이미 v13.26이면 스킵. 검증 md5==4afadf8e3152564bad3d5b2d9b492016"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='aea56ab9f6fd64147fc28da787feac20'
EXPECT_AFTER='4afadf8e3152564bad3d5b2d9b492016'
HUNKS=[["_AGE_SRC = \"\"\nfor _c in (chunks if isinstance(chunks, list) else chunks.values()):\n    _t = re.sub(r\"\\s+\", \"\", _c.get(\"text\", \"\") if isinstance(_c, dict) else str(_c))\n    if \"70세\" in _t and \"5.5%\" in _t and \"4.4%\" in _t and \"3.3%\" in _t:\n        _AGE_SRC = _c.get(\"source\", \"\") if isinstance(_c, dict) else \"\"\n        break\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n# v13.13(일반 게이트 1): 상품명 대조용 코퍼스 블롭(공백 제거). Primary Source = 주입 문서 전체.\n_CORPUS_NORM = \"\\u0001\".join(re.sub(r\"\\s+\", \"\", (c.get(\"text\", \"\") if isinstance(c, dict) else str(c)))\n", "_AGE_SRC = \"\"\nfor _c in (chunks if isinstance(chunks, list) else chunks.values()):\n    _t = re.sub(r\"\\s+\", \"\", _c.get(\"text\", \"\") if isinstance(_c, dict) else str(_c))\n    if \"70세\" in _t and \"5.5\" in _t and \"4.4\" in _t and \"3.3\" in _t and \"연금소득세\" in _t:\n        _AGE_SRC = _c.get(\"source\", \"\") if isinstance(_c, dict) else \"\"\n        break\nprint(f\"연령세율 근거 문서: {_AGE_SRC or '(못 찾음)'}\")\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n# v13.13(일반 게이트 1): 상품명 대조용 코퍼스 블롭(공백 제거). Primary Source = 주입 문서 전체.\n_CORPUS_NORM = \"\\u0001\".join(re.sub(r\"\\s+\", \"\", (c.get(\"text\", \"\") if isinstance(c, dict) else str(c)))\n"], ["    for ln in ans.split(\"\\n\"):\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        for st in sents:\n            # v13.18(M2·G10): 실적배당형(펀드·채권형·위험등급 문맥)에 원금보장처럼 읽히는 표현도 완화\n            if _PB_FUND.search(st) and not _PB_SAFE.search(st):\n                st_new = st\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n", "    for ln in ans.split(\"\\n\"):\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _ln_fund = bool(_PB_FUND.search(ln))          # v13.26: 같은 줄(불릿)에 펀드·투자신탁이 있으면 뒤 문장도 펀드 문맥\n        for st in sents:\n            # v13.18(M2·G10): 실적배당형(펀드·채권형·위험등급 문맥)에 원금보장처럼 읽히는 표현도 완화\n            if (_PB_FUND.search(st) or _ln_fund) and not _PB_SAFE.search(st):\n                st_new = st\n                # v13.26(M2 실측): '원금 손실의 가능성을 최소화합니다'·'원금 보전에 초점'·'알맞으며'·'좋습니다'·'목적에 부합' —\n                #   펀드는 원리금보장상품이 아니므로 손실 가능성을 약하게 전달하는 표현을 문서 기준으로 완화\n                st_new = re.sub(r\"원금\\s*손실의?\\s*가능성을\\s*최소화(?:합니다|하는)\", \"주식형보다 변동성이 낮을 수 있으나 투자원금이 보장되는 상품은 아닙니다\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*초점을\\s*맞춥니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"투자자에게\\s*알맞으며\", \"투자자가 검토할 수 있으며(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"투자자에게\\s*알맞습니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"(저위험|매우\\s*저위험|안전한)\\s*상품이\\s*좋습니다\", r\"\\1 상품을 검토할 수 있습니다\", st_new)\n                st_new = re.sub(r\"안정적으로\\s*마련하려는\\s*목적에\\s*부합합니다\", \"마련하려는 목적으로 검토할 수 있으나, 실적배당형이라 원금 손실 가능성은 있습니다\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n"], ["            ans = ans.rstrip() + \"\\n\\n\" + _req\n            calc_note += \" (연금수령 요건 코드 보강)\"\n\n        # v13.24(N4b): IRP 세액공제 한도 질문에 900만원(합산)만 있고 연금저축 600만원 하위한도가 없으면 한 줄 보강\n        if qtype == \"세제\" and \"IRP\" in question and any(k in question for k in LIMIT_Q) \\\n                and any(k in question for k in LIMIT_Q2) and re.search(r\"900\\s*만\", ans) \\\n", "            ans = ans.rstrip() + \"\\n\\n\" + _req\n            calc_note += \" (연금수령 요건 코드 보강)\"\n\n        # v13.26(X2 실측·doc39): 인출 순서 반대 서술 교정 — 문서: ① 세액공제 받지 않은 돈 → ② 퇴직금 → ③ 세액공제 받은 돈·수익.\n        #   '세액공제를 받지 않은 … 가장 마지막/나중에 인출' 또는 '세액공제를 받은 … 먼저 인출'은 의미가 반대이므로 문서 순서 문장으로 교체.\n        _ORDER = (\"연금계좌 인출은 세법이 정한 순서를 따르며, 세액공제를 받지 않은 납입금이 먼저, 퇴직금이 그다음, \"\n                  \"세액공제를 받은 납입금과 운용수익이 가장 나중에 인출됩니다.\")\n        _b0, _no = re.subn(r\"[^.!?\\n]*(?:세액공제[를을]?\\s*받지\\s*않은|과세\\s*제외|과세제외|이러한\\s*금액)[^.!?\\n]*\"\n                            r\"(?:가장\\s*)?(?:마지막|나중)(?:에|으로)\\s*인출[^.!?\\n]*[.!?]\", _ORDER, ans)\n        _b0, _no2 = re.subn(r\"[^.!?\\n]*세액공제[를을]?\\s*받은[^.!?\\n]*(?:가장\\s*)?먼저\\s*인출[^.!?\\n]*[.!?]\", _ORDER, _b0)\n        if _no or _no2:\n            ans = _b0\n            calc_note += f\" (인출 순서 반대 서술 교정 {_no + _no2}건)\"\n        # v13.26(X2·doc39/doc33): 'ISA 전환금액은 세액공제 대상이 아니다'는 과대 서술(전환금 중 일부는 세액공제 대상) →\n        #   문서 표현으로 교체: 세액공제를 받지 않은 납입금(ISA전환금 포함)은 과세제외 재원\n        _b1, _ni = re.subn(r\"[^.!?\\n]*ISA[^.!?\\n]*전환[^.!?\\n]*세액공제\\s*대상이\\s*아[^.!?\\n]*[.!?]\",\n                            \"ISA 만기자금 전환금액도 세액공제를 받지 않은 납입금에 포함되는 범위에서 과세제외 재원에 해당합니다\"\n                            \"(전환금액 중 세액공제를 받은 부분은 제외).\", ans)\n        if _ni:\n            ans = re.sub(r\"(?m)^(\\s*\\d+\\.)(\\S)\", r\"\\1 \\2\", _b1)      # 번호 뒤 공백 복원\n            calc_note += \" (ISA 전환금 세액공제 서술 교정)\"\n        # v13.26(M2 실측): 상품 하나를 가리키는 문장의 'N등급 및 M등급' — 앞 400자 안의 정답표 상품이 하나면 그 등급으로 교정\n        def _grade_pair_fix(m):\n            _pre = ans[max(0, m.start() - 400):m.start()]\n            _best, _bend = None, -1\n            for f in PRODUCT_FACTS:                          # 가장 가까이(마지막에) 언급된 정답표 상품 하나를 고른다\n                for _mm in re.finditer(f[\"key\"], _pre):\n                    if _mm.end() > _bend:\n                        _best, _bend = f, _mm.end()\n            if _best:\n                _g = re.match(r\"(\\d)\\s*등급\", _best[\"grade\"])\n                if _g and _g.group(1) in (m.group(1), m.group(2)):\n                    return m.group(0)[:m.start(1) - m.start()] + _best[\"grade\"]\n            return m.group(0)\n        _b2 = re.sub(r\"(?:이|해당|위)\\s*상품은[^.\\n]{0,25}?(\\d)\\s*등급\\s*(?:및|과|또는|,)\\s*(\\d)\\s*등급\", _grade_pair_fix, ans)\n        if _b2 != ans:\n            ans = _b2\n            calc_note += \" (단일 상품 등급 쌍 교정)\"\n\n        # v13.24(N4b): IRP 세액공제 한도 질문에 900만원(합산)만 있고 연금저축 600만원 하위한도가 없으면 한 줄 보강\n        if qtype == \"세제\" and \"IRP\" in question and any(k in question for k in LIMIT_Q) \\\n                and any(k in question for k in LIMIT_Q2) and re.search(r\"900\\s*만\", ans) \\\n"], ["        ans = _final_cleanup(ans)\n        if ans != _pre:\n            clean_note += \" 9) 최종정리(글리치·중복·흉터)\"\n        if \"문서 기준 만 70세 미만 5.5%\" in ans and _AGE_SRC and src_tail and _AGE_SRC not in src_tail:\n            src_tail = src_tail.rstrip() + \", \" + _AGE_SRC          # v13.25: 코드 고정 문장의 근거 문서를 출처에 포함\n        if src_tail:                      # v9.25: 출처 줄을 맨 끝으로 되돌린다\n            ans = ans.rstrip() + \"\\n\\n\" + src_tail.strip()\n        if clean_note:\n", "        ans = _final_cleanup(ans)\n        if ans != _pre:\n            clean_note += \" 9) 최종정리(글리치·중복·흉터)\"\n        if _AGE_SRC and src_tail and _AGE_SRC not in src_tail \\\n                and re.search(r\"70\\s*세\\s*(?:미만|이상)[^\\n]{0,60}(?:5\\.5|4\\.4|3\\.3)\\s*%\", ans):\n            src_tail = src_tail.rstrip() + \", \" + _AGE_SRC          # v13.25/26: 연령-세율 문장의 근거 문서를 출처에 포함\n        elif not _AGE_SRC and re.search(r\"70\\s*세\\s*(?:미만|이상)[^\\n]{0,60}(?:5\\.5|4\\.4|3\\.3)\\s*%\", ans):\n            ans = re.sub(r\"문서\\s*기준(?:으로)?\\s*\", \"\", ans)          # 근거 문서를 못 찾으면 '문서 기준' 표현은 쓰지 않는다\n        if src_tail:                      # v9.25: 출처 줄을 맨 끝으로 되돌린다\n            ans = ans.rstrip() + \"\\n\\n\" + src_tail.strip()\n        if clean_note:\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.26(X2 실측" in src: print("[스킵] 이미 v13.26 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1325과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1325_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
