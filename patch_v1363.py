# -*- coding: utf-8 -*-
"""patch_v1363.py — v1362(97343b1c) main.py 에 v13.63 적용. (v1362 적용 후 실행)
  [mini39 실측] ① T11 근거 연결: 다른 상품의 투자설명서 본문에 이름만 나온 문서(수상 목록 등)로는 등급을 붙이지 않음(머리 영역 규칙) — 등급 없으면 '확인되지 않음' 표기
   ② C10: 근거 등급도 원문 대조도 없는 등급 칸은 고지 유무와 무관하게 '확정하지 못함'(열 단위 검증 집합), 모두 미확정이어도 우열 질문엔 코드 결론
   ③ C9: '아래 참고 문서과 아래 참고 문서' 흉터 정리, 'IRP에 집중 투자'→'집중 납입'(계좌≠상품), 코드 머리 문장 멱등 예외
   ④ S1: DB형 '퇴직금을 보장' 과장 → '급여 수준이 사전에 확정되는' ⑤ S3: '혜택을 철회하는 의미' 해석 문장 제거 ⑥ C6: 요건 안내문에 연금 종류별 상이 고지
자동백업·자가검증. 이미 v13.63이면 스킵. 검증 md5==7e08735da70d3200ed01aa5e261cfd12"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='97343b1c9e0c873b03a30a218640167e'
EXPECT_AFTER='7e08735da70d3200ed01aa5e261cfd12'
HUNKS=[["        t = re.sub(r\"\\s+\", \"\", _src_text(s))\n        if core not in t and not (len(_toks) >= 2 and all(w in t for w in _toks)):   # v13.51: 띄어쓰기·표기 차이 → 핵심 토큰 전부 포함으로 재대조\n            continue\n        srcs.append(s)\n        if s.startswith(\"R2_\"):\n            g, lab = _read_grade(_src_text(s))\n", "        t = re.sub(r\"\\s+\", \"\", _src_text(s))\n        if core not in t and not (len(_toks) >= 2 and all(w in t for w in _toks)):   # v13.51: 띄어쓰기·표기 차이 → 핵심 토큰 전부 포함으로 재대조\n            continue\n        srcs.append(s)\n        if s.startswith(\"R2_\"):\n            _hd = t[:3000]                                            # v13.63(T11 실측 '차이나리치투게더'→코리아리치투게더 설명서): 이름이 문서 머리(상품명 영역)에\n            if core not in _hd and not (len(_toks) >= 2 and all(w in _hd for w in _toks)):   #   없으면 다른 상품의 설명서 본문 언급 → 그 문서의 등급을 붙이지 않는다\n                continue\n            g, lab = _read_grade(_src_text(s))\n"], ["        new.append(ind + \"근거 문서: \" + \", \".join(srcs) + (f\" — 위험등급 {grade}\" if grade else \"\"))\n", "        new.append(ind + \"근거 문서: \" + \", \".join(srcs) + (f\" — 위험등급 {grade}\" if grade else \" — 위험등급: 제공 자료에서 확인되지 않음\"))   # v13.63\n"], ["    fixed, n, matched, unver = [cur[0]], 0, set(), 0\n    for h, val in zip(header[1:], cur[1:]):\n        hit_name, hit = _match_col(h, prof)\n        if not hit:\n            # v9.72: 등급을 말하고 있는데 원문에서 그 상품을 못 찾은 칸은 세어 둔다.\n            #   침묵하면 대조된 값처럼 보인다.\n            if re.search(r\"[1-6]\\s*등급\", val):\n                unver += 1\n            fixed.append(val)\n            continue\n        matched.add(hit_name)\n", "    fixed, n, matched, unver = [cur[0]], 0, set(), 0\n    _VERIFIED_COLS.clear()                                        # v13.63: 원문과 맞춘 열(머리말 상품명) — 비교 계약이 칸 단위로 참조\n    for h, val in zip(header[1:], cur[1:]):\n        hit_name, hit = _match_col(h, prof)\n        if not hit:\n            # v9.72: 등급을 말하고 있는데 원문에서 그 상품을 못 찾은 칸은 세어 둔다.\n            #   침묵하면 대조된 값처럼 보인다.\n            if re.search(r\"[1-6]\\s*등급\", val):\n                unver += 1\n            fixed.append(val)\n            continue\n        matched.add(hit_name)\n        _VERIFIED_COLS.add(re.sub(r\"\\s+\", \"\", h))\n"], ["def enforce_risk_row(ans, prof):\n    \"\"\"비교표의 위험등급 칸을 원문 값으로 다시 쓴다.\n", "_VERIFIED_COLS = set()                                            # v13.63: enforce_risk_row가 원문과 맞춘 열(공백 제거 상품명)\n\n\ndef _fn_scar(text):\n    \"\"\"v13.63(C9 실측): 파일명→'아래 참고 문서' 치환 뒤 남는 흉터 정리 — 'A과 B에 따르면'이 '아래 참고 문서과 아래 참고 문서에'가 된다.\"\"\"\n    text = re.sub(r\"(?:아래\\s*참고\\s*문서\\s*(?:과|와|,|·|및|그리고)\\s*)+아래\\s*참고\\s*문서\", \"아래 참고 문서\", text)\n    text = re.sub(r\"아래\\s*참고\\s*문서과\", \"아래 참고 문서와\", text)\n    return text\n\n\ndef enforce_risk_row(ans, prof):\n    \"\"\"비교표의 위험등급 칸을 원문 값으로 다시 쓴다.\n"], ["                            elif RISK_WARN_MARK in ans and re.search(r\"\\d\\s*등급\", _vals[_j]):\n                                _vals[_j] = \"자료에서 확정하지 못함\"; _nfix += 1                  # v13.58 [검증값 전파]: 원문 대조도 실패한 상품의 등급 셀은 값으로 두지 않는다\n                            continue\n", "                            elif re.search(r\"\\d\\s*등급\", _vals[_j]) and (RISK_WARN_MARK in ans or re.sub(r\"\\s+\", \"\", _nm) not in _VERIFIED_COLS):\n                                _vals[_j] = \"자료에서 확정하지 못함\"; _nfix += 1                  # v13.58/63 [검증값 전파]: 근거 등급도, 원문 대조도 없는 등급 칸은 값으로 두지 않는다(C10 '3등급')\n                            continue\n"], ["                    if len(_known) < len(_hp) and re.search(r\"위험\\s*등급|등급\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"우열을 확정할 수 없\" not in ans:\n", "                    if len(_known) < len(_hp) and re.search(r\"위험\\s*등급|등급|위험\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"우열을 확정할 수 없\" not in ans:\n"], ["                    if len(_known) == len(_hp) and re.search(r\"위험\\s*등급|등급\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"등급 비교(근거 문서 기준)\" not in ans:\n", "                    if len(_known) == len(_hp) and re.search(r\"위험\\s*등급|등급|위험\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"등급 비교(근거 문서 기준)\" not in ans:\n"], ["            # v13.60 [요청 속성만 렌더링]: 질문이 위험등급(또는 보수·수익률) 하나만 물으면 그 행만 남긴다(불필요 필드 생성 = 오류 면적)\n", "                # v13.63(C10): 머리말 상품 모두 등급 미확정인데 우열을 물었으면 코드 결론만 남긴다\n                if _hp and not _known and re.search(r\"위험\\s*등급|등급|위험\", question) and re.search(r\"낮|높|안전|덜|더\", question) and \"우열을 확정할 수 없\" not in ans:\n                    _body_, _tail_ = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n                    ans = (_body_.rstrip() + \"\\n\\n위험등급 우열: \" + \", \".join(_hp) + \"의 위험등급을 제공된 자료에서 확정하지 못해, 현재 자료만으로는 우열을 확정할 수 없습니다. \"\n                           \"가입 전 각 상품의 투자설명서에서 위험등급을 직접 확인하시기 바랍니다.\" + ((\"\\n\\n[참고 문서]\" + _tail_) if \"[참고 문서]\" in ans else \"\"))\n                    clean_note += \" 등급 전부 미확정 → 우열 확정 불가 고지\"\n            # v13.60 [요청 속성만 렌더링]: 질문이 위험등급(또는 보수·수익률) 하나만 물으면 그 행만 남긴다(불필요 필드 생성 = 오류 면적)\n"], ["            _fn_lines.append(_FNAME_IN.sub(\"아래 참고 문서\", _l))\n        ans = \"\\n\".join(_fn_lines)\n", "            _fn_lines.append(_FNAME_IN.sub(\"아래 참고 문서\", _l))\n        ans = _fn_scar(\"\\n\".join(_fn_lines))                       # v13.63(C9): '아래 참고 문서과 아래 참고 문서에' 흉터 정리\n"], ["        ans = \"\\n\".join(\n            _l if (_l.strip().startswith(\"[참고 문서]\") or \"근거 문서:\" in _l or \"(출처\" in _l)   # v13.43/55/60: 근거 문서 줄·인용 줄은 출처 표기\n            else _FNAME_IN.sub(\"아래 참고 문서\", _l)\n            for _l in ans.splitlines())\n", "        ans = _fn_scar(\"\\n\".join(\n            _l if (_l.strip().startswith(\"[참고 문서]\") or \"근거 문서:\" in _l or \"(출처\" in _l)   # v13.43/55/60: 근거 문서 줄·인용 줄은 출처 표기\n            else _FNAME_IN.sub(\"아래 참고 문서\", _l)\n            for _l in ans.splitlines()))\n"], ["        # v13.62(C6) [수령액 질문 가드 — 정보한계·역질문]: '연금 얼마 받아요?'처럼 금액을 묻는데 질문에 숫자 조건이 없으면\n", "        # v13.63(S1) [제도 서술 계약 — DB형 '보장' 과장]: DB형은 급여 수준이 사전에 확정되고 운용 책임을 회사가 지는 제도 —\n        #   '퇴직금을 보장해주는' 식의 무조건 보장 표현은 답변 안의 근거 문장(사전 확정·사용자 부담)으로 바꾼다.\n        _b15, _n15 = re.subn(r\"((?:DB형|확정급여형)[^.!?\\n]{0,30}?)(?:퇴직금|연금액|연금|급여)(?:을|를)?\\s*(?:무조건\\s*)?보장(?:해\\s*주는|해주는|하는|합니다|해\\s*줍니다|해줍니다|받는)\",\n                              lambda m_: m_.group(1) + \"급여 수준이 사전에 확정되는\" + (\"\" if m_.group(0).endswith(\"는\") else \" 구조입니다\"), ans)\n        if _n15:\n            ans = _b15; clean_note += f\" DB형 보장 과장 {_n15}건 교정\"\n        # v13.63(S3) [세제 서술 계약 — 해석 문장]: '세금 혜택을 철회하는 의미'처럼 근거 없는 해석 문장은 제거(과세 구조 문장은 그대로)\n        _b16, _n16 = re.subn(r\"\\s*[^.!?\\n]*(?:혜택을|혜택이|공제를|공제가)\\s*(?:철회|취소|박탈)[^.!?\\n]*[.!?]\", \"\", ans)\n        if _n16:\n            ans = _b16; clean_note += f\" 해석 문장 {_n16}건 제거\"\n        # v13.62(C6) [수령액 질문 가드 — 정보한계·역질문]: '연금 얼마 받아요?'처럼 금액을 묻는데 질문에 숫자 조건이 없으면\n"], ["                       \"참고로 자료에서 확인되는 일반적인 수령 요건은 다음과 같습니다.\\n\\n\" + ans)\n", "                       \"참고로 아래는 검색된 자료에 나온 수령 요건이며, 연금 종류(퇴직연금 DC·IRP·연금저축)에 따라 요건이 다를 수 있습니다.\\n\\n\" + ans)   # v13.63\n"], ["            ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(_b14)).strip()\n            if \"공제 한도만 보면\" not in ans:\n", "            ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(_b14)).strip()\n            ans = re.sub(r\"(IRP|연금저축)(에|으로)\\s*(?:집중\\s*)?투자(하되|하고|하면|합니다|하는)\", r\"\\1\\2 집중 납입\\3\", ans)   # v13.63: 계좌 납입≠상품 투자\n            if \"공제 한도만 보면\" not in ans:\n"], ["                if re.search(r\"나이대|은퇴까지\\s*남은\\s*기간\", _l) and re.search(r\"알려\\s*주시면|알려주시면\", _l):\n                    _n14 += 1; continue\n", "                if re.search(r\"나이대|은퇴까지\\s*남은\\s*기간\", _l) and re.search(r\"알려\\s*주시면|알려주시면\", _l):\n                    _n14 += 1; continue\n                if _l.lstrip().startswith(\"세액공제 한도 기준(\"):          # v13.63: 코드 머리 문장은 검사 제외(멱등)\n                    _b14.append(_l); continue\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.63(C9 실측): 파일명" in src: print("[스킵] 이미 v13.63 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1362와 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1362_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
