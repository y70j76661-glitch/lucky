# -*- coding: utf-8 -*-
"""patch_v1375.py — v1374(90e05c76) main.py 에 v13.75 적용. (v1374 적용 후 실행)
  [mini51 리뷰 반영] ① M2: '하나' 요구에 상황별 상품 나열이 남으면 → 확정 보류 + 대표 검토 후보 1개 + 비교 후보 구조로 재구성(렌더러)
   ② 대표 후보 선택 기준을 코드에 명시하고 답변에 표기: 공격형=위험등급 숫자 최소, 안정형=최대, 성향 없음=중간값(안정형 정렬 오류도 수정)
   ③ 표현: '높은 수익을 기대할 수 있는'·'성장 잠재력이 크지만' 완화, B11 '16.5퍼센트의 세금이 발생한다'→'적용될 수 있다', '자유롭게 인출해도 세금 없음' 완화, S4 '연금저축 IRP 계좌' 병기 정리
자동백업·자가검증. 이미 v13.75이면 스킵. 검증 md5==998897969141337dbcaca9c47eb19ef1"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='90e05c76e54e1ea84cfc7e7883a66746'
EXPECT_AFTER='998897969141337dbcaca9c47eb19ef1'
HUNKS=[["    _items = sorted(_prof.items(), key=lambda kv: kv[1][\"grade\"])\n    if _aggr:\n        _items = [kv for kv in _items if kv[1][\"grade\"] <= 3] or _items\n    elif _safe:\n        _items = [kv for kv in _items if kv[1][\"grade\"] >= 4] or _items\n    _lines_ = []\n", "    _items = sorted(_prof.items(), key=lambda kv: kv[1][\"grade\"])\n    if _aggr:\n        _items = [kv for kv in _items if kv[1][\"grade\"] <= 3] or _items          # 위험 높은 순(숫자 작은 순)\n        _crit = \"공격적 성향 조건에 맞춰, 확인된 후보 중 위험등급 숫자가 가장 낮은(위험이 가장 높은) 상품을 대표 후보로 두었습니다\"\n    elif _safe:\n        _items = sorted([kv for kv in _items if kv[1][\"grade\"] >= 4] or _items, key=lambda kv: -kv[1][\"grade\"])   # v13.75: 위험 낮은 순\n        _crit = \"안정 선호 조건에 맞춰, 확인된 후보 중 위험등급 숫자가 가장 높은(위험이 가장 낮은) 상품을 대표 후보로 두었습니다\"\n    else:\n        if len(_items) >= 3:                                                  # v13.75: 성향 정보 없음 → 위험등급 중간값을 대표 후보로\n            _mid = _items[len(_items) // 2]\n            _items = [_mid] + [kv for kv in _items if kv is not _mid]\n        _crit = \"투자 성향 정보가 없어, 확인된 후보 중 위험등급이 중간인 상품을 대표 후보로 두었습니다(성향을 알려주시면 다시 좁혀 드립니다)\"\n    _lines_ = []\n"], ["                     \"대표 검토 후보(조건부):\\n\" + _lines_[0] +\n", "                     \"대표 검토 후보(조건부 — \" + _crit + \"):\\n\" + _lines_[0] +\n"], ["    _drop = st.get(\"drop\", 0)\n", "    _hint_one = _question_hint[0] if _question_hint else \"\"\n    if re.search(r\"하나|한\\s*개|한\\s*가지|1개\", _hint_one) and len(re.findall(r\"근거 문서:[^\\n]*위험등급\\s*[1-6]\\s*등급\", body)) >= 2 and not st.get(\"fallback\") and \"대표 검토 후보\" not in body:\n        # v13.75(M2 실측): '하나 추천' 요구에 상황별 상품 나열은 요구 미충족 → 역질문 + 대표 검토 후보 1개 + 비교 후보 구조로 재구성\n        _notes = [l for l in body.split(\"\\n\") if l.lstrip().startswith(\"※\")]\n        _lead_paras = []\n        for _pp in body.split(\"\\n\\n\"):\n            if not _pp.strip() or _pp.lstrip().startswith(\"※\"):\n                continue\n            if _rr_groups(_pp) or re.match(r\"^\\s*\\d+[.)]\\s\", _pp) or re.search(r\"알려\\s*주시면|말씀해\\s*주시면|추천을?\\s*드리|후보로\\s*검토|다음과\\s*같은|추천(?:해|드릴)|살펴보겠습니다\", _pp):\n                break\n            _lead_paras.append(_pp.strip())\n        body = (\"\\n\\n\".join(_lead_paras) + \"\\n\\n\" if _lead_paras else \"\") + _rc_fallback_body(used, _hint_one)\n        for s_ in _RC_FB_SRCS:\n            if s_ not in cited:\n                cited.append(s_)\n        if _notes:\n            body = body.rstrip() + \"\\n\\n\" + \"\\n\".join(_notes)\n        st[\"fallback\"] = True\n        st[\"fix\"] += 1\n    _drop = st.get(\"drop\", 0)\n"], ["    (re.compile(r\"(?:장기적으로\\s*)?(?:높은\\s*)?성장\\s*가능성(?:을|이)\\s*(?:제공|약속)(?:합니다|하며|하고|할\\s*수\\s*있습니다)\"), \"성장 가능성이 있으나 손실 가능성도 있습니다\"),   # v13.74(M2)\n", "    (re.compile(r\"(?:장기적으로\\s*)?(?:높은\\s*)?성장\\s*가능성(?:을|이)\\s*(?:제공|약속)(?:합니다|하며|하고|할\\s*수\\s*있습니다)\"), \"성장 가능성이 있으나 손실 가능성도 있습니다\"),   # v13.74(M2)\n    (re.compile(r\"(?:장기적으로\\s*)?높은\\s*수익을\\s*기대할\\s*수\\s*있는\"), \"수익 가능성이 있으나 손실 위험도 있는\"),   # v13.75(M2)\n    (re.compile(r\"성장\\s*잠재력이\\s*(?:크|높)(?:지만|으나|고|습니다)\"), \"성장 가능성이 있을 수 있으나\"),\n"], ["                ans = re.sub(r\"(?:전체|모든)\\s*(?:해지|인출)\\s*금액(?:에\\s*대해|에)\\s*(?:기타소득세\\s*)?16\\.5\\s*%\", \"세액공제를 받은 납입금과 운용수익에 기타소득세 16.5%\", ans)\n                clean_note += f\" 인출 재원별 과세 구조(항목 {_drop8}개 대체)\"\n", "                ans = re.sub(r\"(?:전체|모든)\\s*(?:해지|인출)\\s*금액(?:에\\s*대해|에)\\s*(?:기타소득세\\s*)?16\\.5\\s*%\", \"세액공제를 받은 납입금과 운용수익에 기타소득세 16.5%\", ans)\n                clean_note += f\" 인출 재원별 과세 구조(항목 {_drop8}개 대체)\"\n            ans = re.sub(r\"16\\.5\\s*%의\\s*세금이\\s*발생한다\", \"16.5%가 적용될 수 있다\", ans)                                                  # v13.75(B11): 단정 → 조건부\n            ans = re.sub(r\"자유롭게\\s*인출(?:해도|하더라도|할\\s*수\\s*있으며)\\s*(?:세금이\\s*)?(?:부과되지\\s*않습니다|없습니다)\",\n                         \"그 금액 자체에는 기타소득세가 부과되지 않을 수 있으나, 인출 절차와 계좌 규정은 별도로 확인해야 합니다\", ans)\n"], ["        ans = re.sub(r\"연금계좌\\s+계좌\", \"연금계좌\", ans)\n", "        ans = re.sub(r\"연금계좌\\s+계좌\", \"연금계좌\", ans)\n        ans = re.sub(r\"연금저축\\s+IRP\\s*계좌\", \"연금저축·IRP 등 연금계좌\", ans)                        # v13.75(S4): 어색한 병기 정리\n"], ["    p = \"\\n\".join(lines)\n    groups = _rr_groups(p)\n    head = None if _RR_BUL.match(lines[0]) else lines[0]\n", "    p = \"\\n\".join(lines)\n    if re.match(r\"^\\s*(?:비교 후보|대표 검토 후보)\", lines[0]):\n        return lines                                              # v13.75: 코드가 만든 후보 목록 블록은 재구성하지 않는다(멱등)\n    groups = _rr_groups(p)\n    head = None if _RR_BUL.match(lines[0]) else lines[0]\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.75(M2 실측)" in src: print("[스킵] 이미 v13.75 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[중단] 적용전 md5가 예상 v1374와 다름 → 서버 파일이 갈라짐. main_v1375_full.py 로 통째 교체하세요."); sys.exit(4)
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1374_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
