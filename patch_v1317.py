# -*- coding: utf-8 -*-
"""patch_v1317.py — v1316(58ab6a5a) main.py 에 v13.17 적용.
  [답변 전문 정독(52 계산 + open + 상품 + OOD + meta)에서 나온 사실·논리·표기 결함 수정]
   ① 근본 원인: '= **39.6만 원**'처럼 굵게가 붙은 계산식을 교정 정규식이 못 잡아 틀린 식이 남음(E7·M3b·G2 공통)
      → 식 정규식이 **를 허용, 2차 교정 전 ** 제거.
   ② 금액 표기 통일(fmt_credit): 공제액은 정수면 '99만원', 소수면 사사오입 첫째 자리 '140.3만원'으로 카드·요약·본문 통일.
      본문의 98.84/98.8·123.75/123.8·140.25/140.2·'99.0'·'N만 M천원' 변형을 공제 문맥에서 기준 표기로 치환.
   ③ E7: 옛 규정(고소득자 연금저축 한도 300만원)·'소득 구간에 따라 한도가 다름'·'전액 공제 불가'(대상액=납입액) 문장 제거.
   ④ D9: 부분 분해식(500+400=900)은 보존, 합계 산술 오류(149.5)는 정답으로, '한도를 초과하지 않기 때문'(납입>한도) 모순 교정.
   ⑤ N6: 연금소득세를 '1~10년차 5.5%…' 수령 연차로 쓴 형태 → 연령 기준 문장으로 교체(퇴직소득세 감면 연차 문장은 불변).
   ⑥ M2·OP2·OP3: LLM이 이미 확인 요청을 썼으면 코드 확인 질문 줄을 겹쳐 넣지 않음.
   ⑦ M5: 판촉 혜택 질문에 본문이 주제를 언급조차 안 하면(딴소리) 고지만 남김.  ⑧ G2 '(십만원 단위)' 오기, OP3 'IRP 900만원' 합산 기준 병기.
자동백업·자가검증. 이미 v13.17이면 스킵. 검증 md5==f5f2e220aa24ef4d22b4ee58b489baf3"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='58ab6a5acde6a401bc15c21e1f22260b'
EXPECT_AFTER='f5f2e220aa24ef4d22b4ee58b489baf3'
HUNKS=[["    return v, kind\n\n\ndef calc_summary_line(c):\n    \"\"\"v13.8: [5단계 결과 출력] 계산기 값으로 만든 한 줄 요약 — 답변 맨 앞에 코드가 고정한다.\n    LLM이 본문에서 어떤 표기를 쓰든 첫 줄은 결정적 계산 결과다(소득유형→세율→계좌별 한도→대상액→공제액).\"\"\"\n", "    return v, kind\n\n\ndef fmt_credit(v):\n    \"\"\"v13.17: 공제액 기준 표기 — 정수면 '99', 아니면 반올림(사사오입) 소수 첫째 자리 '140.3'. 카드·요약·본문 통일.\"\"\"\n    if abs(v - round(v)) < 0.005:\n        return f\"{int(round(v)):,}\"\n    return f\"{math.floor(v * 10 + 0.5) / 10:,.1f}\"\n\n\ndef calc_summary_line(c):\n    \"\"\"v13.8: [5단계 결과 출력] 계산기 값으로 만든 한 줄 요약 — 답변 맨 앞에 코드가 고정한다.\n    LLM이 본문에서 어떤 표기를 쓰든 첫 줄은 결정적 계산 결과다(소득유형→세율→계좌별 한도→대상액→공제액).\"\"\"\n"], ["    else:\n        mid = f\"납입 {paid:,}만원 → 공제 대상 {base:,}만원(한도 {int(c['limit']):,}만원 이내)\"\n    # '|'는 표 후처리가 표 행으로 오인하므로 쓰지 않는다\n    _cr = c['credit']\n    _cr_txt = (f\"{int(round(_cr)):,}\" if abs(_cr - round(_cr)) < 0.04\n               else (f\"{_cr:,.1f}\" if abs(_cr * 10 - round(_cr * 10)) < 0.04 else f\"{_cr:,.2f}\"))\n    return (f\"계산 결과 요약: {head} / {mid} / 예상 세액공제액 약 {_cr_txt}만원\"\n            f\"(= {base:,}만원 × {c['rate_txt']})\")\n\n", "    else:\n        mid = f\"납입 {paid:,}만원 → 공제 대상 {base:,}만원(한도 {int(c['limit']):,}만원 이내)\"\n    # '|'는 표 후처리가 표 행으로 오인하므로 쓰지 않는다\n    _cr_txt = fmt_credit(c['credit'])\n    return (f\"계산 결과 요약: {head} / {mid} / 예상 세액공제액 약 {_cr_txt}만원\"\n            f\"(= {base:,}만원 × {c['rate_txt']})\")\n\n"], ["    ]\n    if sub_line:\n        lines.append(sub_line)\n    lines.append(f\"- 예상 세액공제액: {int(base):,}만원 × {rate_txt} = 약 {credit:,.1f}만원\")\n    if paid > base:\n        lines.append(f\"- 참고: 공제 대상({int(base):,}만원)을 초과한 {int(paid - base):,}만원은 \"\n                     f\"세액공제 대상이 아님\")\n", "    ]\n    if sub_line:\n        lines.append(sub_line)\n    lines.append(f\"- 예상 세액공제액: {int(base):,}만원 × {rate_txt} = 약 {fmt_credit(credit)}만원\")\n    if paid > base:\n        lines.append(f\"- 참고: 공제 대상({int(base):,}만원)을 초과한 {int(paid - base):,}만원은 \"\n                     f\"세액공제 대상이 아님\")\n"], ["#   실제 오류: 400만+300만=700만인데 답변이 '합산 900만원 x 13.2% = 118.8만원'\n#   → 프롬프트 지시만으로는 막히지 않아 계산식 자체를 정규식으로 바로잡는다.\n_CALC_PAT = re.compile(\n    r\"([\\d,]+)\\s*만\\s*원?\\s*[x\\u00d7X*]\\s*([\\d.]+)\\s*%\\s*=\\s*(?:약\\s*)?([\\d,.]+)\\s*만\\s*(?:(\\d)\\s*천\\s*)?원?\")\n# v13.8(G2): '= 118만8천원'처럼 'N만 M천원' 표기의 결과값을 118이 아니라 118.8로 읽는다(글리치 방지)\n\n\n", "#   실제 오류: 400만+300만=700만인데 답변이 '합산 900만원 x 13.2% = 118.8만원'\n#   → 프롬프트 지시만으로는 막히지 않아 계산식 자체를 정규식으로 바로잡는다.\n_CALC_PAT = re.compile(\n    r\"\\**([\\d,]+)\\s*만\\s*원?\\**\\s*[x\\u00d7X*]\\s*\\**([\\d.]+)\\s*%\\**\\s*=\\s*\\**\\s*(?:약\\s*)?\\**([\\d,.]+)\\s*만\\s*(?:(\\d)\\s*천\\s*)?원?\\**\")\n# v13.17: '= **39.6만 원**'처럼 마크다운 굵게가 식에 붙어 있으면 교정이 통째로 건너뛰어졌다(실측 E7·M3b·G2).\n#   굵게는 맨 마지막에 지워지므로 최종 답변엔 틀린 식만 깨끗하게 남았다 → 식 정규식이 **를 허용한다.\n# v13.8(G2): '= 118만8천원'처럼 'N만 M천원' 표기의 결과값을 118이 아니라 118.8로 읽는다(글리치 방지)\n\n\n"], ["    if not calc:\n        return ans, \"\"\n    base, rate_txt, credit = calc[\"base\"], calc[\"rate_txt\"], calc[\"credit\"]\n    correct = f\"{int(base):,}만원 \\u00d7 {rate_txt} = 약 {credit:,.1f}만원\"\n    cnt = [0]\n\n    def _fix(m):\n        try:\n", "    if not calc:\n        return ans, \"\"\n    base, rate_txt, credit = calc[\"base\"], calc[\"rate_txt\"], calc[\"credit\"]\n    ctxt = fmt_credit(credit)                       # v13.17: 답변 안 공제액 기준 표기(카드·요약과 동일)\n    correct = f\"{int(base):,}만원 \\u00d7 {rate_txt} = 약 {ctxt}만원\"\n    cnt = [0]\n\n    # v13.17(D9): '500만원×16.5%=82.5만원 + 400만원×16.5%=66만원'처럼 대상액을 나눠 계산한 부분식은\n    #   (산술이 맞고 부분들의 합이 대상액이면) 정상이므로 보존한다. 그 외 대상액·세율·결과가 어긋난 식만 교체.\n    _parts = []\n    for _m in _CALC_PAT.finditer(ans):\n        try:\n            _parts.append(float(_m.group(1).replace(\",\", \"\")))\n        except ValueError:\n            pass\n    def _is_partial(a):\n        if a >= base - 0.5:\n            return False\n        others = list(_parts)\n        for i, b in enumerate(others):                  # 자기 자신은 한 번만 제외(300+300=600 자기합 방지)\n            if abs(b - a) < 0.5:\n                others.pop(i)\n                break\n        return any(abs((a + b) - base) < 0.5 for b in others)\n\n    def _fix(m):\n        try:\n"], ["        if abs(a - base) < 0.5 and abs(r - calc[\"rate\"] * 100) < 0.01 \\\n                and abs(c - credit) < 0.06:\n            return m.group(0)          # 계산기와 일치 → 그대로 둔다\n        cnt[0] += 1\n        return correct\n\n    ans = _CALC_PAT.sub(_fix, ans)\n\n    # (2) '한도(900만원)를 납입액으로 착각한' 값이 본문에 남아 있으면 바로잡는다\n    wrong = calc[\"limit\"] * calc[\"rate\"]\n    if abs(wrong - credit) > 0.05:\n        wpat = re.compile(r\"(?<![\\d.])\" + re.escape(f\"{wrong:,.1f}\") + r\"\\s*만\\s*원?\")\n        ans, k = wpat.subn(f\"{credit:,.1f}만원\", ans)\n        cnt[0] += k\n\n    # (3) '합산하여 900만원' 처럼 납입액을 한도로 바꿔 말한 문장을 통째로 교체\n", "        if abs(a - base) < 0.5 and abs(r - calc[\"rate\"] * 100) < 0.01 \\\n                and abs(c - credit) < 0.06:\n            return m.group(0)          # 계산기와 일치 → 그대로 둔다\n        if abs(r - calc[\"rate\"] * 100) < 0.01 and abs(a * r / 100 - c) < 0.06 and _is_partial(a):\n            return m.group(0)          # 올바른 부분 분해식 → 보존\n        cnt[0] += 1\n        return correct\n\n    ans = _CALC_PAT.sub(_fix, ans)\n    ans = re.sub(r\"\\(\\s*십만\\s*원\\s*단위\\s*\\)\", \"(만원 단위)\", ans)   # v13.17(G2): 단위 오기\n\n    # (2) '한도(900만원)를 납입액으로 착각한' 값이 본문에 남아 있으면 바로잡는다\n    wrong = calc[\"limit\"] * calc[\"rate\"]\n    if abs(wrong - credit) > 0.05:\n        wpat = re.compile(r\"(?<![\\d.])\" + re.escape(f\"{wrong:,.1f}\") + r\"\\s*만\\s*원?\")\n        ans, k = wpat.subn(f\"{ctxt}만원\", ans)\n        cnt[0] += k\n\n    # (3) '합산하여 900만원' 처럼 납입액을 한도로 바꿔 말한 문장을 통째로 교체\n"], ["                out.append(sent)\n        ans = \"\\n\".join(out)\n\n    # v13.8(F3): 반대 소득 구간으로 계산한 공제액(예: 종합소득 5,000만 → 16.5%로 99.0만원)이 본문에\n    #   남아 있으면 공제 문맥에서만 계산기 값으로 교정하고, '반대 세율을 적용한 결과' 단정 문장은\n    #   조건문(…면/경우)이 아닐 때만 계산기 판정 문장으로 교체한다.\n", "                out.append(sent)\n        ans = \"\\n\".join(out)\n\n    # v13.17(E7·D9): 계산기와 모순되는 한도 서술 정리 — 옛 규정(고소득자 연금저축 한도 300만원)·\n    #   '전액 공제 불가'(대상액=납입액인데)·'한도를 초과하지 않기 때문'(납입액>한도인데) 문장은 제거.\n    _sents = re.split(r\"(?<=[.!?])(?<!\\d\\.)\\s+|\\n\", ans)\n    _keep = []\n    for _s in _sents:\n        _n = re.sub(r\"\\s+\", \"\", _s)\n        drop = False\n        if re.search(r\"한도[가는은]?\\s*300만\\s*원|300만\\s*원[^.]{0,10}한도\", _s) and calc[\"limit\"] != 300:\n            drop = True                                       # 옛 규정 300만원 한도\n        elif \"소득구간에따라세액공제한도가다르\" in _n or \"소득에따라세액공제한도가다르\" in _n:\n            drop = True                                       # 현행 한도는 소득과 무관(600/900)\n        elif abs(base - calc[\"paid\"]) < 0.5 and re.search(r\"전액[^.]{0,12}(?:받을수없|공제되지않|공제받을수없)\", _n):\n            drop = True                                       # 납입액 전부가 대상인데 '전액 불가'\n        elif calc[\"paid\"] > calc[\"limit\"] + 0.5 and re.search(r\"초과하지않\", _n) and f\"{int(calc['limit']):,}만\" in _s.replace(\" \", \"\"):\n            _s = (f\"합산 납입액 {int(calc['paid']):,}만원은 세액공제 한도 {int(calc['limit']):,}만원을 초과하므로 \"\n                  f\"{int(calc['limit']):,}만원까지만 공제 대상입니다.\")\n            cnt[0] += 1\n        if drop:\n            cnt[0] += 1\n            continue\n        _keep.append(_s)\n    ans = \"\\n\".join(_keep)\n\n    # v13.8(F3): 반대 소득 구간으로 계산한 공제액(예: 종합소득 5,000만 → 16.5%로 99.0만원)이 본문에\n    #   남아 있으면 공제 문맥에서만 계산기 값으로 교정하고, '반대 세율을 적용한 결과' 단정 문장은\n    #   조건문(…면/경우)이 아닐 때만 계산기 판정 문장으로 교체한다.\n"], ["        def _oc_fix(m):\n            if re.search(r\"공제|세액|환급|돌려\", ans[max(0, m.start() - 40):m.start()]):\n                cnt[0] += 1\n                return f\"약 {credit:,.1f}만원\"\n            return m.group(0)\n        for _f in _forms:\n            ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{_f}\\s*만\\s*원\", _oc_fix, ans)\n", "        def _oc_fix(m):\n            if re.search(r\"공제|세액|환급|돌려\", ans[max(0, m.start() - 40):m.start()]):\n                cnt[0] += 1\n                return f\"약 {ctxt}만원\"\n            return m.group(0)\n        for _f in _forms:\n            ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{_f}\\s*만\\s*원\", _oc_fix, ans)\n"], ["        if m.group(2): v += int(m.group(2)) / 10\n        if m.group(3): v += int(m.group(3)) / 10000\n        return v\n    _lo, _hi = base * 0.10, base * 0.20\n    def _amt_fix(m):\n        v = _amt_val(m)\n        if _lo <= v <= _hi and abs(v - credit) > 0.11:\n            cnt[0] += 1\n            return f\"약 {credit:,.1f}만원\" if abs(credit * 10 - round(credit * 10)) < 0.04 else f\"약 {credit:,.2f}만원\"\n        return m.group(0)\n    _out2 = []\n    for _ln in ans.split(\"\\n\"):\n        if re.search(r\"세액공제액|공제액|돌려받|예상\\s*금액|환급\", _ln) and correct not in _ln:\n            _ln = _AMT.sub(_amt_fix, _ln)\n            _ln = re.sub(r\"약\\s*약\\s*\", \"약 \", _ln)\n        _out2.append(_ln)\n    ans = \"\\n\".join(_out2)\n\n    # v13.7(G5): 계산기 정답이 소수(118.8)인데 LLM이 반올림 정수(약 119만원)만 썼으면 소수 정답으로 복원.\n    #   K6 때 지적된 안전망의 부분 문자열 결함('119' in '119.28')도 함께 제거 — 표기 형태로 판정한다.\n    _c1 = f\"{credit:,.1f}\"\n    _ci = int(credit)\n    _cf = int(round((credit - _ci) * 10))\n    if abs(credit - _ci) > 0.04 and _c1 not in ans:\n        def _round_fix(m):\n            # 세액공제 문맥(앞 40자에 공제·세액·환급·돌려)일 때만 — 납입액 등 다른 금액 오교정 방지\n            if re.search(r\"공제|세액|환급|돌려\", ans[max(0, m.start() - 40):m.start()]):\n                cnt[0] += 1\n                return f\"약 {_c1}만원\"\n            return m.group(0)\n        for _cand in {math.floor(credit), math.ceil(credit)}:\n            ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{_cand}\\s*만\\s*원\", _round_fix, ans)\n    _has = (_c1 in ans) \\\n        or (abs(credit - _ci) <= 0.04 and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\", ans)) \\\n        or (_cf and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\\s*{_cf}\\s*천\", ans))\n    # v13.8: 정답 금액이 본문에 없어도 여기서 문장을 덧붙이지 않는다 — 호출부가 계산 요약 줄을\n    #   항상 맨 앞에 고정하므로(calc_summary_line) 중복 없이 결정적 결과가 보장된다.\n    if not _has:\n        cnt[0] += 1\n    if cnt[0]:\n        return ans, f\" (계산식 코드 교정 {cnt[0]}건)\"\n    return ans, \"\"\n", "        if m.group(2): v += int(m.group(2)) / 10\n        if m.group(3): v += int(m.group(3)) / 10000\n        return v\n    _lo, _hi = base * 0.05, base * 0.20               # v13.17: 옛 한도(300만)로 계산한 39.6 같은 값도 범위 안\n    def _amt_fix(m):\n        v = _amt_val(m)\n        if re.search(r\"(?:IRP|연금저축|납입|추가|한도)\\s*$\", ans_line_ctx[0][max(0, m.start() - 8):m.start()]):\n            return m.group(0)                                  # 납입액·한도 금액은 대상 아님\n        if _lo <= v <= _hi and abs(v - credit) > 0.11:\n            cnt[0] += 1\n            return f\"약 {ctxt}만원\"\n        return m.group(0)\n    ans_line_ctx = [\"\"]\n    _out2 = []\n    for _ln in ans.split(\"\\n\"):\n        ans_line_ctx[0] = _ln\n        if re.search(r\"세액공제|공제액|돌려받|예상\\s*금액|환급\", _ln) and correct not in _ln:\n            _ln = _AMT.sub(_amt_fix, _ln)\n            _ln = re.sub(r\"약\\s*약\\s*\", \"약 \", _ln)\n        _out2.append(_ln)\n    ans = \"\\n\".join(_out2)\n\n    # v13.7(G5)+v13.17: 정답의 다른 표기(반올림 정수·소수 둘째 자리·은행가 반올림·'N만 M천원'·'.0')를 전부 기준 표기로 통일.\n    #   공제 문맥(앞 40자에 공제·세액·환급·돌려)일 때만 — 납입액 등 다른 금액 오교정 방지.\n    _c_canon = f\"약 {ctxt}만원\"\n    _variants = set()\n    _ci = int(credit)\n    if abs(credit - _ci) > 0.04:                                   # 소수 정답: 정수 반올림들도 변형\n        _variants.update({str(math.floor(credit)), str(math.ceil(credit))})\n    _variants.update({f\"{credit:.1f}\", f\"{credit:.2f}\", f\"{math.floor(credit*10+0.5)/10:.1f}\",\n                      f\"{credit:.1f}\".rstrip(\"0\").rstrip(\".\"), f\"{_ci}.0\"})\n    _variants.discard(ctxt)\n    _variants = {v for v in _variants if v}\n    def _var_fix(m):\n        if re.search(r\"공제|세액|환급|돌려\", ans[max(0, m.start() - 40):m.start()]):\n            cnt[0] += 1\n            return _c_canon\n        return m.group(0)\n    for _v in sorted(_variants, key=len, reverse=True):\n        ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{re.escape(_v)}\\s*만\\s*원\", _var_fix, ans)\n    # 'N만 M천원' / 'N만 M천 K백원' 형태\n    _cf = int(round((credit - _ci) * 10))\n    if _cf:\n        ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{_ci:,}\\s*만\\s*{_cf}\\s*천\\s*(?:\\d{{3}}\\s*)?원\", _var_fix, ans)\n    ans = re.sub(r\"약\\s*약\\s*\", \"약 \", ans)\n    if cnt[0]:\n        return ans, f\" (계산식 코드 교정 {cnt[0]}건)\"\n    return ans, \"\"\n"], ["            if rel == \"이하\": return _TABLE[r] != _expected(b)\n            return _TABLE[r] != _expected(b)                          # 이상/부터/없음 = 하한\n        _wrong = [m for m in _AGE_RATE.finditer(ans) if _pair_wrong(m)]\n        if _wrong:\n            _fixed = (\"연금소득세율(지방소득세 포함)은 문서 기준으로 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n", "            if rel == \"이하\": return _TABLE[r] != _expected(b)\n            return _TABLE[r] != _expected(b)                          # 이상/부터/없음 = 하한\n        _wrong = [m for m in _AGE_RATE.finditer(ans) if _pair_wrong(m)]\n        # v13.17(N6): '1~10년차 5.5% / 11~20년차 4.4% / 21년차 3.3%'처럼 퇴직소득세 감면 '연차'와 뒤섞은 형태.\n        #   연금소득세율은 연령 기준이지 수령 연차 기준이 아니다(문서). 같은 줄에 퇴직소득세·감면이 없을 때만.\n        _YEAR_RATE = re.compile(r\"\\d{1,2}\\s*(?:~|-|–)?\\s*\\d{0,2}\\s*년\\s*차[^\\n%]{0,15}?(5\\.5|4\\.4|3\\.3|5|4|3)\\s*%\")\n        _year_wrong = [m for m in _YEAR_RATE.finditer(ans)\n                       if not re.search(r\"퇴직소득세|감면\", ans[max(0, ans.rfind(\"\\n\", 0, m.start())): m.end()])]\n        if _year_wrong:\n            _fixed = (\"연금소득세율(지방소득세 포함)은 수령 연차가 아니라 연령 기준으로, 문서 기준 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n            _lines, _done = [], False\n            for _ln in ans.split(\"\\n\"):\n                if any(not re.search(r\"퇴직소득세|감면\", _ln) for m in _YEAR_RATE.finditer(_ln)) and _YEAR_RATE.search(_ln):\n                    if not _done:\n                        _lines.append(_fixed); _done = True\n                    continue\n                _lines.append(_ln)\n            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"연령에\\s*따라\\s*세율이\\s*다릅니다\", \"연령에 따라 세율이 다릅니다\", ans)\n        if _wrong:\n            _fixed = (\"연금소득세율(지방소득세 포함)은 문서 기준으로 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n"], ["                    continue\n                _lines.append(_ln)\n            ans = \"\\n\".join(_lines)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n    #    반복되면 첫 것만 남긴다(카드·보강·본문 어디서 왔든 동일 규칙). 유사 문장은 건드리지 않는다(퍼지 없음).\n    _seen_s, _outl = set(), []\n", "                    continue\n                _lines.append(_ln)\n            ans = \"\\n\".join(_lines)\n    # K4b v13.17(OP3): 'IRP는 연 900만원까지 공제'를 합산 기준 언급 없이 쓰면 오해 → 같은 문장에 '합산'이 없을 때만 보강\n    ans = re.sub(r\"(IRP[은는]\\s*연\\s*900\\s*만\\s*원(?:까지)?)(?![^.\\n]*합산)\", r\"\\1(연금저축과 합산 기준)\", ans)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n    #    반복되면 첫 것만 남긴다(카드·보강·본문 어디서 왔든 동일 규칙). 유사 문장은 건드리지 않는다(퍼지 없음).\n    _seen_s, _outl = set(), []\n"], ["\n        # (1-5) v9.31: 되묻기·성향확인인데 답변에 질문이 없으면 코드가 질문을 넣는다.\n        #   프롬프트에 맡기면 지시가 많을 때 질문이 통째로 빠진다(실측: R15/R35 2회 모두 실패).\n        if ask_line and \"?\" not in ans:\n            where, line = ask_line\n            ans = (line + \"\\n\\n\" + ans.lstrip()) if where == \"head\" \\\n                else (ans.rstrip() + \"\\n\\n\" + line)\n", "\n        # (1-5) v9.31: 되묻기·성향확인인데 답변에 질문이 없으면 코드가 질문을 넣는다.\n        #   프롬프트에 맡기면 지시가 많을 때 질문이 통째로 빠진다(실측: R15/R35 2회 모두 실패).\n        # v13.17(M2·OP2·OP3): LLM이 물음표 없이 확인 요청('…알려주시면', '여쭙')을 이미 썼으면 코드 줄을 겹쳐 넣지 않는다\n        if ask_line and \"?\" not in ans and not re.search(r\"여쭙|여쭤|알려주시면|말씀해\\s*주시|말씀해주시\", ans[:500]):\n            where, line = ask_line\n            ans = (line + \"\\n\\n\" + ans.lstrip()) if where == \"head\" \\\n                else (ans.rstrip() + \"\\n\\n\" + line)\n"], ["        #   답변이 그 혜택을 언급도, 한계 고지도 없이 일반 설명으로 넘어가면(실측 O4: 요구사항 미답)\n        #   맨 앞에 '자료에서 확인할 수 없다'를 한 줄 붙인다. 삭제 없음. 판촉 어휘가 없는 질문엔 불발.\n        _promo = re.search(r\"수수료\\s*면제|면제\\s*혜택|이벤트|프로모션|할인\\s*혜택|캐시백|경품|사은품\", question)\n        if _promo and not re.search(r\"면제|이벤트|프로모션|할인|캐시백|경품|사은품\", ans.split(\"[참고 문서]\")[0]) \\\n                and not _NOINFO.search(ans):\n            ans = (f\"문의하신 '{_promo.group(0)}' 혜택은 제공된 자료에서 확인할 수 없습니다. \"\n                   \"아래는 자료에서 확인되는 관련 내용입니다.\\n\\n\" + ans.lstrip())\n            clean_note += \" 판촉혜택 미확인 고지\"\n        # (1-6e) v13.13 일반 게이트 1: 문서에 없는 특정 상품명 → ※주석 병기(삭제 없음), 펀드 '원금 보장' 단정 완화\n        ans, _unk = annotate_unknown_products(ans, question)\n        if _unk:\n", "        #   답변이 그 혜택을 언급도, 한계 고지도 없이 일반 설명으로 넘어가면(실측 O4: 요구사항 미답)\n        #   맨 앞에 '자료에서 확인할 수 없다'를 한 줄 붙인다. 삭제 없음. 판촉 어휘가 없는 질문엔 불발.\n        _promo = re.search(r\"수수료\\s*면제|면제\\s*혜택|이벤트|프로모션|할인\\s*혜택|캐시백|경품|사은품\", question)\n        if _promo and not re.search(r\"면제|이벤트|프로모션|할인|캐시백|경품|사은품\", ans.split(\"[참고 문서]\")[0]):\n            # v13.17(M5): 판촉 혜택을 물었는데 본문이 그 주제를 한 번도 언급하지 않으면(중도인출 등 딴소리) 본문은\n            #   질문과 무관하므로 고지만 남긴다. 주제를 언급했으면 불변.\n            _tail = (\"\\n\\n[참고 문서]\" + ans.split(\"[참고 문서]\", 1)[1]) if \"[참고 문서]\" in ans else \"\"\n            ans = (f\"문의하신 '{_promo.group(0)}' 혜택은 제공된 자료에서 확인할 수 없습니다. \"\n                   \"제공된 자료에는 해당 혜택에 관한 내용이 없어 안내해 드리기 어렵습니다.\" + _tail)\n            clean_note += \" 판촉혜택 미확인 고지(본문 교체)\"\n        # (1-6e) v13.13 일반 게이트 1: 문서에 없는 특정 상품명 → ※주석 병기(삭제 없음), 펀드 '원금 보장' 단정 완화\n        ans, _unk = annotate_unknown_products(ans, question)\n        if _unk:\n"], ["        #   상태에서 본문만 정리하므로 출처 줄은 보호된다. 내용은 건드리지 않는다.\n        _pre = ans\n        # v13.16: 최종 정리 직전 산술 교정을 한 번 더(멱등). 실측 M3b: 앞선 교정 이후 남은 잘못된 식·금액 방지.\n        if calc_vals:\n            ans, _cn2 = enforce_calc(ans, calc_vals)\n            if _cn2:\n                calc_note += \" 최종 재검증\" + _cn2\n        ans, _na2 = fix_arith(ans)\n        ans = _final_cleanup(ans)\n        if ans != _pre:\n            clean_note += \" 9) 최종정리(글리치·중복·흉터)\"\n", "        #   상태에서 본문만 정리하므로 출처 줄은 보호된다. 내용은 건드리지 않는다.\n        _pre = ans\n        # v13.16: 최종 정리 직전 산술 교정을 한 번 더(멱등). 실측 M3b: 앞선 교정 이후 남은 잘못된 식·금액 방지.\n        ans = ans.replace(\"**\", \"\")          # v13.17: 굵게는 어차피 제거되므로 2차 교정 전에 먼저 벗긴다\n        if calc_vals:\n            ans, _cn2 = enforce_calc(ans, calc_vals)\n            if _cn2:\n                calc_note += \" 최종 재검증\" + _cn2\n        ans, _na2 = fix_arith(ans)\n        ans = re.sub(r\"약\\s*약\\s*\", \"약 \", ans)\n        ans = _final_cleanup(ans)\n        if ans != _pre:\n            clean_note += \" 9) 최종정리(글리치·중복·흉터)\"\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def fmt_credit(" in src: print("[스킵] 이미 v13.17 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1316과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1316_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
