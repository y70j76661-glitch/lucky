# -*- coding: utf-8 -*-
"""patch_v1365.py — v1364(8b1c4fcf) main.py 에 v13.65 적용. (v1364 적용 후 실행)
  [원칙 고정 3 — 재발 방지 장치] ① 세금·과세 답변: 계좌 내 매매·배당 과세 의도는 '즉시 과세 / 인출 시 과세 / 재원 / 예외'를 분리하는 표준 문장으로 —
     '양도소득세·배당소득세를 내지 않습니다' 무조건 면제 단정 교체, 근거 없는 손익통산·위탁계좌 비교 제거, 연금 외 수령 기타소득세 범위 고지(의도 기반)
   ② 상품 추천: 위험등급이 근거(정답표·해당 상품 투자설명서 머리)에서 확인된 펀드·ETF만 후보 — 미확인 블록은 제거하고 제외 사유 고지,
     후보가 없으면 코드가 투자설명서에서 직접 읽은 (상품, 등급, 출처) 목록 + 역질문(공용 함수 _rc_fallback_body)
   ③ 계좌별 요건 분리: 수령액 질문(조건 없음)은 계산 불가 고지 + 역질문 + '요건은 계좌 종류별로 다름'만 — LLM의 특정 계좌 요건 목록은 싣지 않음
자동백업·자가검증. 이미 v13.65이면 스킵. 검증 md5==7ec65eb61995f570ed4f759a4425122e"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='8b1c4fcfd9ad706a63924ab31be30bc0'
EXPECT_AFTER='7ec65eb61995f570ed4f759a4425122e'
HUNKS=[["        # 근거 미연결 상품을 지우고 나니 상품이 하나도 없다 → 설명 조각 대신 근거 자료 발췌로 대체(빈 추천 방지)\n        # v13.55(T11 실측): 원문 덤프 대신 검색된 투자설명서에서 코드가 읽은 (상품명, 위험등급, 출처) 목록으로 대체\n        _prof = extract_risk_profile(list(dict.fromkeys(c.get(\"source\", \"\") for c in used if str(c.get(\"source\", \"\")).startswith(\"R2_\"))))\n        _hint_q = _question_hint[0] if _question_hint else \"\"\n        _aggr = bool(re.search(r\"공격|고수익|높은\\s*수익|성장\", _hint_q)); _safe = bool(re.search(r\"안정|안전|원금|손해|손실\", _hint_q))\n        _items = sorted(_prof.items(), key=lambda kv: kv[1][\"grade\"])\n        if _aggr:\n            _items = [kv for kv in _items if kv[1][\"grade\"] <= 3] or _items\n        elif _safe:\n            _items = [kv for kv in _items if kv[1][\"grade\"] >= 4] or _items\n        _lines_ = []\n        for _n, _r in _items[:4]:\n            _n = re.sub(r\"(\\d)\\s+호\", r\"\\1호\", _n)                       # v13.62: 원문 띄어쓰기('1 호') 정리\n            _lab = (_r.get(\"label\") or _LABEL_BY_GRADE.get(_r[\"grade\"], \"\")).replace(\" \", \"\")\n            _lines_.append(f\"- {_n}: 위험등급 {_r['grade']}등급({_lab}) — 근거 문서: {_r['src']}\\n - 유의: {_n}은(는) 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n        if _lines_:\n            body = (\"문의하신 조건에 맞는 상품을 제공된 자료 안에서 확정하지 못했습니다. 검색된 투자설명서에서 확인되는 상품과 위험등급은 다음과 같으며, \"\n                    \"후보로 검토하실 수 있습니다(1등급이 가장 높은 위험, 6등급이 가장 낮은 위험):\\n\\n\" + \"\\n\".join(_lines_) +\n                    (\"\\n\\n\" + (\"질문하신 대로 하나를 고르려면 \" if re.search(r\"하나|한\\s*개|한\\s*가지|1개\", _hint_q) else \"후보를 좁히려면 \") +\n                     \"연령·투자성향 외에 투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)가 더 필요합니다. 알려주시면 제공된 자료 범위에서 후보를 좁혀 드리겠습니다.\"))   # v13.62(T11): 역질문 마무리\n        else:\n            body = (\"문의하신 조건에 맞는 특정 상품을 제공된 자료 안에서 확정하지 못했습니다. 현재 확인된 자료만으로 특정 상품을 추천하기보다, \"\n                    \"투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)를 추가로 확인한 뒤 후보를 좁히는 것이 적절합니다. 알려주시면 제공된 자료 범위에서 다시 확인해 드리겠습니다.\")\n        _fixed_box[0] += 1\n", "        # 근거 미연결 상품을 지우고 나니 상품이 하나도 없다 → 설명 조각 대신 근거 자료 발췌로 대체(빈 추천 방지)\n        # v13.55(T11 실측): 원문 덤프 대신 검색된 투자설명서에서 코드가 읽은 (상품명, 위험등급, 출처) 목록으로 대체 / v13.65: 함수로 분리(렌더러 공용)\n        body = _rc_fallback_body(used, _question_hint[0] if _question_hint else \"\")\n        _fixed_box[0] += 1\n"], ["def recommend_contract(ans, used):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n", "def _rc_fallback_body(used, _hint_q):\n    \"\"\"v13.65 [추천 후보 원칙]: 후보는 '검색된 투자설명서에서 코드가 직접 읽은 (상품명, 위험등급, 출처)'만 제시한다.\n    질문 성향(공격/안정)으로 등급 범위를 거르고, 정보 부족은 역질문으로 마무리한다. (LLM 호출 없음)\"\"\"\n    _prof = extract_risk_profile(list(dict.fromkeys(c.get(\"source\", \"\") for c in used if str(c.get(\"source\", \"\")).startswith(\"R2_\"))))\n    _aggr = bool(re.search(r\"공격|고수익|높은\\s*수익|성장\", _hint_q)); _safe = bool(re.search(r\"안정|안전|원금|손해|손실\", _hint_q))\n    _items = sorted(_prof.items(), key=lambda kv: kv[1][\"grade\"])\n    if _aggr:\n        _items = [kv for kv in _items if kv[1][\"grade\"] <= 3] or _items\n    elif _safe:\n        _items = [kv for kv in _items if kv[1][\"grade\"] >= 4] or _items\n    _lines_ = []\n    for _n, _r in _items[:4]:\n        _n = re.sub(r\"(\\d)\\s+호\", r\"\\1호\", _n)                       # v13.62: 원문 띄어쓰기('1 호') 정리\n        _lab = (_r.get(\"label\") or _LABEL_BY_GRADE.get(_r[\"grade\"], \"\")).replace(\" \", \"\")\n        _lines_.append(f\"- {_n}: 위험등급 {_r['grade']}등급({_lab}) — 근거 문서: {_r['src']}\\n - 유의: {_n}은(는) 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n    if _lines_:\n        return (\"문의하신 조건에 맞는 상품을 제공된 자료 안에서 확정하지 못했습니다. 검색된 투자설명서에서 확인되는 상품과 위험등급은 다음과 같으며, \"\n                \"후보로 검토하실 수 있습니다(1등급이 가장 높은 위험, 6등급이 가장 낮은 위험):\\n\\n\" + \"\\n\".join(_lines_) +\n                (\"\\n\\n\" + (\"질문하신 대로 하나를 고르려면 \" if re.search(r\"하나|한\\s*개|한\\s*가지|1개\", _hint_q) else \"후보를 좁히려면 \") +\n                 \"연령·투자성향 외에 투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)가 더 필요합니다. 알려주시면 제공된 자료 범위에서 후보를 좁혀 드리겠습니다.\"))   # v13.62(T11): 역질문 마무리\n    return (\"문의하신 조건에 맞는 특정 상품을 제공된 자료 안에서 확정하지 못했습니다. 현재 확인된 자료만으로 특정 상품을 추천하기보다, \"\n            \"투자 기간·손실 감내 수준·계좌 종류(연금저축/IRP)를 추가로 확인한 뒤 후보를 좁히는 것이 적절합니다. 알려주시면 제공된 자료 범위에서 다시 확인해 드리겠습니다.\")\n\n\ndef recommend_contract(ans, used):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n"], ["    for s_ in srcs:                                               # 본문에 인용한 출처는 출처 줄에도 있어야 한다(answer()가 src_tail에 추가)\n        if s_ not in cited:\n            cited.append(s_)\n        allowed.add(s_)\n    ind = next((_RR_BUL.match(l).group(1) for l in rest if _RR_BUL.match(l)), \" - \")\n", "    # v13.65 [추천 후보 원칙 — 등급 미확인 펀드 블록 제거]: 위험등급이 근거 문서(정답표·해당 상품 투자설명서 머리)에서 확인되지 않은 펀드·ETF·TDF는\n    #   '공격형/안정형에 적합'처럼 읽히는 후보로 제시하지 않는다(T11 실측: 다른 상품 설명서 본문 언급만으로 후보가 됨). 예금·원리금보장형은 등급 개념이 없어 제외.\n    if not (grade and grade[0].isdigit()) and re.search(r\"펀드|투자신탁|ETF|TDF|상장지수|리츠\", nm) and not re.search(r\"예금|원리금\\s*보장|ELB|DLB\", p):\n        st[\"drop\"] = st.get(\"drop\", 0) + 1\n        st[\"dropped_names\"] = st.get(\"dropped_names\", []) + [nm]\n        return []\n    for s_ in srcs:                                               # 본문에 인용한 출처는 출처 줄에도 있어야 한다(answer()가 src_tail에 추가)\n        if s_ not in cited:\n            cited.append(s_)\n        allowed.add(s_)\n    ind = next((_RR_BUL.match(l).group(1) for l in rest if _RR_BUL.match(l)), \" - \")\n"], ["    note = f\" 추천 렌더러(블록 {st['blk']}개 구조화, 교정 {st['fix']}건)\" if (st[\"blk\"] or st[\"fix\"]) else \"\"\n    return (body.rstrip() + (\"\\n\\n\" + tail if tail else \"\")), note, cited\n", "    _drop = st.get(\"drop\", 0)\n    if _drop:\n        # 남은 본문에 등급이 확인된 상품 블록('근거 문서: … 위험등급 N등급')이 없으면 구조화 대체 목록으로 바꾼다(※ 고지 줄은 유지)\n        if not re.search(r\"근거 문서:[^\\n]*위험등급\\s*[1-6]\\s*등급\", body):\n            _notes = [l for l in body.split(\"\\n\") if l.lstrip().startswith(\"※\")]\n            body = _rc_fallback_body(used, _question_hint[0] if _question_hint else \"\")\n            _excl = \", \".join(f\"'{re.sub(chr(10), ' ', x)}'\" for x in list(dict.fromkeys(st.get(\"dropped_names\", [])))[:4])\n            body += (f\"\\n\\n※ {_excl}은(는) 사용한 근거 문서에서 위험등급을 확인하지 못해 후보에서 제외했습니다.\" if _excl else \"\")\n            if _notes:\n                body = body.rstrip() + \"\\n\\n\" + \"\\n\".join(_notes)\n        else:\n            body = re.sub(r\"\\n{3,}\", \"\\n\\n\", body)\n            _excl = \", \".join(f\"'{x}'\" for x in list(dict.fromkeys(st.get(\"dropped_names\", [])))[:4])\n            body = body.rstrip() + f\"\\n\\n※ {_excl}은(는) 사용한 근거 문서에서 위험등급을 확인하지 못해 후보에서 제외했습니다.\"\n            # 번호 목록 재번호\n            _i = [0]\n            body = re.sub(r\"(?m)^(\\s*)\\d+[.)]\\s\", lambda m: (_i.__setitem__(0, _i[0] + 1) or m.group(1)) + f\"{_i[0]}. \", body)\n    note = (f\" 추천 렌더러(블록 {st['blk']}개 구조화, 교정 {st['fix']}건\" + (f\", 등급 미확인 블록 {_drop}개 제외\" if _drop else \"\") + \")\") if (st[\"blk\"] or st[\"fix\"] or _drop) else \"\"\n    return (body.rstrip() + (\"\\n\\n\" + tail if tail else \"\")), note, cited\n"], ["        # v13.64(S4) [연금소득세 조건 계약]: 연금소득세 3.3~5.5%는 '수령 연령'에 따른 세율 — '수령금액(액수)에 따라'로 쓰면 조건 오귀속 → 교정.\n", "        # v13.65(S4) [과세 범위 계약 — 계좌 내 매매·배당 과세 의도]: 원칙 = 즉시 과세 / 인출 시 과세 / 과세 재원 / 예외를 분리.\n        #   ① '양도소득세·배당소득세를 내지 않습니다' 같은 무조건 면제 단정 → '즉시 과세되지 않고(과세이연) 인출 시 재원·방식에 따라 과세' 표준 문장으로(1회), 나머지 중복 제거\n        #   ② '손익 통산'·'일반 위탁계좌와 달리' 서술은 사용 근거 문서에 그 표현이 있을 때만 유지 ③ 범위 고지는 아래(v13.64) 조건을 의도 기반으로 적용\n        _tax_scope_q = bool(re.search(r\"매매|사고팔|거래|팔면|배당|분배금|차익\", question) and re.search(r\"세금|과세|소득세|세|내나요|내야\", question)\n                            and re.search(r\"IRP|연금저축|연금계좌|퇴직연금|DC\", question))\n        if _tax_scope_q:\n            _docs_t = re.sub(r\"\\s+\", \"\", \"\".join(c.get(\"text\", \"\") for c in used))\n            _STD = \"IRP·연금저축 등 연금계좌 안에서 발생한 매매차익·분배금에는 즉시 과세되지 않고(과세이연), 인출할 때 인출 재원과 인출 방식(연금 수령/연금 외 수령)에 따라 과세됩니다.\"\n            _b19, _n19, _std_done = [], 0, False\n            for _l in ans.split(\"\\n\"):\n                if _l.lstrip().startswith((\"※\", \"[\")):\n                    _b19.append(_l); continue\n                _lead = _PROSE_LEAD.match(_l).group(0)\n                _ss = re.split(r\"(?<=[.!?])\\s+\", _l[len(_lead):])\n                _kp = []\n                for s_ in _ss:\n                    if re.search(r\"(?:양도소득세|배당소득세|세금|과세)[^.!?\\n]{0,25}?(?:내지\\s*않|부과되지\\s*않|없습니다|면제|과세되지\\s*않)\", s_) and not re.search(r\"즉시|당장|인출\\s*(?:전|시)|이연|수령\\s*(?:시|할\\s*때)\", s_):\n                        _n19 += 1\n                        if not _std_done:\n                            _kp.append(_STD); _std_done = True\n                        continue\n                    if re.search(r\"통산\", s_) and \"통산\" not in _docs_t:\n                        _n19 += 1; continue\n                    if re.search(r\"위탁\\s*계좌|일반\\s*계좌\", s_) and not re.search(r\"위탁계좌|일반계좌\", _docs_t):\n                        _n19 += 1; continue\n                    _kp.append(s_)\n                if not _l.strip():\n                    _b19.append(_l)\n                elif _kp:\n                    _b19.append(_lead + \" \".join(_kp))\n            if _n19:\n                ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(_b19)).strip(); clean_note += f\" 과세 범위 계약 {_n19}건 교정\"\n        # v13.64(S4) [연금소득세 조건 계약]: 연금소득세 3.3~5.5%는 '수령 연령'에 따른 세율 — '수령금액(액수)에 따라'로 쓰면 조건 오귀속 → 교정.\n"], ["        if re.search(r\"과세\\s*이연|과세이연\", ans) and re.search(r\"연금소득세\", ans) and not re.search(r\"기타소득세|연금\\s*외\\s*(?:수령|인출)|일시금\", ans.split(\"[참고 문서]\")[0]) \\\n                and re.search(r\"매매|사고팔|거래|배당|분배금|ETF|펀드\", question):\n", "        if (_tax_scope_q or (re.search(r\"과세\\s*이연|과세이연\", ans) and re.search(r\"연금소득세\", ans) and re.search(r\"매매|사고팔|거래|배당|분배금|ETF|펀드\", question))) \\\n                and not re.search(r\"기타소득세|연금\\s*외\\s*(?:수령|인출)|일시금\", ans.split(\"[참고 문서]\")[0]):   # v13.65: 의도 기반\n"], ["            ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(_b13)).strip()\n            if \"계산할 수 없습니다\" not in ans[:200]:\n                ans = (\"문의하신 연금 수령액은 제공된 자료만으로는 계산할 수 없습니다. 어떤 연금(퇴직연금 DC·IRP·연금저축 등)인지와 \"\n                       \"적립금(또는 연 납입액)·예상 수익률·연금 개시 연령·수령 기간을 알려주시면 그 조건으로 다시 정리해 드리겠습니다.\\n\\n\"\n                       \"참고로 아래는 검색된 자료에 나온 수령 요건이며, 연금 종류(퇴직연금 DC·IRP·연금저축)에 따라 요건이 다를 수 있습니다.\\n\\n\" + ans)   # v13.63\n", "            ans = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(_b13)).strip()\n            if True:                                                    # v13.65: 의도가 맞으면 무조건 코드 문장(LLM 본문 형태와 무관)\n                # v13.65(C6): LLM 요건 목록(퇴직연금 DC 규정 등 특정 계좌 규정)은 계좌 종류가 특정되지 않은 질문에 일반 요건처럼 붙으므로 싣지 않는다 — 코드 문장만.\n                _n13 += len([l for l in ans.split(\"\\n\") if l.strip()])\n                ans = (\"문의하신 연금 수령액은 제공된 자료만으로는 계산할 수 없습니다. 어떤 연금(퇴직연금 DC·IRP·연금저축 등)인지와 \"\n                       \"적립금(또는 연 납입액)·예상 수익률·연금 개시 연령·수령 기간을 알려주시면 그 조건으로 다시 정리해 드리겠습니다.\\n\\n\"\n                       \"연금 수령 요건(개시 연령·가입 기간·연간 수령 한도 등)은 계좌 종류(퇴직연금 DC·IRP·연금저축)에 따라 다르므로, \"\n                       \"계좌 종류를 알려주시면 해당 계좌의 요건도 제공된 자료 범위에서 함께 안내해 드리겠습니다.\")   # v13.63/65\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def _rc_fallback_body(" in src: print("[스킵] 이미 v13.65 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[중단] 적용전 md5가 예상 v1364와 다름 → 서버 파일이 갈라짐. main_v1365_full.py 로 통째 교체하세요."); sys.exit(4)
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1364_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
