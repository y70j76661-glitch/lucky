# -*- coding: utf-8 -*-
"""patch_v1328.py — v1327(c038cb73) main.py 에 v13.28 적용. (v1327 적용 후 실행)
  [답변 계약(결정적 검증층) 도입 — 문구 치환이 아니라 주장 단위 차단]
   계약 1(연금소득세 연령-세율): 질문이 '연금으로 받을 때 세금'이면 문서(doc5) 확인값 카드를 주입하고 근거 청크를 필수 근거로
       보강(생성·검증기가 같은 근거를 봄). 답변에 연령-세율 숫자가 있으면 근거 청크가 사용됐을 때만 허용, 아니면 '확인되지
       않습니다'로 대체. 코드 문장과 모순되는 '자료에 연령별 세율 누락·연령 구분 어려움·관련 법령 참조' 문장 제거.
   계약 2(추천 상품 연결): 추천 답변의 각 상품은 '상품명 ∈ 사용 근거 문서'여야 하고 답변의 위험등급이 그 문서(또는 정답표)의
       등급과 같아야 함. 근거 없는 상품 문장은 차단 후 한 줄 고지, 등급이 다르면 문서 등급으로 교정, 확인 불가면
       '(위험등급: 자료에서 확인 필요)'로 대체. 숫자·상품이 없는 '수수료:·투자 대상:' 일반 불릿과 근거 문서에 없는 기관 안내 제거.
   X2: ISA 전환금 문장을 표시 출처 표현으로 — '세액공제를 받지 않은 납입금에 포함되는 범위에서 과세제외 재원, 과세 구분은
       세액공제 적용 여부 등 조건에 따라 달라질 수 있음'.
자동백업·자가검증. 이미 v13.28이면 스킵. 검증 md5==a1cdd22619a00f2997dfd55a9a3506f4"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='c038cb7372fa025ee794ec46a38103ad'
EXPECT_AFTER='a1cdd22619a00f2997dfd55a9a3506f4'
HUNKS=[["    ((\"세액공제\",),\n     (\"제외\", \"대상이 아\", \"대상은\", \"안 되는\", \"안되는\", \"못 받\", \"받을 수 없\", \"해당하지 않\", \"누가\", \"조건\", \"요건\", \"자격\"),\n     (\"납입액이 모두 세액공제\", \"종합소득이 있어야 세액공제\", \"세액공제를 받지 않은 납입\", \"소득이 없어도 가입이 가능\"), 2),\n    # v9.71: '금융사를 옮기는 맥락'일 때만. 일반 주식계좌→연금계좌 입고는 다른 얘기다.\n    ((\"주식\", \"종목\", \"지분증권\", \"펀드\"), (\"실물이전\", \"현물이전\", \"타사\", \"이관\", \"다른 금융\"),\n     (\"실물이전 불가사유\", \"지분증권/리츠\"), 2),\n", "    ((\"세액공제\",),\n     (\"제외\", \"대상이 아\", \"대상은\", \"안 되는\", \"안되는\", \"못 받\", \"받을 수 없\", \"해당하지 않\", \"누가\", \"조건\", \"요건\", \"자격\"),\n     (\"납입액이 모두 세액공제\", \"종합소득이 있어야 세액공제\", \"세액공제를 받지 않은 납입\", \"소득이 없어도 가입이 가능\"), 2),\n    # v13.28(N6 실측): '연금으로 받을 때 세금' 질문에 검색이 연령-세율 표(doc5/doc39)를 못 올려 LLM·검증기가 '자료에 없다'고\n    #   쓰면서 코드 문장(세율 표)과 자기모순 → 표가 있는 청크를 필수 근거로 보강해 생성·검증 모두 같은 근거를 보게 한다.\n    ((\"연금소득세\", \"연금 수령\", \"연금수령\", \"연금으로 받\", \"연금으로 수령\", \"수령할 때\", \"받을 때\"),\n     (\"세금\", \"세율\", \"과세\", \"소득세\"),\n     (\"만 70세미만 5.5%\", \"70세이상 80세미만 4.4%\", \"만55 ~69세는 5.5%\", \"만55~69세는 5.5%\"), 1),\n    # v9.71: '금융사를 옮기는 맥락'일 때만. 일반 주식계좌→연금계좌 입고는 다른 얘기다.\n    ((\"주식\", \"종목\", \"지분증권\", \"펀드\"), (\"실물이전\", \"현물이전\", \"타사\", \"이관\", \"다른 금융\"),\n     (\"실물이전 불가사유\", \"지분증권/리츠\"), 2),\n"], ["_INCOME_PAT = re.compile(\n    r\"(?:연봉|총급여|급여|연소득)[은는이가]?\\s*(?:[가-힣]{1,4}\\s*)?([\\d,]+)\\s*(천만|억|만)\\s*원\")\n_TAX_WORDS = (\"세액공제\", \"공제\", \"절세\", \"세금\", \"환급\")\n\n\ndef income_rate_card(question):\n", "_INCOME_PAT = re.compile(\n    r\"(?:연봉|총급여|급여|연소득)[은는이가]?\\s*(?:[가-힣]{1,4}\\s*)?([\\d,]+)\\s*(천만|억|만)\\s*원\")\n_TAX_WORDS = (\"세액공제\", \"공제\", \"절세\", \"세금\", \"환급\")\n\n\n# v13.28 [답변 계약 1 — 연금소득세 연령-세율]: 질문이 '연금으로 받을 때 세금'이면 문서 확인값을 카드로 주입한다(생성·검증기가\n#   같은 근거를 본다). 답변 뒤에는 age_rate_contract()가 '근거 청크가 실제로 사용됐을 때만 숫자 허용'을 강제한다.\n_AGE_Q = re.compile(r\"연금소득세|연금\\s*수령|연금수령|연금으로\\s*(?:받|수령)|수령할\\s*때|받을\\s*때\")\n_AGE_Q2 = re.compile(r\"세금|세율|과세|소득세\")\n_AGE_NUMS = re.compile(r\"\\d{2}\\s*세[^\\n%]{0,25}?(?<![\\d.])(?:5\\.5|4\\.4|3\\.3|5|4|3)\\s*%\")\n\n\ndef age_rate_card(question):\n    if _AGE_SRC and _AGE_Q.search(question) and _AGE_Q2.search(question):\n        return (f\"[연금소득세 연령별 세율 — 문서({_AGE_SRC}) 확인값. 이 값만 쓰고 다른 구간·세율을 만들지 말 것]\\n\"\n                \"- 만 70세 미만 5.5%, 70세 이상 80세 미만 4.4%, 80세 이상 3.3% (연금수령한도 내 수령 시, 세액공제 받은 납입금·운용수익 재원)\\n\"\n                \"- 수령 '연차'가 아니라 '연령' 기준이다. 자료에 없다고 쓰지 말 것(위 문서에 있음)\")\n    return None\n\n\ndef age_rate_contract(ans, used):\n    \"\"\"v13.28 [계약 1 검증]: 답변에 연령-세율 숫자가 있으면 근거 청크(70세·5.5·3.3)가 used 안에 있어야 한다.\n    없으면 숫자 문장을 '확인 불가' 문장으로 차단(삭제가 아니라 대체). → (답변, 조치)\"\"\"\n    if not _AGE_NUMS.search(ans):\n        return ans, \"\"\n    _ev = any((\"70세\" in t and \"5.5\" in t and \"3.3\" in t)\n              for t in (re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\")) for c in used))\n    if _ev or _AGE_SRC:\n        return ans, \"\"\n    _out, _n = [], 0\n    for _ln in ans.split(\"\\n\"):\n        _ss = re.split(r\"(?<=[.!?])\\s+\", _ln)\n        _kp = []\n        for x in _ss:\n            if _AGE_NUMS.search(x):\n                _n += 1\n                if not any(\"연령별 연금소득세율은 제공된 자료에서 확인되지 않습니다\" in y for y in _kp + _out):\n                    _kp.append(\"연령별 연금소득세율은 제공된 자료에서 확인되지 않습니다.\")\n            else:\n                _kp.append(x)\n        _out.append(\" \".join(_kp))\n    return \"\\n\".join(_out), (f\" (세율 계약: 근거 없는 연령-세율 {_n}문장 차단)\" if _n else \"\")\n\n\n# v13.28 [답변 계약 2 — 추천 상품 연결]: 추천 답변의 각 상품은 '상품명 ∈ 사용 근거 문서' 이고, 답변이 붙인 위험등급이 그 문서(또는\n#   정답표)의 등급과 같아야 한다. 연결이 끊긴 상품의 줄은 제거하고 한 줄로 알린다(문구 치환이 아니라 주장 단위 차단).\ndef recommend_contract(ans, used):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n    _norm = lambda t: re.sub(r\"\\s+\", \"\", t)\n    _docs = [(c.get(\"source\", \"\"), _norm(c.get(\"text\", \"\"))) for c in used]\n    removed, _fixed_box = [], [0]\n    lines = body.split(\"\\n\")\n    out = []\n    for ln0 in lines:\n        # 문장 단위로 처리: 같은 줄의 다른(실재) 상품 문장은 남긴다\n        _sents = re.split(r\"(?<=[.!?])\\s+\", ln0)\n        _kept = []\n        for ln in _sents:\n            drop = False\n            ln, drop = _rc_sentence(ln, _docs, removed, _fixed_box)\n            if not drop:\n                _kept.append(ln)\n        if ln0.strip() and not \"\".join(_kept).strip():\n            continue\n        out.append(\" \".join(_kept))\n    fixed = _fixed_box[0]\n    body = \"\\n\".join(out)\n    if removed:\n        body = body.rstrip() + (\"\\n\\n※ \" + \", \".join(f\"'{r}'\" for r in removed[:3]) +\n                                \"은(는) 사용한 근거 문서에서 확인되지 않아 추천 후보에서 제외했습니다.\")\n    note = \"\"\n    if removed or fixed:\n        note = f\" (추천 계약: 근거 미연결 상품 {len(removed)}건 제외, 등급 교정/차단 {fixed}건)\"\n    return (body.rstrip() + (\"\\n\\n\" + tail if tail else \"\")), note\n\n\ndef _rc_sentence(ln, _docs, removed, _fixed_box):\n    \"\"\"recommend_contract 의 문장 단위 검사 → (문장, 차단 여부)\"\"\"\n    _norm = lambda t: re.sub(r\"\\s+\", \"\", t)\n    drop = False\n    if True:\n        for m in _PROD_CITE.finditer(ln):\n            nm = m.group(0).strip(\"·-\")\n            core = _norm(re.sub(r\"(?:증권자?투자신탁|투자신탁|펀드|ETF).*$\", \"\", nm))\n            if len(core) < 4 or _PROD_GENERIC.fullmatch(core):\n                continue\n            fact = next((f for f in PRODUCT_FACTS if re.search(f[\"key\"], nm)), None)\n            hit_docs = [t for s0, t in _docs if core in t]\n            if not hit_docs and not fact:\n                drop = True; removed.append(nm); break         # 근거 문서에 없는 상품 → 줄 차단\n            # 등급 주장 검증: 이름 뒤 40자 안의 'N등급'\n            g = re.search(r\"(\\d)\\s*등급\", ln[m.end(): m.end() + 40])\n            if g:\n                if fact:\n                    ok_g = re.match(r\"(\\d)\", fact[\"grade\"]).group(1)\n                else:\n                    gs = set()\n                    for t in hit_docs:\n                        gs.update(re.findall(r\"투자위험등급(\\d)등급|위험등급(\\d)등급|(\\d)등급\\[\", t))\n                    gs = {x for tup in gs for x in tup if x}\n                    ok_g = next(iter(gs)) if len(gs) == 1 else None\n                if ok_g and g.group(1) != ok_g:\n                    ln = ln[: m.end()] + re.sub(r\"(\\d)(\\s*등급)\", lambda mm: ok_g + mm.group(2), ln[m.end(): m.end() + 40], count=1) + ln[m.end() + 40:]\n                    _fixed_box[0] += 1\n                elif not ok_g:\n                    ln = ln[: m.end()] + re.sub(r\"\\(?\\s*위험\\s*등급\\s*\\d\\s*등급\\s*\\)?\", \"(위험등급: 자료에서 확인 필요)\", ln[m.end(): m.end() + 40], count=1) + ln[m.end() + 40:]\n                    _fixed_box[0] += 1\n    return ln, drop\n\n\ndef income_rate_card(question):\n"], ["                _lines.append(\" \".join(_kp))\n            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"(?m)^\\s*[-•]\\s*연령대에\\s*따라\\s*연금소득세가\\s*차등\\s*적용됩니다\\.?\\s*\\n(?=연금소득세율은)\", \"\", ans)\n    # K4b v13.17(OP3): 'IRP는 연 900만원까지 공제'를 합산 기준 언급 없이 쓰면 오해 → 같은 문장에 '합산'이 없을 때만 보강\n    ans = re.sub(r\"(IRP[은는]\\s*연\\s*900\\s*만\\s*원(?:까지)?)(?![^.\\n]*합산)\", r\"\\1(연금저축과 합산 기준)\", ans)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n", "                _lines.append(\" \".join(_kp))\n            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"(?m)^\\s*[-•]\\s*연령대에\\s*따라\\s*연금소득세가\\s*차등\\s*적용됩니다\\.?\\s*\\n(?=연금소득세율은)\", \"\", ans)\n            # v13.28(N6 실측): 코드 문장(근거 문서 출처 포함)과 모순되는 '자료에는 연령별 세율이 누락/없다·연령 구분이 어렵다·\n            #   관련 법령 참조' 문장 제거 — 둘 중 하나만 남긴다(근거 문서가 있으므로 표를 남김)\n            _b, _nc = re.subn(r\"[^.!?\\n]*(?:(?:자료|문서)[^.!?\\n]{0,25}?(?:연령|나이)[^.!?\\n]{0,20}?(?:누락|없|확인되지|제시되지)\"\n                              r\"|(?:연령|나이)\\s*구분이\\s*어렵|관련\\s*법령을\\s*참조)[^.!?\\n]*[.!?]\", \"\", ans)\n            if _nc:\n                ans = re.sub(r\"[ \\t]+\\n\", \"\\n\", _b)\n    # K4b v13.17(OP3): 'IRP는 연 900만원까지 공제'를 합산 기준 언급 없이 쓰면 오해 → 같은 문장에 '합산'이 없을 때만 보강\n    ans = re.sub(r\"(IRP[은는]\\s*연\\s*900\\s*만\\s*원(?:까지)?)(?![^.\\n]*합산)\", r\"\\1(연금저축과 합산 기준)\", ans)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n"], ["        if _irc:\n            context = _irc[0] + \"\\n\\n\" + context\n            action_note += f\" → 세액공제율 판정 카드({_irc[1]})\"\n\n        # [4-a-02a] v9.99: 회사(사용자) 부담금은 세액공제 대상이 아니다.\n        #   실측 K02 — \"회사가 넣어주는 돈도 공제 대상?\"에 '대상이다'라고\n", "        if _irc:\n            context = _irc[0] + \"\\n\\n\" + context\n            action_note += f\" → 세액공제율 판정 카드({_irc[1]})\"\n        _arc = age_rate_card(question)                       # v13.28: 계약 1 카드\n        if _arc:\n            context = _arc + \"\\n\\n\" + context\n            action_note += \" → 연금소득세 연령-세율 카드\"\n\n        # [4-a-02a] v9.99: 회사(사용자) 부담금은 세액공제 대상이 아니다.\n        #   실측 K02 — \"회사가 넣어주는 돈도 공제 대상?\"에 '대상이다'라고\n"], ["        # v13.26(X2·doc39/doc33): 'ISA 전환금액은 세액공제 대상이 아니다'는 과대 서술(전환금 중 일부는 세액공제 대상) →\n        #   문서 표현으로 교체: 세액공제를 받지 않은 납입금(ISA전환금 포함)은 과세제외 재원\n        _b1, _ni = re.subn(r\"[^.!?\\n]*ISA[^.!?\\n]*전환[^.!?\\n]*세액공제\\s*대상이\\s*아[^.!?\\n]*[.!?]\",\n                            \"ISA 만기자금 전환금액은 그중 세액공제를 받지 않은 부분에 한해 과세제외 재원(세액공제를 받지 않은 \"\n                            \"납입금)에 해당하며, 전환금액 중 추가 세액공제를 적용받은 부분은 여기에 포함되지 않습니다.\", ans)\n        if _ni:\n            ans = re.sub(r\"(?m)^(\\s*\\d+\\.)(\\S)\", r\"\\1 \\2\", _b1)      # 번호 뒤 공백 복원\n            calc_note += \" (ISA 전환금 세액공제 서술 교정)\"\n", "        # v13.26(X2·doc39/doc33): 'ISA 전환금액은 세액공제 대상이 아니다'는 과대 서술(전환금 중 일부는 세액공제 대상) →\n        #   문서 표현으로 교체: 세액공제를 받지 않은 납입금(ISA전환금 포함)은 과세제외 재원\n        _b1, _ni = re.subn(r\"[^.!?\\n]*ISA[^.!?\\n]*전환[^.!?\\n]*세액공제\\s*대상이\\s*아[^.!?\\n]*[.!?]\",\n                            \"ISA 만기자금 전환금액은 세액공제를 받지 않은 납입금에 포함되는 범위에서 과세제외 재원에 해당하며, \"\n                            \"전환금액의 과세 구분은 세액공제 적용 여부 등 조건에 따라 달라질 수 있습니다.\", ans)\n        if _ni:\n            ans = re.sub(r\"(?m)^(\\s*\\d+\\.)(\\S)\", r\"\\1 \\2\", _b1)      # 번호 뒤 공백 복원\n            calc_note += \" (ISA 전환금 세액공제 서술 교정)\"\n"], ["            if _nm:\n                ans = \"\\n\".join(_ml)\n                calc_note += f\" (추천 답변 세제 수치 문장 {_nm}건 제거)\"\n\n        # [7단계] 출처 표기: 평가 기준 대응 — 사용한 문서의 출처를 답변 본문에 자동 명시\n        srcs = []\n", "            if _nm:\n                ans = \"\\n\".join(_ml)\n                calc_note += f\" (추천 답변 세제 수치 문장 {_nm}건 제거)\"\n        # v13.28(M2 실측): 추천 답변의 근거 없는 일반 안내 — 숫자·상품이 없는 '수수료:'·'투자 대상:' 선택기준 불릿, 근거 문서에 없는\n        #   기관 안내('연금자산관리센터 상담') 문장 제거. 근거 문서(used) 원문에 그 표현이 있으면 남긴다.\n        if qtype == \"추천\":\n            _ctx_norm = re.sub(r\"\\s+\", \"\", \"\".join(c.get(\"text\", \"\") for c in used))\n            _ml, _ng = [], 0\n            for _ln in ans.split(\"\\n\"):\n                if re.match(r\"\\s*[-•]\\s*(?:수수료|비용|투자\\s*대상|분산\\s*투자)\\s*[:：]\", _ln) and not re.search(r\"\\d\", _ln):\n                    _ng += 1; continue\n                _ss = re.split(r\"(?<=[.!?])\\s+\", _ln)\n                _kp = [x for x in _ss if not (re.search(r\"연금자산관리센터|고객센터|영업점\", x) and \"연금자산관리센터\" not in _ctx_norm and \"고객센터\" not in _ctx_norm)]\n                _ng += len(_ss) - len(_kp)\n                if _ln.strip() and not \"\".join(_kp).strip():\n                    continue\n                _ml.append(\" \".join(_kp))\n            if _ng:\n                # 불릿이 다 빠져 비게 된 머리말('… 다음과 같습니다:') 제거\n                _ml2 = []\n                for _i, _ln in enumerate(_ml):\n                    if re.search(r\"다음과\\s*같습니다\\s*[:：]?\\s*$\", _ln):\n                        _nxt = next((x for x in _ml[_i + 1:] if x.strip()), \"\")\n                        if not re.match(r\"\\s*(?:[-•]|\\d+[.)])\", _nxt):\n                            continue\n                    _ml2.append(_ln)\n                ans = \"\\n\".join(_ml2)\n                calc_note += f\" (추천 일반 안내 문장 {_ng}건 제거)\"\n\n        # [7단계] 출처 표기: 평가 기준 대응 — 사용한 문서의 출처를 답변 본문에 자동 명시\n        srcs = []\n"], ["        ans, _pb = soften_fund_guarantee(ans)\n        if _pb:\n            clean_note += f\" 펀드 원금보장 표현 완화 {_pb}건\"\n        # v13.19: 정답표 상품의 등급·보수가 답변에서 다르게 쓰였으면 교정(질문에 그 상품이 있을 때)\n        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n", "        ans, _pb = soften_fund_guarantee(ans)\n        if _pb:\n            clean_note += f\" 펀드 원금보장 표현 완화 {_pb}건\"\n        # v13.28: 답변 계약 검증(주장 단위 차단) — 세율 계약(모든 유형), 추천 계약(추천 유형)\n        ans, _c1 = age_rate_contract(ans, used)\n        clean_note += _c1\n        if qtype == \"추천\":\n            ans, _c2 = recommend_contract(ans, used)\n            clean_note += _c2\n        # v13.19: 정답표 상품의 등급·보수가 답변에서 다르게 쓰였으면 교정(질문에 그 상품이 있을 때)\n        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def recommend_contract(" in src: print("[스킵] 이미 v13.28 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1327과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1327_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
