# -*- coding: utf-8 -*-
"""patch_v1329.py — v1328(a1cdd226) main.py 에 v13.29 적용. (v1328 적용 후 실행)
  [mini10 정독 + 주장 단위 검증기(claim_check) 도입에 맞춘 근거 정합]
   M2: '고객에게 알맞습니다'·'안정적인 수익을 보장하면서도'·'원금 손실을 최소화합니다'·'거의 무위험에 가까운' → 실적배당형(원금
       비보장) 기준 완화(수익 보장 단정 차단).
   N6: '지방소득세 포함' 표기는 사용 근거 문서에 그 표기가 있을 때만 — 없으면 그 문장만 제거(doc5 표는 세율만 명시).
   계산: 계산기·한도 카드가 쓰는 한도(600/900만원)의 근거 청크(세액공제+900+600이 함께 있는 안내 문서)를 사용 근거에 포함해
       요약 줄의 한도 수치가 표시 출처 안에서 확인되게 함(출처 정합).
자동백업·자가검증. 이미 v13.29면 스킵. 검증 md5==7532c7d4671b6ec2cf262723e0b83708"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='a1cdd22619a00f2997dfd55a9a3506f4'
EXPECT_AFTER='7532c7d4671b6ec2cf262723e0b83708'
HUNKS=[["\n\n\ndef find_fee_rows(sources, exclude, limit=2):\n    \"\"\"v9.20: 보수표는 숫자 나열이라 임베딩 유사도가 낮아 상위에 못 든다.\n    (실측: 질문과 같은 문서인데도 17위) → 검색된 문서 안에서 보수표 청크를 직접 찾아 넣는다.\"\"\"\n", "\n\n\ndef find_limit_evidence(exclude):\n    \"\"\"v13.29(계약 3 — 계산 근거): 계산기·한도 카드가 쓰는 한도(연금저축 600만원·합산 900만원)가 사용 근거 문서 안에 실제로 있어야\n    출처가 성립한다. 없으면 '세액공제'+'900만원'+'600만원'이 함께 있는 청크(안내 문서 docNN 우선)를 하나 찾아 돌려준다.\"\"\"\n    seen = {c[\"text\"] for c in exclude}\n    for c in exclude:\n        t = re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\"))\n        if \"세액공제\" in t and (\"900만원\" in t or \"900만\" in t) and (\"600만원\" in t or \"600만\" in t):\n            return None                                   # 이미 근거 안에 있음\n    best = None\n    for c in chunks:\n        if c[\"text\"] in seen:\n            continue\n        t = re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\"))\n        if \"세액공제\" in t and (\"900만원\" in t or \"900만\" in t) and (\"600만원\" in t or \"600만\" in t):\n            if str(c.get(\"source\", \"\")).startswith(\"doc\"):\n                return c\n            best = best or c\n    return best\n\n\ndef find_fee_rows(sources, exclude, limit=2):\n    \"\"\"v9.20: 보수표는 숫자 나열이라 임베딩 유사도가 낮아 상위에 못 든다.\n    (실측: 질문과 같은 문서인데도 17위) → 검색된 문서 안에서 보수표 청크를 직접 찾아 넣는다.\"\"\"\n"], ["    없으면 숫자 문장을 '확인 불가' 문장으로 차단(삭제가 아니라 대체). → (답변, 조치)\"\"\"\n    if not _AGE_NUMS.search(ans):\n        return ans, \"\"\n    _ev = any((\"70세\" in t and \"5.5\" in t and \"3.3\" in t)\n              for t in (re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\")) for c in used))\n    if _ev or _AGE_SRC:\n        return ans, \"\"\n    _out, _n = [], 0\n    for _ln in ans.split(\"\\n\"):\n", "    없으면 숫자 문장을 '확인 불가' 문장으로 차단(삭제가 아니라 대체). → (답변, 조치)\"\"\"\n    if not _AGE_NUMS.search(ans):\n        return ans, \"\"\n    _texts = [re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\")) for c in used]\n    _ev = any((\"70세\" in t and \"5.5\" in t and \"3.3\" in t) for t in _texts)\n    if _ev or _AGE_SRC:\n        # v13.29: '지방소득세 포함'은 사용 근거 문서에 그 표기가 있을 때만(doc1 등) — 없으면 그 문장만 대체\n        if re.search(r\"지방소득세\", ans) and not any(\"지방소득세\" in t for t in _texts):\n            _o, _k = [], 0\n            for _ln in ans.split(\"\\n\"):\n                _ss = re.split(r\"(?<=[.!?])\\s+\", _ln)\n                _kp = [x for x in _ss if not re.search(r\"지방소득세\", x)]\n                _k += len(_ss) - len(_kp)\n                if _ln.strip() and not \"\".join(_kp).strip():\n                    continue\n                _o.append(\" \".join(_kp))\n            if _k:\n                return \"\\n\".join(_o), f\" (세율 계약: 근거 없는 지방소득세 표기 {_k}문장 제거)\"\n        return ans, \"\"\n    _out, _n = [], 0\n    for _ln in ans.split(\"\\n\"):\n"], ["                st_new = re.sub(r\"원금\\s*보호와\\s*\", \"원금 손실 위험을 낮추는 것과 \", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적절합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적합합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n", "                st_new = re.sub(r\"원금\\s*보호와\\s*\", \"원금 손실 위험을 낮추는 것과 \", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적절합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적합합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                # v13.29(M2 실측): '고객에게 알맞습니다'·'수익을 보장하면서'·'원금 손실을 최소화합니다'·'거의 무위험에 가까운'\n                st_new = re.sub(r\"(?:고객|분들?)에게\\s*알맞습니다\", \"고객이 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"수익을\\s*보장하면서도?\\s*\", \"수익을 추구하지만 원금이 보장되지는 않으며 \", st_new)\n                st_new = re.sub(r\"수익을\\s*보장(?:합니다|하며|하고)\", \"수익을 추구하지만 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"원금\\s*손실을\\s*최소화합니다\", \"원금 손실 가능성을 상대적으로 낮추는 것을 목표로 합니다\", st_new)\n                st_new = re.sub(r\"거의\\s*무위험에\\s*가까운\\s*투자를\\s*제공합니다\", \"위험등급이 가장 낮은 편이지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"(?:거의\\s*)?무위험(?:에\\s*가까운|의|으로)?\", \"위험등급이 낮은\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n"], ["                and any(k in question for k in LIMIT_Q2):\n            context = LIMIT_CARD + \"\\n\\n\" + context\n            action_note += \" → 한도 구조 카드 주입\"\n        if qtype == \"세제\" and not scope_card:   # v9.35: 금액이 섞였으면 계산기 미실행\n            info = extract_json(TAX_EXTRACT_SYSTEM, q_llm)\n            salary = to_num(info.get(\"총급여\"))\n", "                and any(k in question for k in LIMIT_Q2):\n            context = LIMIT_CARD + \"\\n\\n\" + context\n            action_note += \" → 한도 구조 카드 주입\"\n            _lev = find_limit_evidence(used)               # v13.29: 한도 카드의 근거 문서 포함\n            if _lev:\n                used = used + [_lev]\n                context = context + f\"\\n\\n[문서{len(used)}] (출처: {_lev['source']})\\n{_lev['text']}\"\n                action_note += f\"(근거 {_lev['source']})\"\n        if qtype == \"세제\" and not scope_card:   # v9.35: 금액이 섞였으면 계산기 미실행\n            info = extract_json(TAX_EXTRACT_SYSTEM, q_llm)\n            salary = to_num(info.get(\"총급여\"))\n"], ["                context = calc_result + \"\\n\\n\" + context\n                if LIMIT_CARD not in context:            # v13.19(E7): 옛 약관(고소득자 300만원 한도) 유입 차단\n                    context = LIMIT_CARD + \"\\n\\n\" + context\n                action_note += (f\" → 계산기 실행 ({_ikind} {int(salary):,}만원, \"\n                               f\"납입액 {int(paid):,}만원, {_lab} 한도 {_lim}만원\"\n                               f\"{', 연금저축 600 하위한도 적용' if calc_vals.get('subcap') else ''})\")\n", "                context = calc_result + \"\\n\\n\" + context\n                if LIMIT_CARD not in context:            # v13.19(E7): 옛 약관(고소득자 300만원 한도) 유입 차단\n                    context = LIMIT_CARD + \"\\n\\n\" + context\n                _lev = find_limit_evidence(used)           # v13.29: 한도 수치의 근거 문서를 사용 근거에 포함(출처 정합)\n                if _lev:\n                    used = used + [_lev]\n                    context = context + f\"\\n\\n[문서{len(used)}] (출처: {_lev['source']})\\n{_lev['text']}\"\n                    action_note += f\" + 한도 근거 보강({_lev['source']})\"\n                action_note += (f\" → 계산기 실행 ({_ikind} {int(salary):,}만원, \"\n                               f\"납입액 {int(paid):,}만원, {_lab} 한도 {_lim}만원\"\n                               f\"{', 연금저축 600 하위한도 적용' if calc_vals.get('subcap') else ''})\")\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def find_limit_evidence(" in src: print("[스킵] 이미 v13.29 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1328과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1328_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
