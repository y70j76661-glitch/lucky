# -*- coding: utf-8 -*-
"""patch_v1327.py — v1326(4afadf8e) main.py 에 v13.27 적용. (v1326 적용 후 실행)
  [mini8 정독 — N6 세율 체계 충돌·M2 잔여 표현/오탐·X2 ISA 표현]
   N6: '55세에서 64세 5.5%'(범위 구분자 '에서' 미인식)·'80세 이상 1.1%'(표에 없는 세율)가 코드 문장 옆에 남던 것 →
       범위 구분자 확장, 연령에 붙은 표 밖 세율(10% 미만)은 오류로 판정, 코드 문장이 들어가면 다른 연령-세율 줄은 모두
       정리해 체계를 하나만 남김. 코드 문장에서 '지방소득세 포함'·'문서 기준' 문구 제거(표시 출처가 명시하지 않음).
       근거 문서 탐색은 코드 문장과 같은 표기('70세미만 … 80세이상')를 가진 문서를 우선.
   M2: 실재 상품(삼성클래식)이 '확인되지 않는 상품명'으로 표시된 오탐 → 정답표(PRODUCT_FACTS) 상품은 대조 없이 실재로 봄.
       '이 상품들은 각각 5등급과 6등급'(한 상품만 언급된 항목) → 같은 문단의 정답표 상품 등급으로 교정(둘 이상 언급 시 불변).
       '안정적인 수익을 제공합니다'·'원금 보호와 … 투자자에게 적절/적합합니다' 완화(N등급도 펀드 문맥으로 인식).
   X2: ISA 전환금 문장을 보수적으로 — '세액공제를 받지 않은 부분에 한해 과세제외 재원, 추가 세액공제 적용분은 제외'.
자동백업·자가검증. 이미 v13.27이면 스킵. 검증 md5==c038cb7372fa025ee794ec46a38103ad"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='4afadf8e3152564bad3d5b2d9b492016'
EXPECT_AFTER='c038cb7372fa025ee794ec46a38103ad'
HUNKS=[["print(f\"로딩 완료: 청크 {len(idx_list)}개\")\n# v13.25(cite_check N6): 코드가 고정하는 연금소득세 연령-세율 문장의 근거 문서 — 출처 줄에 반드시 포함시키기 위해 기동 시 1회 탐색\n_AGE_SRC = \"\"\nfor _c in (chunks if isinstance(chunks, list) else chunks.values()):\n    _t = re.sub(r\"\\s+\", \"\", _c.get(\"text\", \"\") if isinstance(_c, dict) else str(_c))\n    if \"70세\" in _t and \"5.5\" in _t and \"4.4\" in _t and \"3.3\" in _t and \"연금소득세\" in _t:\n        _AGE_SRC = _c.get(\"source\", \"\") if isinstance(_c, dict) else \"\"\n        break\nprint(f\"연령세율 근거 문서: {_AGE_SRC or '(못 찾음)'}\")\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n", "print(f\"로딩 완료: 청크 {len(idx_list)}개\")\n# v13.25(cite_check N6): 코드가 고정하는 연금소득세 연령-세율 문장의 근거 문서 — 출처 줄에 반드시 포함시키기 위해 기동 시 1회 탐색\n_AGE_SRC = \"\"\nfor _pass in (0, 1):   # v13.27: 코드 문장과 같은 표기('70세미만 5.5% … 80세이상 3.3%')를 가진 문서를 우선\n    for _c in (chunks if isinstance(chunks, list) else chunks.values()):\n        _t = re.sub(r\"\\s+\", \"\", _c.get(\"text\", \"\") if isinstance(_c, dict) else str(_c))\n        _ok = (\"70세미만\" in _t and \"80세이상\" in _t and \"5.5%\" in _t and \"3.3%\" in _t and \"연금소득세\" in _t) if _pass == 0 else \\\n              (\"70세\" in _t and \"5.5\" in _t and \"4.4\" in _t and \"3.3\" in _t and \"연금소득세\" in _t)\n        if _ok:\n            _AGE_SRC = _c.get(\"source\", \"\") if isinstance(_c, dict) else \"\"\n            break\n    if _AGE_SRC:\n        break\nprint(f\"연령세율 근거 문서: {_AGE_SRC or '(못 찾음)'}\")\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n"], ["        if len(cand_words) == 1 and (re.fullmatch(_PROD_SUFFIX + _PROD_PARTICLE, cand_words[0])\n                                     or _PROD_GENERIC.fullmatch(re.sub(_PROD_PARTICLE + r\"$\", \"\", cand_words[0]))):\n            continue                       # 'ETF'·'펀드'·'IRP'·'연금저축' 같은 일반명사 단독은 상품명이 아님\n        grounded = False\n        for k in range(len(cand_words)):\n            tail = re.sub(r\"\\s+\", \"\", \"\".join(w + \" \" for w in cand_words[k:]))\n", "        if len(cand_words) == 1 and (re.fullmatch(_PROD_SUFFIX + _PROD_PARTICLE, cand_words[0])\n                                     or _PROD_GENERIC.fullmatch(re.sub(_PROD_PARTICLE + r\"$\", \"\", cand_words[0]))):\n            continue                       # 'ETF'·'펀드'·'IRP'·'연금저축' 같은 일반명사 단독은 상품명이 아님\n        if any(re.search(f[\"key\"], \" \".join(cand_words)) for f in PRODUCT_FACTS):\n            continue                       # v13.27: 정답표(코퍼스 확인) 상품은 대조 없이 실재로 본다(실측 M2 오탐 방지)\n        grounded = False\n        for k in range(len(cand_words)):\n            tail = re.sub(r\"\\s+\", \"\", \"\".join(w + \" \" for w in cand_words[k:]))\n"], ["\n# v13.13(일반 게이트 1b — 유형-속성 모순): 펀드·ETF·투자신탁(실적배당형)을 언급한 문장에서 '원금 보장'을\n#   긍정 단정하면 문서 기준(투자원금 비보장)으로 완화한다. 예금·원리금보장상품 문맥이나 부정문은 불변.\n_PB_FUND = re.compile(r\"펀드|ETF|투자신탁|TDF|리츠|위험\\s*등급\")\n_PB_SAFE = re.compile(r\"예금|원리금\\s*보장|원리금보장|보장되지|보장하지|보장이\\s*아니|않\")\n_PB_CLAIM = re.compile(r\"(?:거의\\s*)?원금(?:이|을|은)?\\s*보장(?:되|하|이\\s*되)\")\n\n", "\n# v13.13(일반 게이트 1b — 유형-속성 모순): 펀드·ETF·투자신탁(실적배당형)을 언급한 문장에서 '원금 보장'을\n#   긍정 단정하면 문서 기준(투자원금 비보장)으로 완화한다. 예금·원리금보장상품 문맥이나 부정문은 불변.\n_PB_FUND = re.compile(r\"펀드|ETF|투자신탁|TDF|리츠|위험\\s*등급|\\d\\s*등급\")\n_PB_SAFE = re.compile(r\"예금|원리금\\s*보장|원리금보장|보장되지|보장하지|보장이\\s*아니|않\")\n_PB_CLAIM = re.compile(r\"(?:거의\\s*)?원금(?:이|을|은)?\\s*보장(?:되|하|이\\s*되)\")\n\n"], ["                st_new = re.sub(r\"투자자에게\\s*알맞습니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"(저위험|매우\\s*저위험|안전한)\\s*상품이\\s*좋습니다\", r\"\\1 상품을 검토할 수 있습니다\", st_new)\n                st_new = re.sub(r\"안정적으로\\s*마련하려는\\s*목적에\\s*부합합니다\", \"마련하려는 목적으로 검토할 수 있으나, 실적배당형이라 원금 손실 가능성은 있습니다\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n", "                st_new = re.sub(r\"투자자에게\\s*알맞습니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"(저위험|매우\\s*저위험|안전한)\\s*상품이\\s*좋습니다\", r\"\\1 상품을 검토할 수 있습니다\", st_new)\n                st_new = re.sub(r\"안정적으로\\s*마련하려는\\s*목적에\\s*부합합니다\", \"마련하려는 목적으로 검토할 수 있으나, 실적배당형이라 원금 손실 가능성은 있습니다\", st_new)\n                # v13.27(M2 실측): '안정적인 수익을 제공합니다'·'원금 보호와 … 투자자에게 적절합니다'\n                st_new = re.sub(r\"안정적인\\s*수익을\\s*제공합니다\", \"주식형보다 변동성이 낮을 수 있지만, 투자원금이 보장되는 상품은 아닙니다\", st_new)\n                st_new = re.sub(r\"원금\\s*보호와\\s*\", \"원금 손실 위험을 낮추는 것과 \", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적절합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"투자자에게\\s*적합합니다\", \"투자자가 검토할 수 있습니다(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n"], ["    #       이 표와 어긋나면(실측 N6: 55~64세 5.0 / 65~69세 4.0 / 70세 이상 3.3) 그 문장들만 문서 표 문장으로\n    #       교체. 세율이 이 표의 값이 아니면(16.5% 등) 대상 아님. 짝이 맞으면 불변.\n    if \"연금소득세\" in ans:\n        _AGE_RATE = re.compile(r\"(?:(\\d{2})\\s*[~\\-–∼]\\s*)?(\\d{2})\\s*세\\s*(미만|이하|이상|초과|부터)?[^\\n%]{0,20}?(\\d(?:\\.\\d)?)\\s*%\")\n        _TABLE = {5.5: 5.5, 5.0: 5.5, 4.4: 4.4, 4.0: 4.4, 3.3: 3.3, 3.0: 3.3}\n        def _expected(age):\n            return 5.5 if age < 70 else (4.4 if age < 80 else 3.3)\n        def _pair_wrong(m):\n            r = float(m.group(4))\n            if r not in _TABLE: return False\n            a, b, rel = m.group(1), int(m.group(2)), m.group(3)\n            if b < 55 or (a and int(a) < 55):\n                return True                         # v13.19(N6): 55세 미만 구간은 연금 수령 자체가 없음 → 오류\n", "    #       이 표와 어긋나면(실측 N6: 55~64세 5.0 / 65~69세 4.0 / 70세 이상 3.3) 그 문장들만 문서 표 문장으로\n    #       교체. 세율이 이 표의 값이 아니면(16.5% 등) 대상 아님. 짝이 맞으면 불변.\n    if \"연금소득세\" in ans:\n        _AGE_RATE = re.compile(r\"(?:(\\d{2})\\s*세?\\s*(?:[~\\-–∼]|에서|부터)\\s*)?(\\d{2})\\s*세\\s*(미만|이하|이상|초과|부터)?[^\\n%]{0,20}?(\\d(?:\\.\\d)?)\\s*%\")   # v13.27: '55세에서 64세'도 범위\n        _TABLE = {5.5: 5.5, 5.0: 5.5, 4.4: 4.4, 4.0: 4.4, 3.3: 3.3, 3.0: 3.3}\n        def _expected(age):\n            return 5.5 if age < 70 else (4.4 if age < 80 else 3.3)\n        def _pair_wrong(m):\n            r = float(m.group(4))\n            if r not in _TABLE:\n                return r < 10                       # v13.27(N6 실측 '80세 이상 1.1%'): 연령에 붙은 10% 미만 세율이 표에 없으면 오류\n            a, b, rel = m.group(1), int(m.group(2)), m.group(3)\n            if b < 55 or (a and int(a) < 55):\n                return True                         # v13.19(N6): 55세 미만 구간은 연금 수령 자체가 없음 → 오류\n"], ["        _year_wrong = [m for m in _YEAR_RATE.finditer(ans)\n                       if not re.search(r\"퇴직소득세|감면\", ans[max(0, ans.rfind(\"\\n\", 0, m.start())): m.end()])]\n        if _year_wrong:\n            _fixed = (\"연금소득세율(지방소득세 포함)은 수령 연차가 아니라 연령 기준으로, 문서 기준 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n            _lines, _done = [], False\n            for _ln in ans.split(\"\\n\"):\n", "        _year_wrong = [m for m in _YEAR_RATE.finditer(ans)\n                       if not re.search(r\"퇴직소득세|감면\", ans[max(0, ans.rfind(\"\\n\", 0, m.start())): m.end()])]\n        if _year_wrong:\n            _fixed = (\"연금소득세율은 수령 연차가 아니라 연령 기준으로, 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n            _lines, _done = [], False\n            for _ln in ans.split(\"\\n\"):\n"], ["            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"연령에\\s*따라\\s*세율이\\s*다릅니다\", \"연령에 따라 세율이 다릅니다\", ans)\n        if _wrong:\n            _fixed = (\"연금소득세율(지방소득세 포함)은 문서 기준으로 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n            _lines, _done = [], False\n            for _ln in ans.split(\"\\n\"):\n", "            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"연령에\\s*따라\\s*세율이\\s*다릅니다\", \"연령에 따라 세율이 다릅니다\", ans)\n        if _wrong:\n            _fixed = (\"연금소득세율은 만 70세 미만 5.5%, \"\n                      \"70세 이상 80세 미만 4.4%, 80세 이상 3.3%입니다.\")\n            _lines, _done = [], False\n            for _ln in ans.split(\"\\n\"):\n"], ["                    continue\n                _lines.append(_ln)\n            ans = \"\\n\".join(_lines)\n    # K4b v13.17(OP3): 'IRP는 연 900만원까지 공제'를 합산 기준 언급 없이 쓰면 오해 → 같은 문장에 '합산'이 없을 때만 보강\n    ans = re.sub(r\"(IRP[은는]\\s*연\\s*900\\s*만\\s*원(?:까지)?)(?![^.\\n]*합산)\", r\"\\1(연금저축과 합산 기준)\", ans)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n", "                    continue\n                _lines.append(_ln)\n            ans = \"\\n\".join(_lines)\n        # v13.27(N6 실측): 코드 문장이 들어갔으면 남은 연령-세율 줄('55세에서 64세: 5.5%' 등 부분·중복 체계)은 전부 정리해\n        #   연령-세율 체계를 하나만 남긴다(다른 세율 16.5% 등은 무관). 코드 문장이 없으면 불변.\n        _canon = re.compile(r\"연금소득세율은 (?:수령 연차가 아니라 연령 기준으로, )?만 70세 미만 5\\.5%\")\n        if _canon.search(ans):\n            _lines = []\n            for _ln in ans.split(\"\\n\"):\n                if _canon.search(_ln):\n                    _lines.append(_ln); continue\n                _ss = re.split(r\"(?<=[.!?])\\s+\", _ln)\n                _kp = [x for x in _ss if not (re.search(r\"\\d{2}\\s*세\", x) and re.search(r\"(?<![\\d.])(?:[0-9](?:\\.\\d)?)\\s*%\", x)\n                                              and not re.search(r\"16\\.5|13\\.2\", x))]\n                if _ln.strip() and not \"\".join(_kp).strip():\n                    continue\n                _lines.append(\" \".join(_kp))\n            ans = \"\\n\".join(_lines)\n            ans = re.sub(r\"(?m)^\\s*[-•]\\s*연령대에\\s*따라\\s*연금소득세가\\s*차등\\s*적용됩니다\\.?\\s*\\n(?=연금소득세율은)\", \"\", ans)\n    # K4b v13.17(OP3): 'IRP는 연 900만원까지 공제'를 합산 기준 언급 없이 쓰면 오해 → 같은 문장에 '합산'이 없을 때만 보강\n    ans = re.sub(r\"(IRP[은는]\\s*연\\s*900\\s*만\\s*원(?:까지)?)(?![^.\\n]*합산)\", r\"\\1(연금저축과 합산 기준)\", ans)\n    # K5 v13.13(일반 게이트 2 — 중복): 떨어진 위치라도 '완전히 같은 문장'(공백·구두점 무시, 20자 이상)이\n"], ["        # v13.26(X2·doc39/doc33): 'ISA 전환금액은 세액공제 대상이 아니다'는 과대 서술(전환금 중 일부는 세액공제 대상) →\n        #   문서 표현으로 교체: 세액공제를 받지 않은 납입금(ISA전환금 포함)은 과세제외 재원\n        _b1, _ni = re.subn(r\"[^.!?\\n]*ISA[^.!?\\n]*전환[^.!?\\n]*세액공제\\s*대상이\\s*아[^.!?\\n]*[.!?]\",\n                            \"ISA 만기자금 전환금액도 세액공제를 받지 않은 납입금에 포함되는 범위에서 과세제외 재원에 해당합니다\"\n                            \"(전환금액 중 세액공제를 받은 부분은 제외).\", ans)\n        if _ni:\n            ans = re.sub(r\"(?m)^(\\s*\\d+\\.)(\\S)\", r\"\\1 \\2\", _b1)      # 번호 뒤 공백 복원\n            calc_note += \" (ISA 전환금 세액공제 서술 교정)\"\n        # v13.26(M2 실측): 상품 하나를 가리키는 문장의 'N등급 및 M등급' — 앞 400자 안의 정답표 상품이 하나면 그 등급으로 교정\n        def _grade_pair_fix(m):\n            _pre = ans[max(0, m.start() - 400):m.start()]\n            _best, _bend = None, -1\n            for f in PRODUCT_FACTS:                          # 가장 가까이(마지막에) 언급된 정답표 상품 하나를 고른다\n                for _mm in re.finditer(f[\"key\"], _pre):\n                    if _mm.end() > _bend:\n                        _best, _bend = f, _mm.end()\n            if _best:\n                _g = re.match(r\"(\\d)\\s*등급\", _best[\"grade\"])\n                if _g and _g.group(1) in (m.group(1), m.group(2)):\n                    return m.group(0)[:m.start(1) - m.start()] + _best[\"grade\"]\n            return m.group(0)\n        _b2 = re.sub(r\"(?:이|해당|위)\\s*상품은[^.\\n]{0,25}?(\\d)\\s*등급\\s*(?:및|과|또는|,)\\s*(\\d)\\s*등급\", _grade_pair_fix, ans)\n        if _b2 != ans:\n            ans = _b2\n            calc_note += \" (단일 상품 등급 쌍 교정)\"\n", "        # v13.26(X2·doc39/doc33): 'ISA 전환금액은 세액공제 대상이 아니다'는 과대 서술(전환금 중 일부는 세액공제 대상) →\n        #   문서 표현으로 교체: 세액공제를 받지 않은 납입금(ISA전환금 포함)은 과세제외 재원\n        _b1, _ni = re.subn(r\"[^.!?\\n]*ISA[^.!?\\n]*전환[^.!?\\n]*세액공제\\s*대상이\\s*아[^.!?\\n]*[.!?]\",\n                            \"ISA 만기자금 전환금액은 그중 세액공제를 받지 않은 부분에 한해 과세제외 재원(세액공제를 받지 않은 \"\n                            \"납입금)에 해당하며, 전환금액 중 추가 세액공제를 적용받은 부분은 여기에 포함되지 않습니다.\", ans)\n        if _ni:\n            ans = re.sub(r\"(?m)^(\\s*\\d+\\.)(\\S)\", r\"\\1 \\2\", _b1)      # 번호 뒤 공백 복원\n            calc_note += \" (ISA 전환금 세액공제 서술 교정)\"\n        # v13.26(M2 실측): 상품 하나를 가리키는 문장의 'N등급 및 M등급' — 앞 400자 안의 정답표 상품이 하나면 그 등급으로 교정\n        def _grade_pair_fix(m):\n            _pre = ans[max(0, m.start() - 400):m.start()].split(\"\\n\\n\")[-1]   # 같은 문단(항목) 안에서만 상품을 찾는다\n            _best, _bend, _nfact = None, -1, 0\n            for f in PRODUCT_FACTS:                          # 가장 가까이(마지막에) 언급된 정답표 상품 하나를 고른다\n                _ms = list(re.finditer(f[\"key\"], _pre))\n                if _ms:\n                    _nfact += 1\n                    if _ms[-1].end() > _bend:\n                        _best, _bend = f, _ms[-1].end()\n            if _best and _nfact == 1:                        # 상품이 둘 이상 언급됐으면 '각각 N·M등급'은 정상일 수 있어 불변\n                _g = re.match(r\"(\\d)\\s*등급\", _best[\"grade\"])\n                if _g and _g.group(1) in (m.group(1), m.group(2)):\n                    _head = m.group(0)[:m.start(1) - m.start()]\n                    _head = re.sub(r\"상품들은\\s*(?:각각\\s*)?\", \"상품은 \", _head)\n                    return _head + _best[\"grade\"]\n            return m.group(0)\n        _b2 = re.sub(r\"(?:이|해당|위)\\s*상품(?:들)?은(?:\\s*각각)?[^.\\n]{0,25}?(\\d)\\s*등급\\s*(?:및|과|또는|,)\\s*(\\d)\\s*등급\", _grade_pair_fix, ans)\n        if _b2 != ans:\n            ans = _b2\n            calc_note += \" (단일 상품 등급 쌍 교정)\"\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.27: 정답표(코퍼스 확인) 상품은" in src: print("[스킵] 이미 v13.27 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1326과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1326_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
