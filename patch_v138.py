# -*- coding: utf-8 -*-
"""patch_v138.py — v137(84152d9d) main.py 에 v13.8 적용.
  [계산 경로 단계형 구조화: 소득유형 판별 → 계좌별 한도 → 대상액 확정 → 세율 → 결과 출력]
   ① [1단계 소득 슬롯] extract_income: LLM 추출기가 비면 질문에서 '종합소득금액/총급여/연봉 + 금액'을
      결정적으로 뽑고 소득 유형까지 확정(4천만·5천5백만·1억 2천만 표기 파서). 실측 F3 원인
      (종합소득금액을 못 잡아 계산기 미실행) 제거. 월급·1,000만 미만은 제외(오인 방지).
   ② [5단계 결과 출력] calc_summary_line: 계산기가 돌면 코드가 만든 요약 줄을 답변 맨 앞에 항상 고정
      (소득유형·구간·세율·납입·계좌별 대상액·초과분·공제액). LLM 본문 표기와 무관하게 첫 줄이 결정적 결과.
      기존 '정답 없을 때만 못박기' 안전망은 요약 줄로 대체.
   ③ 반대 소득 구간 공제액(예: 99.0)·'반대 세율 적용' 단정 문장을 계산기 값으로 교정(조건문은 보존).
   ④ 'N만 M천원' 결과 표기를 산술 검증이 118로 오독해 글리치를 만들 수 있던 결함 제거(_calc_c).
   그 외 경로 불변.
자동백업·자가검증. 이미 v13.8이면 스킵. 검증 md5==6d5ae2a25a7bda7f916630eeafb625b1"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='84152d9d5e2a5346584a3b188145270d'
EXPECT_AFTER='6d5ae2a25a7bda7f916630eeafb625b1'
HUNKS=[["    return \"총급여\"\n\n\ndef calc_pension_tax_credit(salary, paid, limit=900, limit_label=\"연금저축+IRP 합산\",\n                            subcap=None, income_kind=\"총급여\"):\n    \"\"\"연금계좌 세액공제 계산기 — 문서 기준 반영\n", "    return \"총급여\"\n\n\n_INC_KEY = re.compile(r\"(종합\\s*소득(?:\\s*금액)?|총\\s*급여(?:액)?|세전\\s*연봉|연봉|연\\s*소득|(?<!퇴직)급여)\"\n                      r\"[은는이가의도]?\\s*(?:[가-힣]{1,4}\\s*)?\"\n                      r\"((?:[\\d,]+(?:\\.\\d+)?\\s*(?:억|천|백|만)?\\s*){1,4})\\s*원?\")\n_KOR_TOK = re.compile(r\"([\\d,]+(?:\\.\\d+)?)\\s*(억|천|백|만)?\")\n\n\ndef parse_man(text):\n    \"\"\"'4,000만' '4천만' '5천5백만' '1억 2천만' '6천' '1억' → 만원 단위 숫자 (v13.8)\"\"\"\n    total, seen = 0.0, False\n    for num, unit in _KOR_TOK.findall(text):\n        try:\n            v = float(num.replace(\",\", \"\"))\n        except ValueError:\n            continue\n        seen = True\n        if unit == \"억\":\n            total += v * 10000\n        elif unit == \"천\":\n            total += v * 1000\n        elif unit == \"백\":\n            total += v * 100\n        else:                      # '만' 또는 단위 없음(만원 단위 그대로)\n            total += v\n    return total if seen else None\n\n\ndef extract_income(question):\n    \"\"\"v13.8(F3): [1단계 소득 슬롯] 질문에서 소득 금액과 소득 유형을 결정적으로 뽑는다.\n    LLM 추출기가 '종합소득금액'을 총급여로 못 잡아 계산기가 통째로 건너뛰어진 실측(F3) 대응.\n    반환: (만원, '총급여'|'종합소득') 또는 (None, None).\"\"\"\n    m = _INC_KEY.search(question)\n    if not m:\n        return None, None\n    v = parse_man(m.group(2))\n    if v is None or not (1000 <= v <= 100000):      # 연 소득 범위 밖(월급 500만 등)은 LLM 추출에 맡김\n        return None, None\n    if re.search(r\"(?:월|매달|매월)\\s*\" + re.escape(m.group(1)[:2]), question):   # '월 급여 500만원'\n        return None, None\n    kind = \"종합소득\" if re.search(r\"종합\\s*소득\", m.group(1)) else \"총급여\"\n    return v, kind\n\n\ndef calc_summary_line(c):\n    \"\"\"v13.8: [5단계 결과 출력] 계산기 값으로 만든 한 줄 요약 — 답변 맨 앞에 코드가 고정한다.\n    LLM이 본문에서 어떤 표기를 쓰든 첫 줄은 결정적 계산 결과다(소득유형→세율→계좌별 한도→대상액→공제액).\"\"\"\n    inc = c.get(\"inc_label\", \"총급여\")\n    sal = c.get(\"salary\")\n    band = c.get(\"band\", \"\").replace(inc, \"\", 1).strip().replace(\" (\", \", \").rstrip(\")\")\n    head = f\"{inc} {int(sal):,}만원({band}) → 공제율 {c['rate_txt']}\" if sal else f\"공제율 {c['rate_txt']}\"\n    paid, base = int(c[\"paid\"]), int(c[\"base\"])\n    sub = c.get(\"subcap\")\n    if sub:\n        mid = (f\"납입 {paid:,}만원(연금저축 {int(sub[0]):,} + IRP {int(sub[1]):,}) → 공제 대상 {base:,}만원\"\n               f\"(연금저축분은 600만원까지)\")\n    elif paid > base:\n        mid = f\"납입 {paid:,}만원 → 공제 대상 {base:,}만원(한도 {int(c['limit']):,}만원, 초과 {paid - base:,}만원은 대상 아님)\"\n    else:\n        mid = f\"납입 {paid:,}만원 → 공제 대상 {base:,}만원(한도 {int(c['limit']):,}만원 이내)\"\n    # '|'는 표 후처리가 표 행으로 오인하므로 쓰지 않는다\n    return (f\"계산 결과 요약: {head} / {mid} / 예상 세액공제액 약 {c['credit']:,.1f}만원\"\n            f\"(= {base:,}만원 × {c['rate_txt']})\")\n\n\ndef calc_pension_tax_credit(salary, paid, limit=900, limit_label=\"연금저축+IRP 합산\",\n                            subcap=None, income_kind=\"총급여\"):\n    \"\"\"연금계좌 세액공제 계산기 — 문서 기준 반영\n"], ["    # v9.13: 답변을 코드로 검증하기 위해 계산 값도 함께 돌려준다\n    return \"\\n\".join(lines), {\"paid\": paid, \"base\": base, \"limit\": LIMIT,\n                              \"rate\": rate, \"rate_txt\": rate_txt, \"credit\": credit,\n                              \"subcap\": subcap if sub_line else None}\n\n\n# v9.13: LLM이 계산기 결과를 무시하고 다른 숫자를 쓰는 문제를 코드로 교정한다.\n#   실제 오류: 400만+300만=700만인데 답변이 '합산 900만원 x 13.2% = 118.8만원'\n#   → 프롬프트 지시만으로는 막히지 않아 계산식 자체를 정규식으로 바로잡는다.\n_CALC_PAT = re.compile(\n    r\"([\\d,]+)\\s*만\\s*원?\\s*[x\\u00d7X*]\\s*([\\d.]+)\\s*%\\s*=\\s*(?:약\\s*)?([\\d,.]+)\\s*만\\s*원?\")\n\n\ndef enforce_calc(ans, calc):\n", "    # v9.13: 답변을 코드로 검증하기 위해 계산 값도 함께 돌려준다\n    return \"\\n\".join(lines), {\"paid\": paid, \"base\": base, \"limit\": LIMIT,\n                              \"rate\": rate, \"rate_txt\": rate_txt, \"credit\": credit,\n                              \"subcap\": subcap if sub_line else None,\n                              \"salary\": salary, \"inc_label\": inc_label, \"band\": band}\n\n\n# v9.13: LLM이 계산기 결과를 무시하고 다른 숫자를 쓰는 문제를 코드로 교정한다.\n#   실제 오류: 400만+300만=700만인데 답변이 '합산 900만원 x 13.2% = 118.8만원'\n#   → 프롬프트 지시만으로는 막히지 않아 계산식 자체를 정규식으로 바로잡는다.\n_CALC_PAT = re.compile(\n    r\"([\\d,]+)\\s*만\\s*원?\\s*[x\\u00d7X*]\\s*([\\d.]+)\\s*%\\s*=\\s*(?:약\\s*)?([\\d,.]+)\\s*만\\s*(?:(\\d)\\s*천\\s*)?원?\")\n# v13.8(G2): '= 118만8천원'처럼 'N만 M천원' 표기의 결과값을 118이 아니라 118.8로 읽는다(글리치 방지)\n\n\ndef _calc_c(m):\n    c = float(m.group(3).replace(\",\", \"\"))\n    if m.lastindex and m.lastindex >= 4 and m.group(4):\n        c += int(m.group(4)) / 10\n    return c\n\n\ndef enforce_calc(ans, calc):\n"], ["        try:\n            a = float(m.group(1).replace(\",\", \"\"))\n            r = float(m.group(2))\n            c = float(m.group(3).replace(\",\", \"\"))\n        except ValueError:\n            return m.group(0)\n        if abs(a - base) < 0.5 and abs(r - calc[\"rate\"] * 100) < 0.01 \\\n                and abs(c - credit) < 0.05:\n            return m.group(0)          # 계산기와 일치 → 그대로 둔다\n        cnt[0] += 1\n        return correct\n", "        try:\n            a = float(m.group(1).replace(\",\", \"\"))\n            r = float(m.group(2))\n            c = _calc_c(m)\n        except ValueError:\n            return m.group(0)\n        if abs(a - base) < 0.5 and abs(r - calc[\"rate\"] * 100) < 0.01 \\\n                and abs(c - credit) < 0.06:\n            return m.group(0)          # 계산기와 일치 → 그대로 둔다\n        cnt[0] += 1\n        return correct\n"], ["                out.append(sent)\n        ans = \"\\n\".join(out)\n\n    # v13.7(G5): 계산기 정답이 소수(118.8)인데 LLM이 반올림 정수(약 119만원)만 썼으면 소수 정답으로 복원.\n    #   K6 때 지적된 안전망의 부분 문자열 결함('119' in '119.28')도 함께 제거 — 표기 형태로 판정한다.\n    _c1 = f\"{credit:,.1f}\"\n", "                out.append(sent)\n        ans = \"\\n\".join(out)\n\n    # v13.8(F3): 반대 소득 구간으로 계산한 공제액(예: 종합소득 5,000만 → 16.5%로 99.0만원)이 본문에\n    #   남아 있으면 공제 문맥에서만 계산기 값으로 교정하고, '반대 세율을 적용한 결과' 단정 문장은\n    #   조건문(…면/경우)이 아닐 때만 계산기 판정 문장으로 교체한다.\n    _other_rate = 0.132 if abs(calc[\"rate\"] - 0.165) < 1e-6 else 0.165\n    _other_txt = \"13.2%\" if _other_rate == 0.132 else \"16.5%\"\n    _other_credit = base * _other_rate\n    if abs(_other_credit - credit) > 0.05:\n        _oc = f\"{_other_credit:,.1f}\"\n        _forms = [re.escape(_oc)] + ([re.escape(f\"{int(_other_credit):,}\")] if abs(_other_credit - int(_other_credit)) < 0.04 else [])\n        def _oc_fix(m):\n            if re.search(r\"공제|세액|환급|돌려\", ans[max(0, m.start() - 40):m.start()]):\n                cnt[0] += 1\n                return f\"약 {credit:,.1f}만원\"\n            return m.group(0)\n        for _f in _forms:\n            ans = re.sub(rf\"(?<![\\d.,])(?:약\\s*)?{_f}\\s*만\\s*원\", _oc_fix, ans)\n    if calc.get(\"salary\") and _other_txt in ans:\n        _sents = re.split(r\"(?<=[.!?])\\s+|\\n\", ans)\n        _new = []\n        for _s in _sents:\n            if _other_txt in _s and re.search(r\"적용|해당\", _s) \\\n                    and not re.search(r\"면\\b|면,|면 |경우|라면|때는|시에는|:\", _s) \\\n                    and calc[\"rate_txt\"] not in _s:\n                _inc = calc.get(\"inc_label\", \"총급여\")\n                _bd = calc.get(\"band\", \"\").replace(_inc, \"\", 1).strip().replace(\" (\", \", \").rstrip(\")\")\n                _new.append(f\"{_inc} {int(calc['salary']):,}만원은 {_bd}에 해당하므로 \"\n                            f\"세액공제율은 {calc['rate_txt']}입니다.\")\n                cnt[0] += 1\n            else:\n                _new.append(_s)\n        ans = \"\\n\".join(_new)\n\n    # v13.7(G5): 계산기 정답이 소수(118.8)인데 LLM이 반올림 정수(약 119만원)만 썼으면 소수 정답으로 복원.\n    #   K6 때 지적된 안전망의 부분 문자열 결함('119' in '119.28')도 함께 제거 — 표기 형태로 판정한다.\n    _c1 = f\"{credit:,.1f}\"\n"], ["    _has = (_c1 in ans) \\\n        or (abs(credit - _ci) <= 0.04 and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\", ans)) \\\n        or (_cf and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\\s*{_cf}\\s*천\", ans))\n    # 교정 후에도 정답 금액이 답변에 없으면 결과를 맨 위에 못박는다\n    if not _has:\n        ans = (f\"납입하신 {int(base):,}만원에 대한 예상 세액공제액은 \"\n               f\"**{correct}**입니다.\\n\\n\" + ans)\n        cnt[0] += 1\n    if cnt[0]:\n        return ans, f\" (계산식 코드 교정 {cnt[0]}건)\"\n", "    _has = (_c1 in ans) \\\n        or (abs(credit - _ci) <= 0.04 and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\", ans)) \\\n        or (_cf and re.search(rf\"(?<![\\d.,]){_ci:,}\\s*만\\s*{_cf}\\s*천\", ans))\n    # v13.8: 정답 금액이 본문에 없어도 여기서 문장을 덧붙이지 않는다 — 호출부가 계산 요약 줄을\n    #   항상 맨 앞에 고정하므로(calc_summary_line) 중복 없이 결정적 결과가 보장된다.\n    if not _has:\n        cnt[0] += 1\n    if cnt[0]:\n        return ans, f\" (계산식 코드 교정 {cnt[0]}건)\"\n"], ["        try:\n            a = float(m.group(1).replace(\",\", \"\"))\n            r = float(m.group(2))\n            c = float(m.group(3).replace(\",\", \"\"))\n        except ValueError:\n            return m.group(0)\n        t = round(a * r / 100, 1)\n", "        try:\n            a = float(m.group(1).replace(\",\", \"\"))\n            r = float(m.group(2))\n            c = _calc_c(m)\n        except ValueError:\n            return m.group(0)\n        t = round(a * r / 100, 1)\n"], ["            info = extract_json(TAX_EXTRACT_SYSTEM, q_llm)\n            salary = to_num(info.get(\"총급여\"))\n            paid = to_num(info.get(\"납입액\"))\n            # v13.6(K6): '연금저축 600만원, IRP 600만원'처럼 두 계좌 금액을 LLM 추출기가 합산 못 해\n            #   납입액이 비면, 질문에서 연금저축/IRP 뒤 금액을 결정적으로 찾아 합산한다(계산기 실행 보장).\n            if not paid:\n", "            info = extract_json(TAX_EXTRACT_SYSTEM, q_llm)\n            salary = to_num(info.get(\"총급여\"))\n            paid = to_num(info.get(\"납입액\"))\n            # v13.8(F3): [1단계 소득 슬롯] 추출기가 비면 질문에서 소득 금액·유형을 결정적으로 뽑는다.\n            #   '종합소득금액 5,000만원'을 LLM 추출기가 총급여로 못 잡아 계산기가 안 돌던 원인.\n            _det_sal, _det_kind = extract_income(question)\n            if not salary and _det_sal:\n                salary = _det_sal\n                action_note += f\" → 소득 결정적 추출({_det_kind} {int(salary):,}만원)\"\n            # v13.6(K6): '연금저축 600만원, IRP 600만원'처럼 두 계좌 금액을 LLM 추출기가 합산 못 해\n            #   납입액이 비면, 질문에서 연금저축/IRP 뒤 금액을 결정적으로 찾아 합산한다(계산기 실행 보장).\n            if not paid:\n"], ["        ans, _na = fix_arith(ans)\n        if _na:\n            calc_note += f\" (산술 재검증 교정 {_na}건)\"\n        # v11.6: 소득 미상(band_vals) 합산 맥락 — enforce_calc가 안 도는 경우, LLM이 남긴\n        #   잘못된 '한도 X만원 적용' 결론을 계산된 공제대상으로 재작성(값 기반, 하드코딩 아님).\n        #   순서: (LLM 답변) → 여기서 결론 교정 → 이후 소득미상 카드(base 기준) 부착 → 최종정리.\n", "        ans, _na = fix_arith(ans)\n        if _na:\n            calc_note += f\" (산술 재검증 교정 {_na}건)\"\n        # v13.8: [5단계 결과 출력] 계산기가 돌았으면 코드가 만든 요약 줄을 답변 맨 앞에 항상 고정.\n        #   LLM 본문 표기(118만8천원·약 119만원 등)와 무관하게 첫 줄은 결정적 계산 결과.\n        if calc_vals:\n            _sum = calc_summary_line(calc_vals)\n            if _sum not in ans:\n                ans = _sum + \"\\n\\n\" + ans.lstrip()\n                calc_note += \" 계산 요약 줄 고정\"\n        # v11.6: 소득 미상(band_vals) 합산 맥락 — enforce_calc가 안 도는 경우, LLM이 남긴\n        #   잘못된 '한도 X만원 적용' 결론을 계산된 공제대상으로 재작성(값 기반, 하드코딩 아님).\n        #   순서: (LLM 답변) → 여기서 결론 교정 → 이후 소득미상 카드(base 기준) 부착 → 최종정리.\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def calc_summary_line(" in src: print("[스킵] 이미 v13.8 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v137과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v137_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
