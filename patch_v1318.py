# -*- coding: utf-8 -*-
"""patch_v1318.py — v1317(f5f2e220) main.py 에 v13.18 적용. (v1317 적용 후 실행)
  [사용자 정독 지적 10건 중 원문 확인 없이 구조적으로 확정된 것 — 전부 결정적]
   N6: 질문이 '중도해지 빼고'면 중도해지·연금 외 수령 문장 제거.   M4b: '기한 필요 없어'면 N일 이내·기한 문장 제거.
   N4: '나이는 넘어가고'면 '…나이를 넘어서' 문장 제거, ISA 질문이 아니면 '직전 과세기간' 누출 문장 제거.
   M2·G10: 실적배당형 문맥의 '원금 보전에 중점'·'가장 안전한 선택'·'원금이 보호'를 문서 기준 표현으로 완화.
   F01: '예금·국채 등 안전자산 … 원금 손실' 혼합 서술을 원리금보장형/실적배당형 두 문장으로 분리.
   A4·M3b: '약 66만원을 돌려받을 것' 단정 → '계산상 세액공제액 … 실제 환급액은 결정세액 등에 따라 달라질 수 있음'.
   S4: 표에서 '자료 없음'인 항목을 본문이 '각각 3등급과 2등급'으로 단정한 문장 제거, 주석의 '세 상품' 고정 문구를 실제 상품 수로.
   (S1·S3·S4 상품 수치 자체는 코퍼스 확인 후 별도 처리)
자동백업·자가검증. 이미 v13.18이면 스킵. 검증 md5==19df2cda032af5bf31da1a0a0e7866a7"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='f5f2e220aa24ef4d22b4ee58b489baf3'
EXPECT_AFTER='19df2cda032af5bf31da1a0a0e7866a7'
HUNKS=[["        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        for st in sents:\n            if _PB_FUND.search(st) and _PB_CLAIM.search(st) and not _PB_SAFE.search(st):\n                st2 = re.sub(r\"거의\\s*원금(?:이|을|은)?\\s*보장(?:되면서|되고|되며|되어|돼)\", \"원금 손실 위험이 매우 낮으면서\", st)\n                st2 = re.sub(r\"원금(?:이|을|은)?\\s*보장(?:되는|되며|되고|됩니다|된다|돼요|되어)\", \"원금 손실 위험이 낮은 편이지만 원금이 보장되지는 않는\", st2) if st2 == st else st2\n", "        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        for st in sents:\n            # v13.18(M2·G10): 실적배당형(펀드·채권형·위험등급 문맥)에 원금보장처럼 읽히는 표현도 완화\n            if _PB_FUND.search(st) and not _PB_SAFE.search(st):\n                st_new = st\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금이\\s*보호되면서\", \"원금 손실 위험이 상대적으로 낮으면서\", st_new)\n                st_new = re.sub(r\"원금\\s*손실의\\s*가능성이\\s*낮아\\s*\", \"원금 손실 가능성이 상대적으로 낮아(다만 실적배당형이라 원금 보장은 아님) \", st_new)\n                if st_new != st:\n                    n += 1\n                    st = st_new\n            if _PB_FUND.search(st) and _PB_CLAIM.search(st) and not _PB_SAFE.search(st):\n                st2 = re.sub(r\"거의\\s*원금(?:이|을|은)?\\s*보장(?:되면서|되고|되며|되어|돼)\", \"원금 손실 위험이 매우 낮으면서\", st)\n                st2 = re.sub(r\"원금(?:이|을|은)?\\s*보장(?:되는|되며|되고|됩니다|된다|돼요|되어)\", \"원금 손실 위험이 낮은 편이지만 원금이 보장되지는 않는\", st2) if st2 == st else st2\n"], ["            hit.append(lab)\n    if not hit:\n        return ans, 0\n    note = (\"※ \" + \"·\".join(f\"'{h}'\" for h in hit)\n            + \" 항목은 일부 상품의 값이 자료에서 확인되지 않아, 세 상품 전체에 대한 \"\n              \"일반화가 아니라 확인된 상품에 한한 설명입니다.\")\n    if note in ans:\n        return ans, 0\n    return ans.rstrip() + \"\\n\\n\" + note, len(hit)\n\n\n\n", "            hit.append(lab)\n    if not hit:\n        return ans, 0\n    _ncol = max((len(_cells(lines[i])) - 1 for i in idx), default=0)\n    _cnt_txt = {2: \"두 상품\", 3: \"세 상품\", 4: \"네 상품\"}.get(_ncol, f\"{_ncol}개 상품\")\n    note = (\"※ \" + \"·\".join(f\"'{h}'\" for h in hit)\n            + f\" 항목은 일부 상품의 값이 자료에서 확인되지 않아, {_cnt_txt} 전체에 대한 \"\n              \"일반화가 아니라 확인된 상품에 한한 설명입니다.\")\n    if note in ans:\n        return ans, 0\n    return ans.rstrip() + \"\\n\\n\" + note, len(hit)\n\n\ndef drop_contradicting_summary(ans):\n    \"\"\"v13.18(S4): 표에서 어떤 상품의 값이 '자료 없음'인 항목(예: 위험 등급)을 본문이 '각각 3등급과 2등급'처럼\n    모든 상품에 값을 단정하면 그 문장을 제거한다(표와 본문의 수치 충돌 방지). 표가 없으면 불변.\"\"\"\n    lines = ans.splitlines()\n    idx = [i for i, ln in enumerate(lines) if ln.count(\"|\") >= 2]\n    conv = [i for i, ln in enumerate(lines) if re.match(r\"\\s*-\\s*[^:：]{1,20}[:：].*;\", ln)]   # 변환된 표 행\n    partial = []\n    for i in idx[1:]:\n        c = _cells(lines[i])\n        if len(c) >= 3 and any(\"자료 없음\" in x for x in c[1:]) and not all(\"자료 없음\" in x for x in c[1:]):\n            partial.append(c[0])\n    for i in conv:\n        m = re.match(r\"\\s*-\\s*([^:：]{1,20})[:：](.*)$\", lines[i])\n        vals = [v.strip() for v in m.group(2).split(\";\")]\n        if len(vals) >= 2 and any(\"자료 없음\" in v for v in vals) and not all(\"자료 없음\" in v for v in vals):\n            partial.append(m.group(1).strip())\n    if not partial:\n        return ans, 0\n    n = 0\n    out = []\n    for ln in lines:\n        if ln.count(\"|\") >= 2 or re.match(r\"\\s*-\\s*[^:：]{1,20}[:：].*;\", ln):\n            out.append(ln); continue\n        sents = re.split(r\"(?<=[.!?])(?<!\\d\\.)\\s+\", ln)\n        keep = []\n        for st in sents:\n            bad = False\n            for lab in partial:\n                keys = [k for k in re.split(r\"[()·\\s/]+\", lab) if len(k) >= 2]\n                if any(k in st for k in keys) and re.search(r\"각각[^.]{0,40}\\d\\s*등급[^.]{0,20}\\d\\s*등급|각각[^.]{0,30}\\d[\\d.]*\\s*%[^.]{0,20}\\d[\\d.]*\\s*%\", st):\n                    bad = True\n            if bad:\n                n += 1\n                continue\n            keep.append(st)\n        out.append(\" \".join(keep))\n    return \"\\n\".join(out), n\n\n\n\n"], ["            ans = (f\"문의하신 '{_promo.group(0)}' 혜택은 제공된 자료에서 확인할 수 없습니다. \"\n                   \"제공된 자료에는 해당 혜택에 관한 내용이 없어 안내해 드리기 어렵습니다.\" + _tail)\n            clean_note += \" 판촉혜택 미확인 고지(본문 교체)\"\n        # (1-6e) v13.13 일반 게이트 1: 문서에 없는 특정 상품명 → ※주석 병기(삭제 없음), 펀드 '원금 보장' 단정 완화\n        ans, _unk = annotate_unknown_products(ans, question)\n        if _unk:\n", "            ans = (f\"문의하신 '{_promo.group(0)}' 혜택은 제공된 자료에서 확인할 수 없습니다. \"\n                   \"제공된 자료에는 해당 혜택에 관한 내용이 없어 안내해 드리기 어렵습니다.\" + _tail)\n            clean_note += \" 판촉혜택 미확인 고지(본문 교체)\"\n        # (1-6f) v13.18: 사용자가 '빼고·필요 없어'라고 한 주제의 문장 제거 + 단정 완화 + 혼합 서술 분리 (전부 결정적)\n        _body, _tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n        _tail = (\"[참고 문서]\" + _tail) if \"[참고 문서]\" in ans else \"\"\n        _drop_pats = []\n        if re.search(r\"(?:중도\\s*해지|해지|중도\\s*인출)[^\\n]{0,12}?(?:빼고|말고|제외|넘어가|됐고|필요\\s*없)\", question):\n            _drop_pats.append(r\"중도\\s*해지|연금\\s*외\\s*수령|연금외수령|해지\\s*사유\")           # N6\n        if re.search(r\"기한[^\\n]{0,12}?(?:빼고|말고|제외|넘어가|됐고|필요\\s*없)\", question):\n            _drop_pats.append(r\"\\d+\\s*일\\s*(?:이내|내에|안에)|기한\")                        # M4b\n        if re.search(r\"(?:나이|수령\\s*나이)[^\\n]{0,8}?(?:넘어가|됐고|빼고|말고)\", question):\n            _drop_pats.append(r\"수령\\s*나이를?\\s*넘어\")                                    # N4 첫 문장\n        if not re.search(r\"ISA\", question, re.I):\n            _drop_pats.append(r\"직전\\s*과세기간\")                                         # N4: ISA 규정 누출\n        if _drop_pats:\n            _pat = re.compile(\"|\".join(_drop_pats))\n            _outl, _nd = [], 0\n            for _ln in _body.split(\"\\n\"):\n                _sents = re.split(r\"(?<=[.!?])(?<!\\d\\.)\\s+\", _ln)\n                _keep = [st for st in _sents if not _pat.search(st)]\n                _nd += len(_sents) - len(_keep)\n                if _ln.strip() and not \"\".join(_keep).strip():\n                    continue\n                _outl.append(\" \".join(_keep))\n            if _nd:\n                _body = \"\\n\".join(_outl)\n                clean_note += f\" 제외요청 주제 문장 {_nd}건 제거\"\n        # F01: '예금·국채 등 안전자산 … 원금 손실' 혼합 서술 → 원리금보장형/실적배당형 분리\n        _b2, _nf = re.subn(r\"[^.!?\\n]*예금[^.!?\\n]*(?:국채|채권)[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?]\"\n                            r\"(?:\\s*[^.!?\\n]*(?:금리|가격)\\s*변동[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?])?\",\n                            \"예금 등 원리금보장형 상품은 해당 상품의 보장 조건에 따라 원리금이 보장될 수 있습니다. \"\n                            \"반면 채권·펀드·ETF 등 실적배당형 상품은 가격 변동으로 손실이 발생할 수 있습니다.\", _body)\n        if _nf:\n            _body = _b2\n            clean_note += \" 예금·채권 위험 혼합서술 분리\"\n        # A4: 세액공제액을 실제 환급액처럼 단정('약 66만원을 돌려받을 것으로 예상') → 계산상 공제액 + 환급 안내\n        if calc_vals:\n            _b3, _na4 = re.subn(r\"[^.!?\\n]*(?:(?:약\\s*)?[\\d,.]+\\s*만\\s*원[^.!?\\n]{0,10}?돌려받|돌려받[^.!?\\n]{0,25}?(?:약\\s*)?[\\d,.]+\\s*만\\s*원)[^.!?\\n]*[.!?]\",\n                                f\"계산상 세액공제액은 약 {fmt_credit(calc_vals['credit'])}만원입니다. \"\n                                \"실제 연말정산 환급액은 결정세액과 이미 납부한 세액 등에 따라 달라질 수 있습니다.\", _body)\n            if _na4:\n                _body = _b3\n                clean_note += \" 환급액 단정 완화\"\n        ans = (_body.rstrip() + (\"\\n\\n\" + _tail if _tail else \"\")) if _tail else _body\n        # (1-6e) v13.13 일반 게이트 1: 문서에 없는 특정 상품명 → ※주석 병기(삭제 없음), 펀드 '원금 보장' 단정 완화\n        ans, _unk = annotate_unknown_products(ans, question)\n        if _unk:\n"], ["            ans, n_gen = warn_partial_generalization(ans)\n            if n_gen:\n                clean_note += f\" 부분자료 일반화 주의 {n_gen}건\"\n\n        # (1-9z) v9.83: 6.5단계 이후 붙은 블록(기한 보강 등)의 파일명을 한 번 더 청소\n        ans = \"\\n\".join(\n", "            ans, n_gen = warn_partial_generalization(ans)\n            if n_gen:\n                clean_note += f\" 부분자료 일반화 주의 {n_gen}건\"\n        ans, _ncs = drop_contradicting_summary(ans)   # v13.18(S4): 표 '자료 없음' vs 본문 '각각 N등급' 충돌\n        if _ncs:\n            clean_note += f\" 표-본문 수치충돌 문장 {_ncs}건 제거\"\n\n        # (1-9z) v9.83: 6.5단계 이후 붙은 블록(기한 보강 등)의 파일명을 한 번 더 청소\n        ans = \"\\n\".join(\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def drop_contradicting_summary(" in src: print("[스킵] 이미 v13.18 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1317과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1317_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
