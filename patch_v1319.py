# -*- coding: utf-8 -*-
"""patch_v1319.py — v1318(19df2cda) main.py 에 v13.19 적용. (v1318 적용 후 실행)
  [잔여 버그 4 + 정답표 + 필드 가드 + 제외 조건 일반 파서]
   E7: 계산기 실행 시 한도 구조 카드(600/900, 소득 무관) 항상 주입 + '한도보다 작은 금액을 한도/제한으로 서술'·
       '소득이 높은 경우 한도' 문장 일반 제거(옛 약관 300만원).   D9: 공제액 치환이 요약 줄의 초과분(100만원)까지
       건드리던 v13.17 버그 — 요약 줄 제외·'초과' 보호.   N6: 55세 미만 구간은 오류로 처리.   M2: '원금을 보호하면서'·
       '원금 손실 가능성 최소화' 완화.
   PRODUCT_FACTS(코퍼스 확인값만): 인덱스12M 6등급·C 총보수 0.42%(판매보수 0.28%, 동종 0.40%, 총보수·비용 0.4313%,
       기간별 43·87·134·234·532천원은 비용 예시), 또박또박 C-P2 합성총보수 0.87%·보통위험, 삼성클래식 채권 5등급·주식 2등급,
       TIGER 미국배당다우존스 0.08%·높은위험. 질문에 있으면 카드 주입 + 답변의 등급(라벨 포함)·'N년: x%' 오독 블록·
       근거 없는 클래스별 보수 나열을 사실 문장으로 교정. TDF는 코퍼스 미확정이라 제외.
   제외 조건 일반 파서: '<주제> 빼고/말고/제외/넘어가고/됐고/필요 없어/생략' → 주제 사전(중도해지·인출·기한·이전·나이·
       회사부담금·퇴직금·ISA·수수료·세액공제·연금소득세·디폴트옵션)의 문장 패턴 제거. 요청 부분에 같은 주제가 있으면 제외 안 함.
자동백업·자가검증. 이미 v13.19면 스킵. 검증 md5==c14225ff4968dae81ef80a782a4aaf14"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='19df2cda032af5bf31da1a0a0e7866a7'
EXPECT_AFTER='c14225ff4968dae81ef80a782a4aaf14'
HUNKS=[["    for _s in _sents:\n        _n = re.sub(r\"\\s+\", \"\", _s)\n        drop = False\n        if re.search(r\"한도[가는은]?\\s*300만\\s*원|300만\\s*원[^.]{0,10}한도\", _s) and calc[\"limit\"] != 300:\n            drop = True                                       # 옛 규정 300만원 한도\n        elif \"소득구간에따라세액공제한도가다르\" in _n or \"소득에따라세액공제한도가다르\" in _n:\n            drop = True                                       # 현행 한도는 소득과 무관(600/900)\n        elif abs(base - calc[\"paid\"]) < 0.5 and re.search(r\"전액[^.]{0,12}(?:받을수없|공제되지않|공제받을수없)\", _n):\n", "    for _s in _sents:\n        _n = re.sub(r\"\\s+\", \"\", _s)\n        drop = False\n        _amts = [float(x.replace(\",\", \"\")) for x in re.findall(r\"(\\d[\\d,]*)\\s*만\\s*원\", _s)]\n        if re.search(r\"한도|제한\", _s) and any(0 < v < calc[\"limit\"] - 0.5 and abs(v - calc[\"paid\"]) > 0.5\n                                              and abs(v - base) > 0.5 and v not in (600, 900, 1800) for v in _amts):\n            drop = True                                       # v13.19(E7): 한도보다 작은 금액을 '한도/제한'으로 서술(옛 규정 300만원 등)\n        elif re.search(r\"(?:소득이\\s*높|고소득|소득\\s*구간|소득에\\s*따라)[^.]{0,20}(?:한도|제한)\", _s):\n            drop = True                                       # 현행 한도는 소득과 무관\n        elif \"소득구간에따라세액공제한도가다르\" in _n or \"소득에따라세액공제한도가다르\" in _n:\n            drop = True                                       # 현행 한도는 소득과 무관(600/900)\n        elif abs(base - calc[\"paid\"]) < 0.5 and re.search(r\"전액[^.]{0,12}(?:받을수없|공제되지않|공제받을수없)\", _n):\n"], ["    _lo, _hi = base * 0.05, base * 0.20               # v13.17: 옛 한도(300만)로 계산한 39.6 같은 값도 범위 안\n    def _amt_fix(m):\n        v = _amt_val(m)\n        if re.search(r\"(?:IRP|연금저축|납입|추가|한도)\\s*$\", ans_line_ctx[0][max(0, m.start() - 8):m.start()]):\n            return m.group(0)                                  # 납입액·한도 금액은 대상 아님\n        if _lo <= v <= _hi and abs(v - credit) > 0.11:\n            cnt[0] += 1\n            return f\"약 {ctxt}만원\"\n", "    _lo, _hi = base * 0.05, base * 0.20               # v13.17: 옛 한도(300만)로 계산한 39.6 같은 값도 범위 안\n    def _amt_fix(m):\n        v = _amt_val(m)\n        if re.search(r\"(?:IRP|연금저축|납입|추가|한도|초과|중)\\s*$\", ans_line_ctx[0][max(0, m.start() - 8):m.start()]):\n            return m.group(0)                                  # 납입액·한도·초과분 금액은 대상 아님\n        if _lo <= v <= _hi and abs(v - credit) > 0.11:\n            cnt[0] += 1\n            return f\"약 {ctxt}만원\"\n"], ["    _out2 = []\n    for _ln in ans.split(\"\\n\"):\n        ans_line_ctx[0] = _ln\n        if re.search(r\"세액공제|공제액|돌려받|예상\\s*금액|환급\", _ln) and correct not in _ln:\n            _ln = _AMT.sub(_amt_fix, _ln)\n            _ln = re.sub(r\"약\\s*약\\s*\", \"약 \", _ln)\n        _out2.append(_ln)\n", "    _out2 = []\n    for _ln in ans.split(\"\\n\"):\n        ans_line_ctx[0] = _ln\n        if re.search(r\"세액공제|공제액|돌려받|예상\\s*금액|환급\", _ln) and correct not in _ln \\\n                and not _ln.startswith(\"계산 결과 요약\"):            # v13.19(D9): 요약 줄은 코드가 만든 것 — 불변\n            _ln = _AMT.sub(_amt_fix, _ln)\n            _ln = re.sub(r\"약\\s*약\\s*\", \"약 \", _ln)\n        _out2.append(_ln)\n"], ["    return ans, unknown\n\n\n# v13.13(일반 게이트 1b — 유형-속성 모순): 펀드·ETF·투자신탁(실적배당형)을 언급한 문장에서 '원금 보장'을\n#   긍정 단정하면 문서 기준(투자원금 비보장)으로 완화한다. 예금·원리금보장상품 문맥이나 부정문은 불변.\n_PB_FUND = re.compile(r\"펀드|ETF|투자신탁|TDF|리츠|위험\\s*등급\")\n", "    return ans, unknown\n\n\n# v13.19: PRODUCT_FACTS — 코퍼스에서 직접 확인한 빈출 상품 사실만 고정(정답표). 검색이 흔들려도 핵심 사실은 유지.\n#   값의 근거: 각 항목의 src 문서 원문(corpus_facts.py 로 확인). 확인되지 않은 속성은 넣지 않는다.\nPRODUCT_FACTS = [\n    {\"key\": r\"삼성\\s*클래식\\s*연금[^\\n]{0,30}?(?:주식|\\[주식\\])|삼성클래식연금증권전환형자투자신탁\",\n     \"name\": \"삼성클래식연금증권전환형자투자신탁 제1호[주식]\", \"grade\": \"2등급(높은위험)\", \"src\": \"R2_KR5114450222.pdf\"},\n    {\"key\": r\"삼성\\s*클래식\\s*연금[^\\n]{0,30}?(?:채권|\\[채권\\])|삼성클래식연금증권전환형투자신탁(?!.*자투자)\",\n     \"name\": \"삼성클래식연금증권전환형투자신탁 제1호[채권]\", \"grade\": \"5등급(낮은위험)\", \"src\": \"R2_KR5114420022.pdf\"},\n    {\"key\": r\"인덱스\\s*12M|퇴직연금인덱스\",\n     \"name\": \"삼성퇴직연금인덱스12M증권자투자신탁 제1호[채권]\", \"grade\": \"6등급(매우낮은위험)\",\n     \"fee\": \"총보수 연 0.42%(수수료미징구-오프라인-퇴직연금(C) 클래스, 판매보수 0.28% 포함; 동종유형 총보수 0.40%, 총보수·비용 0.4313%). \"\n            \"온라인-퇴직연금(Ce) 클래스는 총보수 0.28%. 문서 표의 1년·2년·3년·5년·10년 숫자(43·87·134·234·532)는 \"\n            \"1,000만원 투자 시 투자기간별 총보수·비용 예시(천원 단위)이지 연 보수율이 아님\",\n     \"src\": \"R2_KR5114420046.pdf\"},\n    {\"key\": r\"또박또박\",\n     \"name\": \"미래에셋투자하며또박또박연금받는펀드 C-P2\", \"grade\": \"보통위험\",\n     \"fee\": \"합성총보수 연 0.87%(C-P2 클래스). 다른 클래스의 보수는 자료에서 확인되지 않음. 운용설정액 37억(Fund Doctor Pro, 2025.6.30 기준)\",\n     \"src\": \"doc37.pdf\"},\n    {\"key\": r\"미국배당다우존스\",\n     \"name\": \"TIGER 미국배당다우존스\", \"grade\": \"높은위험\",\n     \"fee\": \"합성총보수 0.08%, 증권거래비용 0.0611%. 운용설정액 20,124억(2025.6.30 기준)\", \"src\": \"doc37.pdf\"},\n]\n\n\ndef product_fact_card(question):\n    \"\"\"질문에 정답표 상품이 있으면 카드 문자열, 없으면 None\"\"\"\n    hits = [f for f in PRODUCT_FACTS if re.search(f[\"key\"], question)]\n    if not hits:\n        return None, []\n    lines = [\"[상품 사실표 — 문서에서 직접 확인된 값. 다른 값을 쓰지 말고 이 값을 그대로 인용하라]\"]\n    for f in hits:\n        ln = f\"- {f['name']}: 위험등급 {f['grade']}\"\n        if f.get(\"fee\"):\n            ln += f\"; {f['fee']}\"\n        ln += f\" (출처 {f['src']})\"\n        lines.append(ln)\n    return \"\\n\".join(lines), hits\n\n\ndef enforce_product_facts(ans, hits):\n    \"\"\"답변이 정답표 상품의 등급·보수를 다르게 쓰면 교정. 'N년: x%' 기간별 오독 블록은 사실 문장으로 교체. → (답변, 건수)\"\"\"\n    n = 0\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n    for f in hits:\n        g = re.match(r\"(\\d)\\s*등급\", f[\"grade\"])\n        if g:\n            def _grade_fix(m, _g=g.group(1), _full=f[\"grade\"]):\n                nonlocal n\n                if m.group(1) != _g or (m.group(2) and re.sub(r\"\\s\", \"\", m.group(2)) not in re.sub(r\"\\s\", \"\", _full)):\n                    n += 1\n                    return _full\n                return m.group(0)\n            # 상품명(핵심어)이 든 줄, 또는 정답표 상품이 하나뿐이면 다른 상품명이 없는 줄에서 'N등급(라벨)' 교정\n            _others = [o for o in PRODUCT_FACTS if o is not f]\n            out = []\n            for ln in body.split(\"\\n\"):\n                _mine = bool(re.search(f[\"key\"], ln))\n                _other = any(re.search(o[\"key\"], ln) for o in _others)\n                if _mine or (len(hits) == 1 and not _other and \"등급\" in ln):\n                    ln = re.sub(r\"(\\d)\\s*등급(\\s*\\([^)]{2,12}\\))?\", _grade_fix, ln)\n                out.append(ln)\n            body = \"\\n\".join(out)\n        # S3형: '1년: 0.42% … 10년: 0.532%' 기간별 퍼센트 나열 = 표 열 오독\n        if f.get(\"fee\") and re.search(r\"(?:^|\\n)\\s*[-*•]?\\s*1\\s*년\\s*[:：]\\s*[\\d.]+\\s*%\", body):\n            body, k = re.subn(r\"(?:\\n\\s*[-*•]?\\s*(?:1|2|3|5|10)\\s*년\\s*[:：]\\s*[\\d.]+\\s*%[^\\n]*)+\",\n                              f\"\\n- 문서 기준 {f['fee']}\", body)\n            n += k\n        # S1형: 또박또박 — 자료에 없는 클래스별 보수 나열\n        if f[\"key\"] == r\"또박또박\" and re.search(r\"클래스\\s*(?:A|C-P)\\s*[:：]\\s*[\\d.]+\\s*%\", body):\n            body, k = re.subn(r\"(?:\\n\\s*[-*•]?\\s*클래스\\s*[A-Za-z0-9\\-]+\\s*[:：]\\s*[\\d.]+\\s*%[^\\n]*)+\",\n                              \"\\n- 자료에서 확인되는 것은 C-P2 클래스의 합성총보수 연 0.87%(보통위험)이며, 다른 클래스의 총보수는 \"\n                              \"제공된 자료에서 확인되지 않습니다.\", body)\n            n += k\n    ans = body + (\"\\n\\n\" + tail if tail else \"\")\n    return ans, n\n\n\n# v13.13(일반 게이트 1b — 유형-속성 모순): 펀드·ETF·투자신탁(실적배당형)을 언급한 문장에서 '원금 보장'을\n#   긍정 단정하면 문서 기준(투자원금 비보장)으로 완화한다. 예금·원리금보장상품 문맥이나 부정문은 불변.\n_PB_FUND = re.compile(r\"펀드|ETF|투자신탁|TDF|리츠|위험\\s*등급\")\n"], ["                st_new = st\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금이\\s*보호되면서\", \"원금 손실 위험이 상대적으로 낮으면서\", st_new)\n                st_new = re.sub(r\"원금\\s*손실의\\s*가능성이\\s*낮아\\s*\", \"원금 손실 가능성이 상대적으로 낮아(다만 실적배당형이라 원금 보장은 아님) \", st_new)\n                if st_new != st:\n                    n += 1\n", "                st_new = st\n                st_new = re.sub(r\"원금\\s*보전에\\s*중점을\\s*둡니다\", \"상대적으로 변동성이 낮지만 실적배당형이라 원금이 보장되지는 않습니다\", st_new)\n                st_new = re.sub(r\"가장\\s*안전한\\s*선택(?:을\\s*제공합니다|입니다)\", \"제시된 펀드 중 위험등급이 상대적으로 낮습니다(실적배당형이라 원금 보장은 아닙니다)\", st_new)\n                st_new = re.sub(r\"원금(?:이|을)\\s*보호(?:되면서|하면서|하며|하고)\", \"원금 손실 위험을 상대적으로 낮추면서\", st_new)\n                st_new = re.sub(r\"원금\\s*손실의?\\s*가능성을\\s*최소화하면서\", \"원금 손실 가능성을 상대적으로 낮추면서(실적배당형이라 원금 보장은 아님)\", st_new)\n                st_new = re.sub(r\"원금\\s*손실의\\s*가능성이\\s*낮아\\s*\", \"원금 손실 가능성이 상대적으로 낮아(다만 실적배당형이라 원금 보장은 아님) \", st_new)\n                if st_new != st:\n                    n += 1\n"], ["            r = float(m.group(4))\n            if r not in _TABLE: return False\n            a, b, rel = m.group(1), int(m.group(2)), m.group(3)\n            if a:                                   # '55~64세' 범위형: 하한으로 판정 + 상한은 69/79만 허용\n                lo, hi = int(a), b\n                if hi not in (69, 79) and hi < 100: return True\n", "            r = float(m.group(4))\n            if r not in _TABLE: return False\n            a, b, rel = m.group(1), int(m.group(2)), m.group(3)\n            if b < 55 or (a and int(a) < 55):\n                return True                         # v13.19(N6): 55세 미만 구간은 연금 수령 자체가 없음 → 오류\n            if rel == \"미만\" and b <= 55:\n                return True                         # '55세 미만' 표기도 오류\n            if a:                                   # '55~64세' 범위형: 하한으로 판정 + 상한은 69/79만 허용\n                lo, hi = int(a), b\n                if hi not in (69, 79) and hi < 100: return True\n"], ["                context = _rc + \"\\n\\n\" + context\n                action_note += f\" → 위험등급 원문 카드({len(risk_prof)}개 상품)\"\n\n        # [4-a-02] v9.33: 질문에 소득이 있으면 유형과 무관하게 세율 판정을 못박는다\n        _irc = income_rate_card(question)\n        if _irc:\n", "                context = _rc + \"\\n\\n\" + context\n                action_note += f\" → 위험등급 원문 카드({len(risk_prof)}개 상품)\"\n\n        # [4-a-04b] v13.19: 정답표(PRODUCT_FACTS) 카드 — 질문의 빈출 상품은 코퍼스 확인값을 못박는다\n        _pf_card, _pf_hits = product_fact_card(question)\n        if _pf_card:\n            context = _pf_card + \"\\n\\n\" + context\n            action_note += f\" → 상품 사실표 카드({len(_pf_hits)}개)\"\n\n        # [4-a-02] v9.33: 질문에 소득이 있으면 유형과 무관하게 세율 판정을 못박는다\n        _irc = income_rate_card(question)\n        if _irc:\n"], ["                calc_result, calc_vals = calc_pension_tax_credit(salary, paid, _lim, _lab,\n                                                                 subcap=_sub, income_kind=_ikind)\n                context = calc_result + \"\\n\\n\" + context\n                action_note += (f\" → 계산기 실행 ({_ikind} {int(salary):,}만원, \"\n                               f\"납입액 {int(paid):,}만원, {_lab} 한도 {_lim}만원\"\n                               f\"{', 연금저축 600 하위한도 적용' if calc_vals.get('subcap') else ''})\")\n", "                calc_result, calc_vals = calc_pension_tax_credit(salary, paid, _lim, _lab,\n                                                                 subcap=_sub, income_kind=_ikind)\n                context = calc_result + \"\\n\\n\" + context\n                if LIMIT_CARD not in context:            # v13.19(E7): 옛 약관(고소득자 300만원 한도) 유입 차단\n                    context = LIMIT_CARD + \"\\n\\n\" + context\n                action_note += (f\" → 계산기 실행 ({_ikind} {int(salary):,}만원, \"\n                               f\"납입액 {int(paid):,}만원, {_lab} 한도 {_lim}만원\"\n                               f\"{', 연금저축 600 하위한도 적용' if calc_vals.get('subcap') else ''})\")\n"], ["        _body, _tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n        _tail = (\"[참고 문서]\" + _tail) if \"[참고 문서]\" in ans else \"\"\n        _drop_pats = []\n        if re.search(r\"(?:중도\\s*해지|해지|중도\\s*인출)[^\\n]{0,12}?(?:빼고|말고|제외|넘어가|됐고|필요\\s*없)\", question):\n            _drop_pats.append(r\"중도\\s*해지|연금\\s*외\\s*수령|연금외수령|해지\\s*사유\")           # N6\n        if re.search(r\"기한[^\\n]{0,12}?(?:빼고|말고|제외|넘어가|됐고|필요\\s*없)\", question):\n            _drop_pats.append(r\"\\d+\\s*일\\s*(?:이내|내에|안에)|기한\")                        # M4b\n        if re.search(r\"(?:나이|수령\\s*나이)[^\\n]{0,8}?(?:넘어가|됐고|빼고|말고)\", question):\n            _drop_pats.append(r\"수령\\s*나이를?\\s*넘어\")                                    # N4 첫 문장\n        if not re.search(r\"ISA\", question, re.I):\n", "        _body, _tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n        _tail = (\"[참고 문서]\" + _tail) if \"[참고 문서]\" in ans else \"\"\n        _drop_pats = []\n        # v13.19(일반 게이트 3 — 제외 조건 파서): 질문에서 '<주제> … 빼고/말고/제외/넘어가고/됐고/필요 없어/생략'을\n        #   찾아, 주제별로 미리 정한 '그 주제의 문장 패턴'을 답변에서 제거한다(LLM 파서 없이 결정적).\n        #   주제어 사전(질문 표현 → 답변 문장 패턴). 요청 부분에도 같은 주제가 있으면(예: 세금) 제외하지 않는다.\n        _EXCL_TOPICS = [\n            (r\"중도\\s*해지|해지\", r\"중도\\s*해지|연금\\s*외(?:\\s*방식)?(?:으로)?\\s*수령|연금외수령|해지\\s*사유|기타소득세\"),\n            (r\"중도\\s*인출|인출\", r\"중도\\s*인출|인출\\s*사유\"),\n            (r\"기한|기간\\s*얘기\", r\"\\d+\\s*일\\s*(?:이내|내에|안에)|기한\"),\n            (r\"이전\\s*(?:얘기|얘긴|은|는)|옮기는\\s*(?:얘기|것)\", r\"이전\\s*(?:기한|절차)|60\\s*일|옮기\"),\n            (r\"수령\\s*나이|나이|연령\", r\"수령\\s*나이|만\\s*55\\s*세|55\\s*세\"),\n            (r\"회사\\s*부담금|회사가\\s*넣\", r\"회사\\s*부담금|사용자\\s*부담금\"),\n            (r\"퇴직금\", r\"퇴직금|퇴직급여\"),\n            (r\"ISA\", r\"ISA\"),\n            (r\"수수료|보수\", r\"수수료|보수\"),\n            (r\"세액공제\", r\"세액공제\"),\n            (r\"연금소득세\", r\"연금소득세\"),\n            (r\"디폴트옵션|디폴트\\s*옵션\", r\"디폴트\\s*옵션|사전지정운용\"),\n        ]\n        _NEG = r\"(?:빼고|말고|제외(?:하고)?|넘어가고|됐고|필요\\s*없|생략|건너뛰|안\\s*해도|괜찮)\"\n        for _tq, _tpat in _EXCL_TOPICS:\n            _mneg = re.search(r\"(\" + _tq + r\")[^\\n]{0,14}?\" + _NEG, question)\n            if not _mneg:\n                continue\n            _rest = question[_mneg.end():]                      # 제외 절 뒤의 '요청' 부분\n            if re.search(_tq, _rest):\n                continue                                        # 요청 부분에 같은 주제가 있으면 제외 아님\n            _drop_pats.append(_tpat)\n        if re.search(r\"(?:나이|수령\\s*나이)[^\\n]{0,8}?(?:넘어가|됐고|빼고|말고)\", question):\n            _drop_pats.append(r\"수령\\s*나이를?\\s*넘어\")                                    # N4 첫 문장\n        if not re.search(r\"ISA\", question, re.I):\n"], ["        ans, _pb = soften_fund_guarantee(ans)\n        if _pb:\n            clean_note += f\" 펀드 원금보장 표현 완화 {_pb}건\"\n\n        # (1-6b) v9.41: ISA 전환금 질문이면 '대상 총액'을 코드가 계산해 붙인다.\n        #   조건 없이 붙인다 — '이미 썼는지'를 판단하려 들면 그 판단이 또 어긋난다.\n", "        ans, _pb = soften_fund_guarantee(ans)\n        if _pb:\n            clean_note += f\" 펀드 원금보장 표현 완화 {_pb}건\"\n        # v13.19: 정답표 상품의 등급·보수가 답변에서 다르게 쓰였으면 교정(질문에 그 상품이 있을 때)\n        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n            if _npf:\n                clean_note += f\" 상품 사실표 교정 {_npf}건\"\n\n        # (1-6b) v9.41: ISA 전환금 질문이면 '대상 총액'을 코드가 계산해 붙인다.\n        #   조건 없이 붙인다 — '이미 썼는지'를 판단하려 들면 그 판단이 또 어긋난다.\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "PRODUCT_FACTS = [" in src: print("[스킵] 이미 v13.19 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1318과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1318_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
