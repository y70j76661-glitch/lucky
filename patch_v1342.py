# -*- coding: utf-8 -*-
"""patch_v1342.py — v1341(fdaeb677) main.py 에 v13.42 적용. (v1341 적용 후 실행)
  [추천 렌더러 — 결정적 구조화, LLM 호출 없음] ① 추천 답변의 상품 블록을 근거 기준 구조(머리말→위험 고지→본문→근거 문서→유의)로 재구성
   ② '근거 문서: <출처> — 위험등급 N등급(라벨)' 줄을 정답표/사용 투자설명서(R2_) 원문에서 읽어 추가(출처 줄에 있는 문서로 한정; 정답표 출처는 출처 줄에 추가)
   ③ 블록 안의 등급 오기를 근거 값으로 교정, 표준 유의 문장 블록당 1개·맨 끝, 도입·맺음에 반복된 유의 문장 제거
   ④ 상품이 둘 이상 섞인 블록(비교 머리말)·산문 블록은 불변 ⑤ 펀드 서술 계약: '안정성이 높은' 단정 추가, 단일 상품 답변의 주어는 상품명(NH-Amundi 띄어쓰기 상품명 인식)
자동백업·자가검증. 이미 v13.42면 스킵. 검증 md5==0021cabe08db5a4ecfb74918c110b917"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='fdaeb6772d14866a42a34c7051c70552'
EXPECT_AFTER='0021cabe08db5a4ecfb74918c110b917'
HUNKS=[["def income_rate_card(question):\n    \"\"\"질문의 총급여로 세액공제율을 코드가 판정한 카드 (없으면 None)\"\"\"\n", "# v13.42 [추천 렌더러 — 결정적 구조화(LLM 호출 없음)]\n#   추천 답변의 '상품 블록'(상품명이 든 문단 + 항목줄)을 근거 기준으로 같은 구조로 다시 그린다.\n#   - 블록 내 순서: 머리말 → 위험 고지 → 본문 항목(원문 유지) → 근거 문서(위험등급·출처는 코드가 근거에서 읽음) → 유의(표준 문장, 1개)\n#   - 위험등급·출처는 정답표(PRODUCT_FACTS) 또는 사용한 근거 문서(R2_ 투자설명서) 원문에서만 읽는다. 못 읽으면 쓰지 않는다.\n#   - 블록 안의 'N등급'이 근거 등급과 다르면 근거 값으로 교정(척도 표현 '6등급 중'은 제외).\n#   - 상품이 둘 이상 섞인 블록, 예금·원리금보장 블록, 항목줄이 없는 산문 블록은 손대지 않는다.\n_RR_BUL = re.compile(r\"^(\\s*[-•·*]\\s+)\")\n_RR_CANON = re.compile(r\"실적배당형.{0,80}보장되지\\s*않\")\n_RR_RISKNOTE = re.compile(r\"^\\s*[-•·*]\\s*위험\\s*고지\\s*[:：]\")\n\n\ndef _rr_core(nm):\n    \"\"\"상품 식별 키: 공백·따옴표만 제거한 전체 표기(접미어 '자투자신탁'·'[주식]'까지 보존 — 삼성클래식 주식형/채권형처럼\n    앞부분이 같은 상품을 하나로 합치지 않는다).\"\"\"\n    return re.sub(r\"\\s+\", \"\", nm).strip(\"·-'\\\"\")\n\n\ndef _rr_same(a, b):\n    \"\"\"같은 상품의 긴 표기/짧은 표기 관계(한쪽이 다른 쪽의 접두)만 동일 상품으로 본다.\"\"\"\n    return a.startswith(b) or b.startswith(a)\n\n\ndef _rr_evidence(nm, used, allowed):\n    \"\"\"상품명 → (등급 문자열 또는 None, 출처 목록). 정답표 우선, 다음은 '참고 문서에 표기된' R2_ 투자설명서 원문.\"\"\"\n    fact = next((f for f in PRODUCT_FACTS if re.search(f[\"key\"], nm)), None)\n    if fact:\n        return fact[\"grade\"], [fact[\"src\"]]\n    core = _rr_core(nm)\n    if len(core) < 6 or _PROD_GENERIC.fullmatch(re.sub(r\"(?:증권자?투자신탁|투자신탁|펀드|ETF).*$\", \"\", core)):\n        return None, []\n    srcs, grades = [], set()\n    for s in dict.fromkeys(c.get(\"source\", \"\") for c in used):\n        if not s or (allowed and s not in allowed):\n            continue\n        t = _src_text(s)\n        if core not in re.sub(r\"\\s+\", \"\", t):\n            continue\n        srcs.append(s)\n        if s.startswith(\"R2_\"):\n            g, lab = _read_grade(t)\n            if g is not None:\n                grades.add((g, (lab or _LABEL_BY_GRADE.get(g, \"\")).replace(\" \", \"\")))\n    if not srcs:\n        return None, []\n    if len(grades) == 1:\n        g, lab = next(iter(grades))\n        return f\"{g}등급({lab})\" if lab else f\"{g}등급\", srcs\n    return None, srcs\n\n\ndef render_recommend(ans, used, allowed=None):\n    \"\"\"→ (답변, trace 메모, 본문에 인용한 출처 목록). allowed = 출처 줄([참고 문서])에 표기된 문서 집합(answer()가 src_tail에서 전달).\"\"\"\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n    paras = body.split(\"\\n\\n\")\n    if allowed is None:\n        allowed = set(re.findall(r\"[\\w.\\-]+\\.(?:pdf|docx|xlsx|xls|txt|csv|hwp|pptx)\", tail))\n    allowed = set(allowed)\n    cited = []\n    n_blk = n_fix = 0\n    _done = set()\n    all_names = []\n    for p in paras:\n        for m in _PROD_SPAN.finditer(p):\n            c = _rr_core(m.group(0).strip(\"·-'\\\"\"))\n            if len(c) >= 6 and not any(_rr_same(c, x) for x in all_names):\n                all_names.append(c)\n    for pi, p in enumerate(paras):\n        lines = p.split(\"\\n\")\n        if len(lines) < 2 or p.lstrip().startswith(\"※\"):\n            continue\n        if not any(_RR_BUL.match(l) for l in lines):\n            continue\n        groups = []                                               # [[키 표기들...], ...] 같은 상품끼리 묶음\n        for m in list(_PROD_SPAN.finditer(p)) + list(_PROD_CITE.finditer(p)):\n            raw = m.group(0).strip(\"·-'\\\"\")\n            c = _rr_core(raw)\n            if len(c) < 6 or _PROD_GENERIC.fullmatch(re.sub(r\"(?:증권자?투자신탁|투자신탁|펀드|ETF).*$\", \"\", c)):\n                continue\n            g_ = next((g for g in groups if _rr_same(g[0][0], c)), None)\n            if g_ is None:\n                groups.append([(c, raw)])\n            else:\n                g_.append((c, raw))\n        if len(groups) != 1:\n            continue                                              # 상품 0개 또는 2개 이상(비교 블록 등) → 손대지 않음\n        core = min(groups[0], key=lambda x: len(x[0]))[0]         # 포함 검사용: 가장 짧은 표기\n        nm = max(groups[0], key=lambda x: len(x[0]))[1]           # 표시·근거 대조용: 가장 구체적인 표기\n        grade, srcs = _rr_evidence(nm, used, allowed)\n        if not srcs and len(core) >= 8 and core != _rr_core(nm):\n            grade, srcs = _rr_evidence(core, used, allowed)       # 구체 표기가 문서 표기와 다르면(괄호·호수) 짧은 표기로 재대조\n        for s_ in srcs:                                            # 정답표 출처가 참고 문서에 없으면 출처 줄에 추가(표기 일관성)\n            if s_ not in cited:\n                cited.append(s_)\n            if tail and s_ not in allowed:\n                tail = tail.rstrip() + \", \" + s_\n            allowed.add(s_)\n        head = None if _RR_BUL.match(lines[0]) else lines[0]\n        rest = lines[1:] if head is not None else lines\n        risk, others, caveats = [], [], []\n        for l in rest:\n            if _RR_RISKNOTE.match(l):\n                risk.append(l)\n            elif _RR_BUL.match(l) and _RR_CANON.search(l) and len(re.split(r\"(?<=[.!?])\\s+\", l.strip())) == 1:\n                caveats.append(l)\n            else:\n                others.append(l)\n        ind = next((_RR_BUL.match(l).group(1) for l in rest if _RR_BUL.match(l)), \" - \")\n        # 등급 교정: 근거 등급과 다른 'N등급'(척도 '6등급 중'·'총 N등급' 제외)\n        if grade and grade[0].isdigit():\n            g0 = grade[0]\n            def _gfix(m):\n                nonlocal n_fix\n                if m.group(1) != g0:\n                    n_fix += 1\n                    return g0 + m.group(2)\n                return m.group(0)\n            _pat = re.compile(r\"(?<![\\d.총])([1-6])(\\s*등급)(?!\\s*중)\")\n            others = [_pat.sub(_gfix, l) for l in others]\n            if head is not None:\n                head = _pat.sub(_gfix, head)\n        _has_nm = lambda ls: any(core in re.sub(r\"\\s+\", \"\", l) for l in ls)\n        if not _has_nm(([head] if head is not None else []) + risk + others):\n            others.insert(0, ind + \"상품 예시: \" + nm)                # 상품명이 유의 문장에만 남았으면 이름 줄을 세운다\n        new = ([head] if head is not None else []) + risk + others\n        if srcs and not any(s in p for s in srcs):\n            new.append(ind + \"근거 문서: \" + \", \".join(srcs) + (f\" — 위험등급 {grade}\" if grade else \"\"))\n        if caveats:\n            new.append(caveats[0])                                 # 표준 유의 문장은 블록당 하나, 맨 끝\n            n_fix += len(caveats) - 1\n        newp = \"\\n\".join(new)\n        _done.add(pi)\n        if newp != p:\n            paras[pi] = newp\n            n_blk += 1\n    # 블록에 이미 든 표준 유의 문장이 다른 문단(도입·맺음)에 그대로 반복되면 그 문장만 제거(문단이 비면 문단 제거)\n    _cav = set()\n    for _bi in _done:\n        for l in paras[_bi].split(\"\\n\"):\n            if _RR_CANON.search(l):\n                _cav.add(re.sub(r\"\\s+\", \"\", re.sub(r\"^\\s*[-•·*]\\s*(?:유의\\s*[:：]\\s*)?\", \"\", l)))\n    if _cav:\n        for pi, p in enumerate(paras):\n            if pi in _done:\n                continue\n            _nl = []\n            for l in p.split(\"\\n\"):\n                if l.lstrip().startswith(\"※\") or not l.strip():\n                    _nl.append(l); continue\n                ss = re.split(r\"(?<=[.!?])\\s+\", l.strip())\n                kp = [x for x in ss if re.sub(r\"\\s+\", \"\", x) not in _cav]\n                if len(kp) != len(ss):\n                    n_fix += len(ss) - len(kp)\n                    if kp:\n                        _nl.append(\" \".join(kp))\n                else:\n                    _nl.append(l)\n            paras[pi] = \"\\n\".join(_nl)\n        paras = [p for p in paras if p.strip()]\n    body = \"\\n\\n\".join(paras)\n    # 상품이 둘 이상인 답변에서 문단 첫머리 '이 상품은 실적배당형…'(주어 불명) → '위 상품들은 모두'\n    if len(all_names) >= 2:\n        body, k = re.subn(r\"(?m)^이 상품은 실적배당형으로, 상대적으로 변동성이 낮을 수 있으나\",\n                          \"위 상품들은 모두 실적배당형으로, 상대적으로 변동성이 낮을 수 있으나\", body)\n        n_fix += k\n    note = f\" 추천 렌더러(블록 {n_blk}개 구조화, 교정 {n_fix}건)\" if (n_blk or n_fix) else \"\"\n    return (body.rstrip() + (\"\\n\\n\" + tail if tail else \"\")), note, cited\n\n\ndef income_rate_card(question):\n    \"\"\"질문의 총급여로 세액공제율을 코드가 판정한 카드 (없으면 None)\"\"\"\n"], ["        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n            if _npf:\n                clean_note += f\" 상품 사실표 교정 {_npf}건\"\n", "        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n            if _npf:\n                clean_note += f\" 상품 사실표 교정 {_npf}건\"\n        # v13.42 [추천 렌더러]: 상품 블록을 근거 기준 구조(머리말→위험 고지→본문→근거 문서→유의)로 결정적으로 재구성(LLM 호출 없음).\n        #   본문에 인용하는 출처는 출처 줄(src_tail)에 있는 문서로 한정하고, 정답표 출처는 출처 줄에 추가한다(본문-출처 정합).\n        if qtype == \"추천\":\n            _rr_allow = set(re.findall(r\"[\\w.\\-]+\\.(?:pdf|docx|xlsx|xls|txt|csv|hwp|pptx)\", src_tail))\n            ans, _rr, _rr_cited = render_recommend(ans, used, _rr_allow)\n            clean_note += _rr\n            for _s in _rr_cited:\n                if src_tail and _s not in src_tail:\n                    src_tail = src_tail.rstrip() + \", \" + _s\n"], ["r\"안전성을\\s*(?:제공|보장)|손실(?:의)?\\s*가능성이\\s*(?:매우|거의|아주)\\s*낮\")", "r\"안전성을\\s*(?:제공|보장)|손실(?:의)?\\s*가능성이\\s*(?:매우|거의|아주)\\s*낮|안정성이\\s*(?:가장\\s*)?높\")   # v13.42: '안정성이 높은' 단정(실측 R2)"], ["                elif not _PROD_CITE.search(ans):\n                    _sj = \"펀드·ETF 등 실적배당형 상품은 \"                  # 답변 전체에 상품명이 없으면(제도 답변) 일반 주어\n                else:\n                    _sj = \"이 상품은 \"\n", "                elif not (_PROD_CITE.search(ans) or _PROD_SPAN.search(ans)):\n                    _sj = \"펀드·ETF 등 실적배당형 상품은 \"                  # 답변 전체에 상품명이 없으면(제도 답변) 일반 주어\n                else:\n                    # v13.42(R2 실측): 답변의 상품이 하나뿐이면 그 이름을 주어로(띄어쓰기 상품명 'NH-Amundi 하나로 단기채 …'도 인식)\n                    _spans = [m_.group(0).strip(\"·-'\\\"\") for m_ in _PROD_SPAN.finditer(ans)] or [m_.group(0).strip(\"·-'\\\"\") for m_ in _PROD_CITE.finditer(ans)]\n                    _cores = {_rr_core(x_) for x_ in _spans if len(_rr_core(x_)) >= 4}\n                    _sj = (_spans[0] + \"은(는) \") if len(_cores) == 1 else \"이 상품은 \"\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.42 [추천 렌더러]" in src: print("[스킵] 이미 v13.42 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1341과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1341_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
