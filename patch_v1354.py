# -*- coding: utf-8 -*-
"""patch_v1354.py — v1353(2eff73b3) main.py 에 v13.54 적용. (v1353 적용 후 실행)
  [불변식 3개] ① 검증값 전파: 비교 답변의 표 밖 문장에서 상품 'N등급'은 근거 등급과 같을 때만 남김(미확정·불일치 문장 제거 — T6 요약)
   ② 추천 마무리 계약(recommend_finalize): 상품 목적어의 단정 추천(추천드립니다·적합하다고 판단·가장 좋습니다·상품이 좋습니다)을 '후보로 검토' 표현으로 통일,
   판촉성 정성 주장(수상·인정받·우수·1위)은 근거 문서에 없으면 제거, 한 문장에 펀드 상품명+원리금보장형이 섞이면 유형 분리 표준 문장으로,
   원리금보장형 문장의 '안정적인 수익 제공' 단정 완화 ③ 질문 관련성: 상품 추천 질문에 세제·절세 전략 블록이 끼면(질문에 세제 요구 없음) 제거 후 재번호
자동백업·자가검증. 이미 v13.54면 스킵. 검증 md5==9c766a173f656ca639415c2a8d5f6c25"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='2eff73b366d4e3c35df1df844ec234ae'
EXPECT_AFTER='9c766a173f656ca639415c2a8d5f6c25'
HUNKS=[["def render_recommend(ans, used, allowed=None):", "# v13.54 [추천 마무리 계약 — RecommendationContract 불변식] (LLM 호출 없음)\n#   ① 단정 추천 표현(추천드립니다·적합하다고 판단·가장 좋습니다…)은 '후보로 검토' 표현으로 통일(과제: 단정 추천 지양)\n#   ② 판촉성 정성 주장(수상·인정받·우수·대표·1위·최고)은 사용 근거 문서에 그 낱말이 없으면 문장 제거\n#   ③ 유형 혼합: 한 문장에 실적배당형 펀드 상품명과 '원리금보장형'이 함께 오면 두 유형을 분리한 표준 문장으로 교체\n#   ④ 원리금보장형 문장의 '안정적인 수익을 제공' 단정은 '보장 조건에 따라 원리금 지급'으로\n#   ⑤ 상품 추천 질문에 세제·절세 전략 블록(번호 항목)이 끼면 질문에 세제 요구가 없을 때 제거(질문 관련성)\n_RF_ASSERT = [\n    # 상품(명·펀드·ETF·상품)을 목적어로 한 단정 추천만 바꾼다('상품을 추천드리겠습니다' 같은 안내문은 불변)\n    (re.compile(r\"((?:펀드|ETF|투자신탁|\\]|\\)|호)(?:\\s*C-P\\d)?)(을|를)\\s*추천\\s*(?:드립니다|합니다|드릴\\s*수\\s*있습니다|해\\s*드립니다)\"), r\"\\1\\2 후보로 검토하실 수 있습니다\"),\n    (re.compile(r\"(이|가)\\s*(?:적합하다고\\s*판단됩니다|적합합니다|적절합니다|가장\\s*좋습니다|최선입니다|알맞습니다|가장\\s*적합합니다)\"), r\"\\1 검토 가능한 후보입니다\"),\n    (re.compile(r\"(상품|자산)(들)?이\\s*좋습니다\"), r\"\\1\\2을 검토할 수 있습니다\"),\n    (re.compile(r\"(펀드|ETF)(들)?이\\s*좋습니다\"), r\"\\1\\2를 검토할 수 있습니다\"),\n    (re.compile(r\"추천\\s*상품\\s*[:：]\"), \"검토 후보:\"),\n]\n_RF_PROMO = re.compile(r\"수상|인정받|우수한|우수성|대표적인\\s*(?:상품|펀드)|최고의|업계\\s*(?:최|1위)|1위|검증된\\s*(?:성과|수익)\")\n\n\ndef recommend_finalize(ans, used, question):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n    _docs = re.sub(r\"\\s+\", \"\", \"\".join(c.get(\"text\", \"\") for c in used))\n    n = 0\n    out = []\n    _skip = False\n    _tax_q = bool(re.search(r\"세제|절세|세액공제|세금|공제|과세\", question))\n    for ln in body.split(\"\\n\"):\n        # ⑤ 세제 전략 블록 제거(질문에 세제 요구 없을 때): 번호 머리말이 세제·절세면 그 블록 전체\n        if re.match(r\"^\\s*\\d+[.)]\\s\", ln):\n            _skip = (not _tax_q) and bool(re.search(r\"세제|절세|세액공제|세금|과세\", ln))\n            if _skip:\n                n += 1; continue\n        elif not ln.strip():\n            _skip = False\n        if _skip:\n            continue\n        if ln.lstrip().startswith(\"※\") or \"근거 문서:\" in ln:\n            out.append(ln); continue\n        _lead = re.match(r\"^\\s*(?:[-•·*]\\s*|\\d+[.)]\\s*)?\", ln).group(0)\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln[len(_lead):])\n        kp = []\n        for st in sents:\n            if not st.strip():\n                continue\n            # ② 판촉성 주장: 근거 문서에 그 낱말이 없으면 제거\n            _pm = _RF_PROMO.search(st)\n            if _pm and _pm.group(0).replace(\" \", \"\")[:2] not in _docs:\n                n += 1; continue\n            # ③ 유형 혼합: 펀드 상품명 + 원리금보장형이 한 문장\n            _prod = _PROD_SPAN.search(st) or _PROD_CITE.search(st)\n            if _prod and re.search(r\"원리금\\s*보장\", st):\n                _nm = re.sub(r\"은\\(는\\)$\", \"\", _prod.group(0).strip(\"·-'\\\"\"))\n                st = (f\"{_nm}은(는) 실적배당형(펀드)이라 상대적으로 위험등급이 낮더라도 원금이 보장되지 않습니다. \"\n                      \"원금 보장을 우선한다면 예금 등 원리금보장형 상품군을 별도로 확인해야 합니다.\")\n                n += 1\n            # ④ 원리금보장형 문장의 '안정적인 수익 제공' 단정\n            elif re.search(r\"원리금\\s*보장\", st) and re.search(r\"안정적인\\s*수익(?:을|이)?\\s*(?:제공|보장)\", st):\n                st = re.sub(r\"안정적인\\s*수익(?:을|이)?\\s*(?:제공|보장)(?:합니다|됩니다|할\\s*수\\s*있습니다|하는)\", \"보장 조건에 따라 원리금이 지급됩니다\", st); n += 1\n            # ① 단정 추천 표현 통일\n            for pat, rep in _RF_ASSERT:\n                st2 = pat.sub(rep, st)\n                if st2 != st:\n                    st = st2; n += 1\n            kp.append(st)\n        if ln.strip() and not kp:\n            continue\n        out.append(_lead + \" \".join(kp) if kp else ln)\n    body = re.sub(r\"\\n{3,}\", \"\\n\\n\", \"\\n\".join(out)).strip()\n    # 번호가 빠진 목록 재번호(세제 블록 제거 뒤)\n    _i = [0]\n    def _renum(m):\n        _i[0] += 1; return f\"{_i[0]}. \"\n    if n and re.search(r\"(?m)^\\s*\\d+[.)]\\s\", body):\n        body = re.sub(r\"(?m)^(\\s*)\\d+[.)]\\s\", lambda m: m.group(1) + _renum(m), body)\n    return (body + (\"\\n\\n\" + tail if tail else \"\")), (f\" 추천 마무리 계약 {n}건\" if n else \"\")\n\n\ndef render_recommend(ans, used, allowed=None):"], ["            ans, _rr, _rr_cited = render_recommend(ans, used, _rr_allow)\n            clean_note += _rr", "            ans, _rr, _rr_cited = render_recommend(ans, used, _rr_allow)\n            clean_note += _rr\n            ans, _rf = recommend_finalize(ans, used, question)      # v13.54: 단정 표현·판촉 주장·유형 혼합·무관 세제 블록\n            clean_note += _rf"], ["                _known = {_nm: int(_g[0]) for _nm, _g in _ge if _g}\n                if len(_known) >= 1:", "                _known = {_nm: int(_g[0]) for _nm, _g in _ge if _g}\n                # v13.54 [검증값 전파 불변식]: 표 밖 문장에서 상품의 'N등급'을 말하면 근거 등급과 같을 때만 남긴다(미확정·불일치 → 문장 제거)\n                def _mp2(_s, _k):\n                    _kk = re.sub(r\"\\s+\", \"\", _k); _ss_ = re.sub(r\"\\s+\", \"\", _s)\n                    for _L in (len(_kk), 12, 8, 6):\n                        _p = _ss_.find(_kk[:_L])\n                        if _p >= 0:\n                            return _p\n                    return -1\n                _out2, _nd2 = [], 0\n                for _ln in ans.split(\"\\n\"):\n                    if _ln.lstrip().startswith((\"|\", \"※\", \"[\", \"(\", \"-\")) or not re.search(r\"\\d\\s*등급\", _ln) or \"등급 비교(근거 문서 기준)\" in _ln or \"위험등급 우열:\" in _ln:\n                        _out2.append(_ln); continue\n                    _lead = re.match(r\"^\\s*(?:\\d+[.)]\\s*)?\", _ln).group(0)\n                    _kp = []\n                    for _s in re.split(r\"(?<=[.!?])\\s+\", _ln[len(_lead):]):\n                        _gm = re.findall(r\"(?<![\\d.])([1-6])\\s*등급\", _s)\n                        if _gm:\n                            _ments = sorted([(_mp2(_s, _k), _k) for _k in _hp if _mp2(_s, _k) >= 0])\n                            _subj = _ments[0][1] if _ments else None\n                            if _subj is None or _subj not in _known or any(int(g) != _known[_subj] for g in _gm):\n                                _nd2 += 1; continue\n                        _kp.append(_s)\n                    if _kp:\n                        _out2.append(_lead + \" \".join(_kp))\n                if _nd2:\n                    ans = \"\\n\".join(_out2)\n                    clean_note += f\" 미검증 등급 문장 {_nd2}건 제거\"\n                if len(_known) >= 1:"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.54 [추천 마무리 계약" in src: print("[스킵] 이미 v13.54 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1353과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1353_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
