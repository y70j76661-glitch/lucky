# -*- coding: utf-8 -*-
"""patch_v1338.py — v1337(5a6b4e58) main.py 에 v13.38 적용. (v1337 적용 후 실행)
  [변형 회귀(variant 14문항) 정독 — 전부 의미 조건 규칙으로]
   S1: 표의 상품이 정답표와 같은 등급이면 '위험등급 원문 대조 불가' 고지(거짓)를 제거.
   R2: '62등급' 같은 1~6 밖 등급 표기 차단('(위험등급: 자료에서 확인 필요)'), 등급 판정에 숫자 lookbehind, '손실 가능성이 매우 낮'
       단정을 계약 대상에 추가, 띄어쓰기 섞인 상품명 전체(NH-Amundi 하나로 단기채 …, 한국밸류 10년투자 연금 …)를 주어로 인식.
   R1: '… 경우입니다.' 형태 사례 머리말과 상품명 없는 번호 줄은 계약 대상에서 제외(머리말 대체 재발 방지).
   P1: 상품명이 없는 제도 답변의 보장 단정은 '펀드·ETF 등 실적배당형 상품은 투자원금과 수익이 보장되지 않습니다'로.
   P2: 추천 답변의 '세액공제 혜택 극대화·세금 절약 극대화·세제 혜택:' 문장 제거. 전 답변: '(문서9 참조)' 내부 참조 제거.
   X1·X2: 제외 뒤 남은 조각('놓치면 혜택을 받을 수 없습니다.') 제거, 목록 개수 서술('세 가지 조건')을 실제 항목 수로 교정.
자동백업·자가검증. 이미 v13.38이면 스킵. 검증 md5==cea136d92709df500b54fc825f6d32b2"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='5a6b4e581e806cf83df6dd4f64f29305'
EXPECT_AFTER='cea136d92709df500b54fc825f6d32b2'
HUNKS=[["        break\nprint(f\"연령세율 근거 문서: {_AGE_SRC or '(못 찾음)'}\")\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n# v13.13(일반 게이트 1): 상품명 대조용 코퍼스 블롭(공백 제거). Primary Source = 주입 문서 전체.\n_CORPUS_NORM = \"\\u0001\".join(re.sub(r\"\\s+\", \"\", (c.get(\"text\", \"\") if isinstance(c, dict) else str(c)))\n                            for c in (chunks if isinstance(chunks, list) else chunks.values()))\n", "        break\nprint(f\"연령세율 근거 문서: {_AGE_SRC or '(못 찾음)'}\")\n_PROD_CITE = re.compile(r\"[가-힣A-Za-z0-9()\\[\\]·\\-]{6,}(?:증권자?투자신탁|투자신탁|펀드|ETF)\")\n# v13.38: 띄어쓰기가 섞인 상품명 전체('NH-Amundi 하나로 단기채 증권투자신탁[채권]', '한국밸류 10년투자 연금 증권전환형자투자신탁 1호(채권)')\n_PROD_SPAN = re.compile(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|NH-Amundi|NH-아문디|한국밸류|미래에셋|삼성|KB|신한|하나|키움|교보|IBK|한화|우리)\"\n                        r\"(?:\\s*[가-힣A-Za-z0-9()\\[\\]·\\-]+){0,6}?\\s*(?:증권자?투자신탁|투자신탁|펀드|ETF|\\(합성\\)|C-P\\d)\"\n                        r\"(?:\\s*(?:제\\s*)?\\d\\s*호)?(?:\\s*(?:\\[[가-힣]+\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\)))?\"\n                        r\"|(?:TIGER|KODEX|ACE|KBSTAR|SOL|HANARO|ARIRANG|RISE)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}(?:\\s*ETF)?\")   # ETF 브랜드는 접미어 없이도\n# v13.13(일반 게이트 1): 상품명 대조용 코퍼스 블롭(공백 제거). Primary Source = 주입 문서 전체.\n_CORPUS_NORM = \"\\u0001\".join(re.sub(r\"\\s+\", \"\", (c.get(\"text\", \"\") if isinstance(c, dict) else str(c)))\n                            for c in (chunks if isinstance(chunks, list) else chunks.values()))\n"], ["    for _pi, _p in enumerate(_paras):\n        if re.search(r\"예금|원리금\\s*보장|원리금보장|ELB|DLB\", _p) or _p.lstrip().startswith(\"※\"):\n            continue\n        _pm = _PROD_CITE.search(_p) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _p)\n        if not _pm:\n            continue\n        _lines = _p.split(\"\\n\")\n", "    for _pi, _p in enumerate(_paras):\n        if re.search(r\"예금|원리금\\s*보장|원리금보장|ELB|DLB\", _p) or _p.lstrip().startswith(\"※\"):\n            continue\n        _pm = _PROD_CITE.search(_p) or _PROD_SPAN.search(_p)\n        if not _pm:\n            continue\n        _lines = _p.split(\"\\n\")\n"], ["            if not hit_docs and not fact:\n                drop = True; removed.append(nm); break         # 근거 문서에 없는 상품 → 줄 차단\n            # 등급 주장 검증: 이름 뒤 40자 안의 'N등급'\n            g = re.search(r\"(\\d)\\s*등급\", ln[m.end(): m.end() + 40])\n            if g:\n                if fact:\n                    ok_g = re.match(r\"(\\d)\", fact[\"grade\"]).group(1)\n", "            if not hit_docs and not fact:\n                drop = True; removed.append(nm); break         # 근거 문서에 없는 상품 → 줄 차단\n            # 등급 주장 검증: 이름 뒤 40자 안의 'N등급'\n            _bad = re.search(r\"(?<![\\d.])\\d{2,}\\s*등급\", ln[m.end(): m.end() + 60])\n            if _bad:                                              # v13.38(R2 실측 '62등급'): 1~6 밖의 등급 표기는 차단\n                ln = ln[: m.end()] + re.sub(r\"(?<![\\d.])\\d{2,}\\s*등급\", \"(위험등급: 자료에서 확인 필요)\", ln[m.end(): m.end() + 60], count=1) + ln[m.end() + 60:]\n                _fixed_box[0] += 1\n            g = re.search(r\"(?<![\\d.])(\\d)\\s*등급\", ln[m.end(): m.end() + 40])\n            if g:\n                if fact:\n                    ok_g = re.match(r\"(\\d)\", fact[\"grade\"]).group(1)\n"], ["                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자|장기\\s*성장\\s*가능성이\\s*큽|장기적으로\\s*(?:더\\s*)?큰\\s*수익|\"\n                       r\"성장\\s*잠재력이\\s*큽|손실이\\s*(?:거의\\s*)?(?:일어나지|발생하지|나지)\\s*않|손실\\s*(?:위험|가능성)이\\s*(?:거의\\s*)?없|\"\n                       r\"안전성을\\s*(?:제공|보장)\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_HI = \"실적배당형이며 변동성이 큰 편으로, 장기 투자자가 검토할 수 있으나 투자원금과 수익(분배금)은 보장되지 않으며 손실 가능성이 있습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    HI_RISK = re.compile(r\"높은\\s*위험|매우\\s*높은|고위험|(?:위험\\s*등급\\s*)?\\(?\\s*[12]\\s*등급|주식형|주식에\\s*주로\")\n    _para_text = {}\n    for _l, _pidx in zip(_all_lines, _line_para):\n        _para_text[_pidx] = _para_text.get(_pidx, \"\") + \"\\n\" + _l\n", "                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자|장기\\s*성장\\s*가능성이\\s*큽|장기적으로\\s*(?:더\\s*)?큰\\s*수익|\"\n                       r\"성장\\s*잠재력이\\s*큽|손실이\\s*(?:거의\\s*)?(?:일어나지|발생하지|나지)\\s*않|손실\\s*(?:위험|가능성)이\\s*(?:거의\\s*)?없|\"\n                       r\"안전성을\\s*(?:제공|보장)|손실(?:의)?\\s*가능성이\\s*(?:매우|거의|아주)\\s*낮\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)(?:입니다|이라면|라면|에는)?\\s*\\**\\s*[.:：]?\\s*\\**\\s*$\")   # 사례 머리말('… 경우:'·'… 경우입니다.')\n    NUMHEAD = re.compile(r\"^\\s*\\d+[.)]\\s*\\**[^\\n]{0,90}(?:경우|성향|선호|중시|원하)\")\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_GEN = \"투자원금과 수익(분배금)이 보장되지 않습니다.\"\n    CANON_HI = \"실적배당형이며 변동성이 큰 편으로, 장기 투자자가 검토할 수 있으나 투자원금과 수익(분배금)은 보장되지 않으며 손실 가능성이 있습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    HI_RISK = re.compile(r\"높은\\s*위험|매우\\s*높은|고위험|(?:위험\\s*등급\\s*)?\\(?\\s*(?<![\\d.])[12]\\s*등급|주식형|주식에\\s*주로\")\n    _para_text = {}\n    for _l, _pidx in zip(_all_lines, _line_para):\n        _para_text[_pidx] = _para_text.get(_pidx, \"\") + \"\\n\" + _l\n"], ["            out.append(ln); continue\n        if _PB_SAFE_DEPOSIT.search(ln):\n            out.append(ln); continue                             # 예금·원리금보장 상품 줄은 통째로 불변\n        if ln.lstrip().startswith(\"※\") or HEADER.match(ln.replace(\"**\", \"\")):\n            out.append(ln); continue                             # v13.32: 코드 캐비엇·사례 머리말('… 경우:')은 상품 서술이 아님\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _pk = _line_para[_li]\n        _canon_done = _para_canon.get(_pk, False)          # v13.35: 표준 문장은 문단(항목)당 한 번만\n        for st in sents:\n            _has_prod = bool(_PROD_CITE.search(st) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,}\", st))\n            if CRITERIA.search(st) and not _has_prod:\n                # v13.35: 투자자 조건 서술의 '…분들께 적합/알맞습니다'는 '해당하는 경우입니다'로(상품 적합성 단정 아님)\n                st = re.sub(r\"((?:분들?|투자자|고객)(?:께|에게))\\s*(?:적합합니다|알맞습니다|적절합니다)\", r\"\\1 해당하는 경우입니다\", st)\n", "            out.append(ln); continue\n        if _PB_SAFE_DEPOSIT.search(ln):\n            out.append(ln); continue                             # 예금·원리금보장 상품 줄은 통째로 불변\n        if ln.lstrip().startswith(\"※\") or HEADER.match(ln.replace(\"**\", \"\")) \\\n                or (NUMHEAD.match(ln.replace(\"**\", \"\")) and not _PROD_CITE.search(ln)):\n            out.append(ln); continue                             # v13.32/38: 코드 캐비엇·사례 머리말(상품명 없는 번호 줄)은 상품 서술이 아님\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _pk = _line_para[_li]\n        _canon_done = _para_canon.get(_pk, False)          # v13.35: 표준 문장은 문단(항목)당 한 번만\n        for st in sents:\n            _has_prod = bool(_PROD_CITE.search(st) or _PROD_SPAN.search(st))\n            if CRITERIA.search(st) and not _has_prod:\n                # v13.35: 투자자 조건 서술의 '…분들께 적합/알맞습니다'는 '해당하는 경우입니다'로(상품 적합성 단정 아님)\n                st = re.sub(r\"((?:분들?|투자자|고객)(?:께|에게))\\s*(?:적합합니다|알맞습니다|적절합니다)\", r\"\\1 해당하는 경우입니다\", st)\n"], ["            _C = CANON_HI if _hi else CANON\n            lead = re.match(r\"^(\\s*(?:[-*•·]\\s*|\\d+[.)]\\s*)?(?:(?:참고|근거|추천\\s*상품|상품\\s*예시|적합\\s*이유|투자\\s*특징|유의\\s*사항)\\s*[:：]\\s*)?)\", st).group(1)\n            body = st[len(lead):]\n            pm = _PROD_CITE.search(body) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|미래에셋|삼성|KB|한화|신한)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}(?:\\s*ETF|\\s*\\(합성\\)|\\s*C-P\\d)?\", body)\n            if pm:\n                _end = pm.end()\n                _g = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\))?\\s*(?:C-P\\d\\s*)?(?:\\(\\s*(?:위험\\s*등급\\s*)?[^)]{1,12}\\))?\", body[_end:])\n                _end += _g.end() if _g else 0\n                subj = body[pm.start():_end].strip().rstrip(\"은는이가도\")\n                new = f\"{subj}은(는) \" + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                _pt = _para_text.get(_line_para[_li], \"\")\n                _ppm = _PROD_CITE.search(_pt) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _pt)\n                if _ppm:\n                    _gg = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\))?\", _pt[_ppm.end():])\n                    _sj = (_pt[_ppm.start(): _ppm.end() + (_gg.end() if _gg else 0)].strip().strip(\"·-\") + \"은(는) \")\n                else:\n                    _sj = \"이 상품은 \"\n                new = _sj + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            _para_canon[_pk] = True\n", "            _C = CANON_HI if _hi else CANON\n            lead = re.match(r\"^(\\s*(?:[-*•·]\\s*|\\d+[.)]\\s*)?(?:(?:참고|근거|추천\\s*상품|상품\\s*예시|적합\\s*이유|투자\\s*특징|유의\\s*사항)\\s*[:：]\\s*)?)\", st).group(1)\n            body = st[len(lead):]\n            pm = _PROD_CITE.search(body) or _PROD_SPAN.search(body)\n            if pm:\n                _end = pm.end()\n                _g = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\))?\\s*(?:C-P\\d\\s*)?(?:\\(\\s*(?:위험\\s*등급\\s*)?[^)]{1,12}\\))?\", body[_end:])\n                _end += _g.end() if _g else 0\n                subj = body[pm.start():_end].strip().rstrip(\"은는이가도\")\n                new = f\"{subj}은(는) \" + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            elif \"상품들\" in body and _PROD_CITE.search(ans):\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                _pt = _para_text.get(_line_para[_li], \"\")\n                _ppm = _PROD_CITE.search(_pt) or _PROD_SPAN.search(_pt)\n                if _ppm:\n                    _gg = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\d\\s*호)?\\s*(?:\\[[가-힣]+\\]|\\((?:주식|채권|혼합|주식혼합|채권혼합)\\))?\", _pt[_ppm.end():])\n                    _st0 = _ppm.start()\n                    # v13.38: 이름 앞의 짧은 토큰('한국밸류 10년투자 연금')도 상품명의 일부 — 같은 줄, 4단어 이내, 구두점 없음\n                    _ls0 = _pt.rfind(\"\\n\", 0, _st0) + 1\n                    _seg = re.sub(r\"^\\s*(?:\\d+[.)]\\s*|[-•·]\\s*|(?:추천\\s*상품|상품\\s*예시)\\s*[:：]\\s*)?\", \"\", _pt[_ls0:_st0])\n                    if _seg.strip() and len(_seg.split()) <= 4 and not re.search(r\"[.,:：()'\\\"]|같은|예를|등의\", _seg):\n                        _st0 = _st0 - len(_seg)\n                    _sj = (_pt[_st0: _ppm.end() + (_gg.end() if _gg else 0)].strip().strip(\"·-'\") + \"은(는) \")\n                elif not _PROD_CITE.search(ans):\n                    _sj = \"펀드·ETF 등 실적배당형 상품은 \"                  # 답변 전체에 상품명이 없으면(제도 답변) 일반 주어\n                else:\n                    _sj = \"이 상품은 \"\n                new = _sj + (CANON_GEN if _sj.startswith(\"펀드·ETF\") else (CANON_SUIT if (has_suit and not has_claim) else _C))\n            fixed.append(lead + new)\n            _canon_done = True\n            _para_canon[_pk] = True\n"], ["    #    노출되지 않도록 최종 답변에서 볼드 마커만 뗀다(내용·숫자·간격은 그대로).\n    ans = ans.replace(\"**\", \"\")\n    ans = ans.replace(\"`\", \"\")          # v13.15(G09): raw 렌더에서 깨지는 백틱 제거\n    # H) v13.1(4단계): 미닫힘 따옴표 정리 — 본문의 따옴표 개수가 홀수면 짝 없는(마지막) 따옴표만\n    #    제거한다. 한국어 본문의 따옴표는 열고-닫음 쌍이라 홀수 = 미닫힘(실측 G01 '부득이한 사유(…).\n    #    [참고 문서] 줄은 이미 분리된 상태라 안전. 내용은 안 건드리고 고아 마커만 뗀다.\n", "    #    노출되지 않도록 최종 답변에서 볼드 마커만 뗀다(내용·숫자·간격은 그대로).\n    ans = ans.replace(\"**\", \"\")\n    ans = ans.replace(\"`\", \"\")          # v13.15(G09): raw 렌더에서 깨지는 백틱 제거\n    ans = re.sub(r\"\\s*\\(\\s*문서\\s*\\d+\\s*(?:참조|기준|참고)\\s*\\)\", \"\", ans)   # v13.38: 내부 문서 번호 참조('(문서9 참조)') 제거 — 출처는 [참고 문서]에\n    ans = re.sub(r\"(?<![\\d.])\\d{2,}\\s*등급(?:으로|이며|이고|입니다)?\", \"(위험등급: 자료에서 확인 필요)\", ans)   # v13.38: 위험등급은 1~6등급뿐('62등급' 같은 오출력 차단)\n    ans = re.sub(r\"(?m)^\\s*(?:놓치면|이를\\s*놓치면|그렇지\\s*않으면|이\\s*기간을\\s*넘기면)[^\\n]{0,40}$\\n?\", \"\", ans)   # v13.38: 앞 문장이 지워져 남은 조각\n    # H) v13.1(4단계): 미닫힘 따옴표 정리 — 본문의 따옴표 개수가 홀수면 짝 없는(마지막) 따옴표만\n    #    제거한다. 한국어 본문의 따옴표는 열고-닫음 쌍이라 홀수 = 미닫힘(실측 G01 '부득이한 사유(…).\n    #    [참고 문서] 줄은 이미 분리된 상태라 안전. 내용은 안 건드리고 고아 마커만 뗀다.\n"], ["                _kp = [x for x in _ss if not ((re.search(r\"소득공제|세액공제|비과세|이자소득세|세제\\s*혜택\", x)\n                                               and re.search(r\"\\d+\\s*(?:%|만\\s*원|년)\", x))\n                                              or \"조세특례제한법\" in x\n                                              or re.search(r\"소득공제|비과세\\s*혜택|세금\\s*혜택을\\s*극대화\", x)      # v13.36: 숫자 없어도 세제 혜택 서술 제거\n                                              or re.search(r\"\\(구\\)\\s*개인연금|구\\s*개인연금저축|개인연금저축\\s*\\(구\\)\", x))]   # v13.36: 신규 가입 불가 구상품 추천 제거\n                _nm += len(_ss) - len(_kp)\n                if _ln.strip() and not \"\".join(_kp).strip():\n", "                _kp = [x for x in _ss if not ((re.search(r\"소득공제|세액공제|비과세|이자소득세|세제\\s*혜택\", x)\n                                               and re.search(r\"\\d+\\s*(?:%|만\\s*원|년)\", x))\n                                              or \"조세특례제한법\" in x\n                                              or re.search(r\"소득공제|비과세\\s*혜택|세금\\s*혜택을\\s*극대화|세액공제\\s*혜택을\\s*극대화|세금\\s*절약\\s*효과를\\s*극대화|세제\\s*혜택\\s*[:：]\", x)      # v13.36/38: 숫자 없어도 세제 혜택 서술 제거\n                                              or re.search(r\"\\(구\\)\\s*개인연금|구\\s*개인연금저축|개인연금저축\\s*\\(구\\)\", x))]   # v13.36: 신규 가입 불가 구상품 추천 제거\n                _nm += len(_ss) - len(_kp)\n                if _ln.strip() and not \"\".join(_kp).strip():\n"], ["            if _nd:\n                _body = \"\\n\".join(_outl)\n                clean_note += f\" 제외요청 주제 문장 {_nd}건 제거\"\n        # F01: '예금·국채 등 안전자산 … 원금 손실' 혼합 서술 → 원리금보장형/실적배당형 분리\n        _b2, _nf = re.subn(r\"[^.!?\\n]*예금[^.!?\\n]*(?:국채|채권)[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?]\"\n                            r\"(?:\\s*[^.!?\\n]*(?:금리|가격)\\s*변동[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?])?\",\n", "            if _nd:\n                _body = \"\\n\".join(_outl)\n                clean_note += f\" 제외요청 주제 문장 {_nd}건 제거\"\n                # v13.38(X1·X2 실측): 제거 뒤 남는 조각('놓치면 혜택을 받을 수 없습니다.')과 개수 서술('이 세 가지 조건')을 정리\n                _b4, _nf = re.subn(r\"(?m)^\\s*(?:놓치면|이를\\s*놓치면|그렇지\\s*않으면|이\\s*기간을\\s*넘기면)[^\\n]{0,40}$\", \"\", _body)\n                if _nf:\n                    _body = re.sub(r\"\\n{3,}\", \"\\n\\n\", _b4)\n                _n_items = len(re.findall(r\"(?m)^\\s*\\d+[.)]\\s\", _body))\n                _kw = {2: \"두\", 3: \"세\", 4: \"네\", 5: \"다섯\"}\n                if _n_items in _kw:\n                    _body = re.sub(r\"(?:두|세|네|다섯)\\s*가지\\s*(조건|요건|사항)\", _kw[_n_items] + r\" 가지 \\1\", _body)\n        # F01: '예금·국채 등 안전자산 … 원금 손실' 혼합 서술 → 원리금보장형/실적배당형 분리\n        _b2, _nf = re.subn(r\"[^.!?\\n]*예금[^.!?\\n]*(?:국채|채권)[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?]\"\n                            r\"(?:\\s*[^.!?\\n]*(?:금리|가격)\\s*변동[^.!?\\n]*원금\\s*손실[^.!?\\n]*[.!?])?\",\n"], ["            if n_unver:\n                clean_note += f\" 원문 미대조 {n_unver}칸\"\n            clean_note += _rnote\n        # v9.99f: 카테고리 비교(TDF·원리금보장형 등)라 원문 상품을 못 찾으면\n        #   risk_prof가 비어 위 블록이 통째로 건너뛴다. 그래도 표에 '위험등급'\n        #   행이 등급을 주장하면(N등급) 어떤 마크도 없어 검사 불변식에 걸린다\n", "            if n_unver:\n                clean_note += f\" 원문 미대조 {n_unver}칸\"\n            clean_note += _rnote\n            # v13.38(S1 실측): 표의 상품이 정답표(PRODUCT_FACTS, 코퍼스 확인값)에 있고 표 등급이 정답표와 같으면\n            #   '원문 대조 불가' 고지는 사실이 아니므로 제거한다(대조가 코드로 이미 된 것).\n            if RISK_WARN_MARK in ans and _pf_hits and len(_pf_hits) >= 1:\n                _tbl = next((ln for ln in ans.splitlines() if re.search(r\"위험\\s*등급\", ln) and (ln.count(\"|\") >= 2 or ln.count(\";\") >= 1)), \"\")\n                _facts_ok = all((re.match(r\"(\\d)\", f[\"grade\"]) and re.match(r\"(\\d)\", f[\"grade\"]).group(1) + \"등급\" in _tbl.replace(\" \", \"\"))\n                                for f in _pf_hits if re.match(r\"\\d\", f[\"grade\"]))\n                if _tbl and _facts_ok:\n                    ans = re.sub(r\"\\n*\\[위험등급 원문 대조[^\\n]*\", \"\", ans)\n                    clean_note += \" (정답표 일치로 대조 불가 고지 제거)\"\n        # v9.99f: 카테고리 비교(TDF·원리금보장형 등)라 원문 상품을 못 찾으면\n        #   risk_prof가 비어 위 블록이 통째로 건너뛴다. 그래도 표에 '위험등급'\n        #   행이 등급을 주장하면(N등급) 어떤 마크도 없어 검사 불변식에 걸린다\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "_PROD_SPAN = re.compile(" in src: print("[스킵] 이미 v13.38 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1337과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1337_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
