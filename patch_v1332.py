# -*- coding: utf-8 -*-
"""patch_v1332.py — v1331(d645985f) main.py 에 v13.32 적용. (v1331 적용 후 실행)
  [mini13 실측 — 원인과 수정]
   M2 목록 깨짐('2. *이 상품은…', 빈 '3.'): v13.31 계약이 사례 머리말('2. 은퇴가 가깝거나 원금 보전이 중요한 경우:')을 상품
       서술로 보고 표준 문장으로 바꾼 뒤, 같은 문장이 중복 제거되며 '3.'만 남음 → 머리말(…경우/때/분들: 로 끝나는 줄)·※ 캐비엇·
       부정문은 계약 대상에서 제외.
   M2 '장기 투자자에게 적합하다고'·'변동성을 관리하기에 용이': 적합 판정을 '적합(?!성)|알맞|적절|부합|용이'로 넓힘. 표준 문장은
       위험도에 맞춤(높은위험·1·2등급·주식형: '변동성이 큰 편', 그 외: '상대적으로 변동성이 낮을 수 있으나'), 한 줄에 한 번만.
   상품별 비보장 고지(계약 2c): 추천 답변에서 상품이 언급된 문단마다 '보장되지 않' 고지가 없으면 ' - 유의: 실적배당형으로 투자원금과
       수익(분배금)은 보장되지 않습니다'를 붙임. 안정형 질문(안정·안전·원금)에 높은위험 상품이 오면 ' - 위험 고지: … 안정형 후보로
       보기 어렵습니다'를 그 상품 첫 줄에 명시.
   G11(계약 4): '원금과 이자를 보장해주는 상품' → '해당 금융기관·상품의 보장 조건에 따라 원리금이 보장되는 상품'; 근거 문서에 없는
       '중도 해지 시 이자 손실' 문장 제거.
자동백업·자가검증. 이미 v13.32면 스킵. 검증 md5==2e776920d280a5bbe7447ee89ffcc27a"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='d645985f50532fcceb09f1913288da8f'
EXPECT_AFTER='2e776920d280a5bbe7447ee89ffcc27a'
HUNKS=[["\n# v13.28 [답변 계약 2 — 추천 상품 연결]: 추천 답변의 각 상품은 '상품명 ∈ 사용 근거 문서' 이고, 답변이 붙인 위험등급이 그 문서(또는\n#   정답표)의 등급과 같아야 한다. 연결이 끊긴 상품의 줄은 제거하고 한 줄로 알린다(문구 치환이 아니라 주장 단위 차단).\ndef recommend_contract(ans, used):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n", "\n# v13.28 [답변 계약 2 — 추천 상품 연결]: 추천 답변의 각 상품은 '상품명 ∈ 사용 근거 문서' 이고, 답변이 붙인 위험등급이 그 문서(또는\n#   정답표)의 등급과 같아야 한다. 연결이 끊긴 상품의 줄은 제거하고 한 줄로 알린다(문구 치환이 아니라 주장 단위 차단).\ndef deposit_guarantee_contract(ans, used):\n    if not re.search(r\"원리금\\s*보장|원리금보장|예금\", ans):\n        return ans, \"\"\n    _texts = [re.sub(r\"\\s+\", \"\", c.get(\"text\", \"\")) for c in used]\n    n = 0\n    out = []\n    for ln in ans.split(\"\\n\"):\n        if ln.lstrip().startswith(\"※\") or \"[참고 문서]\" in ln:\n            out.append(ln); continue\n        ss = re.split(r\"(?<=[.!?])\\s+\", ln)\n        kp = []\n        for st in ss:\n            if re.search(r\"원금과\\s*이자를\\s*보장|원리금[이을]?\\s*보장(?:해|하|되)\", st) and not re.search(r\"조건|따라|한도|예금자보호|보장하지|보장되지|않\", st):\n                st2 = re.sub(r\"원금과\\s*이자를\\s*보장해\\s*주는\\s*상품(?:으로|이며|입니다)?\", \"해당 금융기관·상품의 보장 조건에 따라 원리금이 보장되는 상품으로\", st)\n                if st2 == st:\n                    st2 = re.sub(r\"원리금[이을]?\\s*보장(?:해\\s*주는|하는|되는)\\s*상품\", \"해당 금융기관·상품의 보장 조건에 따라 원리금이 보장되는 상품\", st)\n                if st2 == st:\n                    st2 = re.sub(r\"원리금[이을]?\\s*보장됩니다\", \"해당 금융기관·상품의 보장 조건에 따라 원리금이 보장됩니다\", st)\n                st = st2\n                n += 1\n            if re.search(r\"중도\\s*해지[^.!?]{0,20}이자[^.!?]{0,10}손실\", st) and not any((\"중도해지\" in t or \"중도상환\" in t) and \"이자\" in t for t in _texts):\n                n += 1\n                continue                                          # 근거 문서에 없는 중도해지 이자 손실 서술 제거\n            kp.append(st)\n        if ln.strip() and not \"\".join(kp).strip():\n            continue\n        out.append(\" \".join(kp))\n    return \"\\n\".join(out), (f\" (원리금보장 서술 계약 {n}건)\" if n else \"\")\n\n\n_question_hint = [\"\"]      # v13.32: recommend_contract 가 질문 성향(안정형)을 알기 위한 슬롯(answer()에서 설정)\n\n\ndef recommend_contract(ans, used):\n    body, tail = (ans.split(\"[참고 문서]\", 1) + [\"\"])[:2]\n    tail = (\"[참고 문서]\" + tail) if \"[참고 문서]\" in ans else \"\"\n"], ["        out.append(\" \".join(_kept))\n    fixed = _fixed_box[0]\n    body = \"\\n\".join(out)\n    if removed:\n        body = body.rstrip() + (\"\\n\\n※ \" + \", \".join(f\"'{r}'\" for r in removed[:3]) +\n                                \"은(는) 사용한 근거 문서에서 확인되지 않아 추천 후보에서 제외했습니다.\")\n", "        out.append(\" \".join(_kept))\n    fixed = _fixed_box[0]\n    body = \"\\n\".join(out)\n    # v13.32 [계약 2c — 상품별 비보장 고지]: 상품이 언급된 문단마다 '보장되지 않' 고지가 없으면 그 문단 끝에 표준 문장을 붙인다.\n    #   예금·원리금보장 문단은 제외. 질문이 안정형(안정·안전·원금)인데 상품이 높은위험(1·2등급/높은위험)이면 위험을 먼저 명시한다.\n    _paras = body.split(\"\\n\\n\")\n    _stable_q = bool(re.search(r\"안정|안전|원금\", _question_hint[0])) if _question_hint else False\n    _added = 0\n    for _pi, _p in enumerate(_paras):\n        if re.search(r\"예금|원리금\\s*보장|원리금보장|ELB|DLB\", _p) or _p.lstrip().startswith(\"※\"):\n            continue\n        _pm = _PROD_CITE.search(_p) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _p)\n        if not _pm:\n            continue\n        _lines = _p.split(\"\\n\")\n        if _stable_q and re.search(r\"높은\\s*위험|매우\\s*높은\\s*위험|\\(?\\s*(?:위험\\s*등급\\s*)?[12]\\s*등급\", _p) \\\n                and not re.search(r\"안정형\\s*후보로\\s*보기\", _p):\n            _lines.insert(1, \" - 위험 고지: 이 상품은 높은위험 등급으로 주가·환율·분배금 변동에 따라 원금 손실이 발생할 수 있어, \"\n                             \"안정형 후보로 보기 어렵습니다(참고용 후보).\")\n            _added += 1\n        if not re.search(r\"보장되지\\s*않|보장은\\s*아님|보장되는\\s*상품은\\s*아닙\", _p):\n            _lines.append(\" - 유의: 이 상품은 실적배당형으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\")\n            _added += 1\n        _paras[_pi] = \"\\n\".join(_lines)\n    if _added:\n        body = \"\\n\\n\".join(_paras)\n        _fixed_box[0] += _added\n    if removed:\n        body = body.rstrip() + (\"\\n\\n※ \" + \", \".join(f\"'{r}'\" for r in removed[:3]) +\n                                \"은(는) 사용한 근거 문서에서 확인되지 않아 추천 후보에서 제외했습니다.\")\n"], ["            _para_fund[_pidx] = True\n    CLAIM = re.compile(r\"원금(?:이|을|은|의)?\\s*(?:보장|보전|보호|유지)|거의\\s*확실|가장\\s*안전|안전성이\\s*(?:가장\\s*)?높|안전한?\\s*(?:옵션|선택|상품|자산)|무위험|\"\n                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장\")\n    SUIT = re.compile(r\"적합(?:합니다|한|하며)|알맞(?:습니다|은|으며)|적절(?:합니다|한|하며)|부합(?:합니다|하며)\")\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    _ans_fund = bool(_PROD_CITE.search(ans)) or bool(re.search(r\"TIGER|KODEX|ETF|펀드\", ans))   # 상품 추천 답변이면 전체가 펀드 문맥\n    out = []\n    for _li, ln in enumerate(_all_lines):\n", "            _para_fund[_pidx] = True\n    CLAIM = re.compile(r\"원금(?:이|을|은|의)?\\s*(?:보장|보전|보호|유지)|거의\\s*확실|가장\\s*안전|안전성이\\s*(?:가장\\s*)?높|안전한?\\s*(?:옵션|선택|상품|자산)|무위험|\"\n                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n    STRONG = re.compile(r\"거의\\s*확실|가장\\s*안전|안전한\\s*옵션|무위험|원금(?:이|을|은)?\\s*(?:보전|보호|유지)\")\n    CRITERIA = re.compile(r\"^\\s*[-•]?\\s*선택\\s*기준|선호하|원하시|찾고\\s*계|중시하는\\s*(?:경우|분)\")\n    CANON = \"실적배당형으로, 상대적으로 변동성이 낮을 수 있으나 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_HI = \"실적배당형이며 변동성이 큰 편으로, 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    CANON_SUIT = \"후보로 검토할 수 있으며, 실적배당형이라 투자원금과 수익(분배금)은 보장되지 않습니다.\"\n    HI_RISK = re.compile(r\"높은\\s*위험|매우\\s*높은|고위험|(?:위험\\s*등급\\s*)?\\(?\\s*[12]\\s*등급|주식형|주식에\\s*주로\")\n    _para_text = {}\n    for _l, _pidx in zip(_all_lines, _line_para):\n        _para_text[_pidx] = _para_text.get(_pidx, \"\") + \"\\n\" + _l\n    _ans_fund = bool(_PROD_CITE.search(ans)) or bool(re.search(r\"TIGER|KODEX|ETF|펀드\", ans))   # 상품 추천 답변이면 전체가 펀드 문맥\n    out = []\n    for _li, ln in enumerate(_all_lines):\n"], ["            out.append(ln); continue\n        if _PB_SAFE_DEPOSIT.search(ln):\n            out.append(ln); continue                             # 예금·원리금보장 상품 줄은 통째로 불변\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        for st in sents:\n            if CRITERIA.search(st):\n                fixed.append(st); continue                       # 투자자 조건 서술은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n                fixed.append(st); continue                       # 이미 비보장 부정이 든 문장은 그대로\n            if not (has_claim or has_suit):\n                fixed.append(st); continue\n            lead = re.match(r\"^(\\s*(?:[-*•·]\\s*|\\d+[.)]\\s*)?(?:(?:참고|근거|추천\\s*상품|상품\\s*예시|적합\\s*이유|투자\\s*특징|유의\\s*사항)\\s*[:：]\\s*)?)\", st).group(1)\n            body = st[len(lead):]\n            pm = _PROD_CITE.search(body) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|미래에셋|삼성|KB|한화|신한)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}(?:\\s*ETF|\\s*\\(합성\\)|\\s*C-P\\d)?\", body)\n", "            out.append(ln); continue\n        if _PB_SAFE_DEPOSIT.search(ln):\n            out.append(ln); continue                             # 예금·원리금보장 상품 줄은 통째로 불변\n        if ln.lstrip().startswith(\"※\") or HEADER.match(ln.replace(\"**\", \"\")):\n            out.append(ln); continue                             # v13.32: 코드 캐비엇·사례 머리말('… 경우:')은 상품 서술이 아님\n        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _canon_done = False\n        for st in sents:\n            if CRITERIA.search(st) or re.search(r\"어렵|않|아니|없습니다\", st) and not CLAIM.search(st):\n                fixed.append(st); continue                       # 투자자 조건 서술·부정문은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n                fixed.append(st); continue                       # 이미 비보장 부정이 든 문장은 그대로\n            if not (has_claim or has_suit):\n                fixed.append(st); continue\n            if _canon_done:\n                n += 1; continue                                 # 같은 줄에 표준 문장이 이미 들어갔으면 중복 삽입 대신 제거\n            _C = CANON_HI if _hi else CANON\n            lead = re.match(r\"^(\\s*(?:[-*•·]\\s*|\\d+[.)]\\s*)?(?:(?:참고|근거|추천\\s*상품|상품\\s*예시|적합\\s*이유|투자\\s*특징|유의\\s*사항)\\s*[:：]\\s*)?)\", st).group(1)\n            body = st[len(lead):]\n            pm = _PROD_CITE.search(body) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL|미래에셋|삼성|KB|한화|신한)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}(?:\\s*ETF|\\s*\\(합성\\)|\\s*C-P\\d)?\", body)\n"], ["                _g = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\])?\\s*(?:C-P\\d\\s*)?(?:\\(\\s*(?:위험\\s*등급\\s*)?[^)]{1,12}\\))?\", body[_end:])\n                _end += _g.end() if _g else 0\n                subj = body[pm.start():_end].strip().rstrip(\"은는이가도\")\n                new = f\"{subj}은(는) \" + (CANON_SUIT if (has_suit and not has_claim) else CANON)\n            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + CANON\n            else:\n                new = \"이 상품은 \" + (CANON_SUIT if (has_suit and not has_claim) else CANON)\n            fixed.append(lead + new)\n            n += 1\n        out.append(\" \".join(fixed))\n    return \"\\n\".join(out), n\n", "                _g = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\])?\\s*(?:C-P\\d\\s*)?(?:\\(\\s*(?:위험\\s*등급\\s*)?[^)]{1,12}\\))?\", body[_end:])\n                _end += _g.end() if _g else 0\n                subj = body[pm.start():_end].strip().rstrip(\"은는이가도\")\n                new = f\"{subj}은(는) \" + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                new = \"이 상품은 \" + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            n += 1\n        out.append(\" \".join(fixed))\n    return \"\\n\".join(out), n\n"], ["        ans, _c1 = age_rate_contract(ans, used)\n        clean_note += _c1\n        if qtype == \"추천\":\n            ans, _c2 = recommend_contract(ans, used)\n            clean_note += _c2\n        # v13.19: 정답표 상품의 등급·보수가 답변에서 다르게 쓰였으면 교정(질문에 그 상품이 있을 때)\n        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n", "        ans, _c1 = age_rate_contract(ans, used)\n        clean_note += _c1\n        if qtype == \"추천\":\n            _question_hint[0] = question\n            ans, _c2 = recommend_contract(ans, used)\n            clean_note += _c2\n        # v13.32 [계약 4 — 원리금보장형 서술]: '원금과 이자를 보장해주는 상품' 같은 무조건 보장 단정은 보장 주체·조건을 붙이고,\n        #   근거 문서에 없는 '중도 해지 시 이자 손실' 서술은 제거\n        ans, _c4 = deposit_guarantee_contract(ans, used)\n        clean_note += _c4\n        # v13.19: 정답표 상품의 등급·보수가 답변에서 다르게 쓰였으면 교정(질문에 그 상품이 있을 때)\n        if _pf_hits:\n            ans, _npf = enforce_product_facts(ans, _pf_hits)\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "def deposit_guarantee_contract(" in src: print("[스킵] 이미 v13.32 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1331과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1331_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
