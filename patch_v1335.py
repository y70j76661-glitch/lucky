# -*- coding: utf-8 -*-
"""patch_v1335.py — v1334(c02a2fce) main.py 에 v13.35 적용. (v1334 적용 후 실행)
  [mini16 정독] M2 '선택 기준: … 분들께 적합/알맞습니다'(투자자 조건 줄) → '해당하는 경우입니다'; 같은 항목에 표준 문장이 두 번
   들어가던 것(문단당 한 번만) + 문장이 다 빠진 줄은 빈 줄로 남기지 않음; '거의 원금 손실이 일어나지 않도록 설계' 같은 부정형
   보장 단정도 계약 대상; 문단 상품명 주어에 '제N호[유형]' 포함; G11 '원리금보장형 상품이 적합할 것입니다'(가장 없이)도 후보 표현으로.
자동백업·자가검증. 이미 v13.35면 스킵. 검증 md5==b44d31ce892b76c46003cf187d12ddd8"""
import sys,os,time,hashlib,py_compile
TARGET=sys.argv[1] if len(sys.argv)>1 else "main.py"
EXPECT_BEFORE='c02a2fce4a55709dbefb7a9fa999c8bf'
EXPECT_AFTER='b44d31ce892b76c46003cf187d12ddd8'
HUNKS=[["                n += 1\n                continue                                          # 근거 문서에 없는 중도해지 이자 손실 서술 제거\n            # v13.33(G11): '원리금보장형 상품이 가장 적합할 것입니다' → 후보 표현; 재예치·만기상환 서술은 근거 문서에 그 단어가 있을 때만\n            if re.search(r\"원리금\\s*보장형?\\s*상품이\\s*가장\\s*적합\", st):\n                st = re.sub(r\"원리금\\s*보장형?\\s*상품이\\s*가장\\s*적합할?\\s*(?:것입니다|합니다|수\\s*있습니다)\",\n                            \"자료상 원금 보장을 중시할 때 검토할 수 있는 후보는 원리금보장형 상품입니다\", st)\n                n += 1\n            if re.search(r\"재예치|만기상환\", st) and not any((\"재예치\" in t or \"만기상환\" in t) for t in _texts):\n", "                n += 1\n                continue                                          # 근거 문서에 없는 중도해지 이자 손실 서술 제거\n            # v13.33(G11): '원리금보장형 상품이 가장 적합할 것입니다' → 후보 표현; 재예치·만기상환 서술은 근거 문서에 그 단어가 있을 때만\n            if re.search(r\"원리금\\s*보장형?\\s*상품이\\s*(?:가장\\s*)?적합\", st):\n                st = re.sub(r\"원리금\\s*보장형?\\s*상품이\\s*(?:가장\\s*)?적합할?\\s*(?:것입니다|합니다|수\\s*있습니다)\",\n                            \"자료상 원금 보장을 중시할 때 검토할 수 있는 후보는 원리금보장형 상품입니다\", st)\n                n += 1\n            if re.search(r\"재예치|만기상환\", st) and not any((\"재예치\" in t or \"만기상환\" in t) for t in _texts):\n"], ["                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자|장기\\s*성장\\s*가능성이\\s*큽|장기적으로\\s*(?:더\\s*)?큰\\s*수익|\"\n                       r\"성장\\s*잠재력이\\s*큽\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n", "                       r\"손실(?:의\\s*가능성|\\s*위험)?[을이]?\\s*(?:최소화|거의\\s*없|없)|안정적인\\s*(?:수익|배당|이자|연금\\s*수령|분배|현금흐름|수입)[^.!?]{0,40}?\"\n                       r\"(?:제공|보장|돕|추구|목표|창출|얻|확보|기대|위해)|정기적인\\s*(?:수입|수익|분배|현금)[^.!?]{0,15}?(?:제공|보장|확보)|(?:수익|배당|이자)[을이]?\\s*보장|\"\n                       r\"변동성을\\s*관리하기(?:에)?\\s*용이|안전한\\s*(?:투자|채권)에\\s*투자|장기\\s*성장\\s*가능성이\\s*큽|장기적으로\\s*(?:더\\s*)?큰\\s*수익|\"\n                       r\"성장\\s*잠재력이\\s*큽|손실이\\s*(?:거의\\s*)?(?:일어나지|발생하지|나지)\\s*않|손실\\s*(?:위험|가능성)이\\s*(?:거의\\s*)?없\")\n    SUIT = re.compile(r\"적합(?!성)|알맞|적절|부합|용이합니다\")\n    HEADER = re.compile(r\"^\\s*(?:\\d+[.)]\\s*)?\\**[^\\n]{0,80}(?:경우|때|분들?|투자자|고객)\\s*\\**\\s*[:：]?\\s*\\**\\s*$\")   # '2. 은퇴가 가깝거나 원금 보전이 중요한 경우:' 같은 사례 머리말\n    NEG_OK = re.compile(r\"보장(?:되|하)?(?:지|은)?\\s*(?:않|아니|없)|보장은\\s*아님|보장되는\\s*상품은\\s*아닙|손실\\s*(?:가능성|위험)[이은]?\\s*(?:있|존재)\")\n"], ["    for _l, _pidx in zip(_all_lines, _line_para):\n        _para_text[_pidx] = _para_text.get(_pidx, \"\") + \"\\n\" + _l\n    _ans_fund = bool(_PROD_CITE.search(ans)) or bool(re.search(r\"TIGER|KODEX|ETF|펀드\", ans))   # 상품 추천 답변이면 전체가 펀드 문맥\n    out = []\n    for _li, ln in enumerate(_all_lines):\n        if not (_PB_FUND.search(ln) or _para_fund.get(_line_para[_li], False) or _ans_fund):\n", "    for _l, _pidx in zip(_all_lines, _line_para):\n        _para_text[_pidx] = _para_text.get(_pidx, \"\") + \"\\n\" + _l\n    _ans_fund = bool(_PROD_CITE.search(ans)) or bool(re.search(r\"TIGER|KODEX|ETF|펀드\", ans))   # 상품 추천 답변이면 전체가 펀드 문맥\n    _para_canon = {}\n    out = []\n    for _li, ln in enumerate(_all_lines):\n        if not (_PB_FUND.search(ln) or _para_fund.get(_line_para[_li], False) or _ans_fund):\n"], ["        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _canon_done = False\n        for st in sents:\n            _has_prod = bool(_PROD_CITE.search(st) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,}\", st))\n            if (CRITERIA.search(st) and not _has_prod) or (re.search(r\"어렵|않|아니|없습니다\", st) and not CLAIM.search(st)):\n                fixed.append(st); continue                       # 투자자 조건 서술(상품명 없음)·부정문은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n", "        sents = re.split(r\"(?<=[.!?])\\s+\", ln)\n        fixed = []\n        _hi = bool(HI_RISK.search(_para_text.get(_line_para[_li], \"\") + ln))\n        _pk = _line_para[_li]\n        _canon_done = _para_canon.get(_pk, False)          # v13.35: 표준 문장은 문단(항목)당 한 번만\n        for st in sents:\n            _has_prod = bool(_PROD_CITE.search(st) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,}\", st))\n            if CRITERIA.search(st) and not _has_prod:\n                # v13.35: 투자자 조건 서술의 '…분들께 적합/알맞습니다'는 '해당하는 경우입니다'로(상품 적합성 단정 아님)\n                st = re.sub(r\"((?:분들?|투자자|고객)(?:께|에게))\\s*(?:적합합니다|알맞습니다|적절합니다)\", r\"\\1 해당하는 경우입니다\", st)\n                fixed.append(st); continue\n            if re.search(r\"어렵|않|아니|없습니다\", st) and not CLAIM.search(st):\n                fixed.append(st); continue                       # 부정문은 불변\n            has_claim = bool(CLAIM.search(st))\n            has_suit = bool(SUIT.search(st))\n            if NEG_OK.search(st) and not STRONG.search(st) and not re.search(r\"안정적인\\s*(?:수익|배당|이자)[^.!?]{0,20}?(?:제공|보장|돕)\", st):\n"], ["            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                _ppm = _PROD_CITE.search(_para_text.get(_line_para[_li], \"\")) or \\\n                       re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _para_text.get(_line_para[_li], \"\"))\n                _sj = (_ppm.group(0).strip(\"·-\") + \"은(는) \") if _ppm else \"이 상품은 \"   # 문단의 상품명을 주어로(전역 중복 제거 회피)\n                new = _sj + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            n += 1\n        out.append(\" \".join(fixed))\n    return \"\\n\".join(out), n\n\n", "            elif \"상품들\" in body:\n                new = \"이 상품들은 모두 \" + _C\n            else:\n                _pt = _para_text.get(_line_para[_li], \"\")\n                _ppm = _PROD_CITE.search(_pt) or re.search(r\"(?:TIGER|KODEX|ACE|KBSTAR|SOL)\\s*[가-힣A-Za-z0-9()·\\-]{2,40}\", _pt)\n                if _ppm:\n                    _gg = re.match(r\"\\s*(?:제\\s*\\d+\\s*호)?\\s*(?:\\[[가-힣]+\\])?\", _pt[_ppm.end():])\n                    _sj = (_pt[_ppm.start(): _ppm.end() + (_gg.end() if _gg else 0)].strip().strip(\"·-\") + \"은(는) \")\n                else:\n                    _sj = \"이 상품은 \"\n                new = _sj + (CANON_SUIT if (has_suit and not has_claim) else _C)\n            fixed.append(lead + new)\n            _canon_done = True\n            _para_canon[_pk] = True\n            n += 1\n        if ln.strip() and not \"\".join(fixed).strip():\n            continue                                             # 문장이 다 빠진 줄은 빈 줄로 남기지 않는다\n        out.append(\" \".join(fixed))\n    return \"\\n\".join(out), n\n\n"]]
def md5(s): return hashlib.md5(s.encode("utf-8")).hexdigest()
def main():
    if not os.path.exists(TARGET): print("[중단] 대상 없음:",TARGET); sys.exit(1)
    src=open(TARGET,encoding="utf-8").read(); before=md5(src)
    print("대상:",TARGET,"\n적용전 md5:",before)
    if "v13.35: 표준 문장은 문단(항목)당 한 번만" in src: print("[스킵] 이미 v13.35 적용됨."); sys.exit(0)
    if before!=EXPECT_BEFORE: print("[경고] 적용전 md5가 예상 v1334과 다름. 훅 매칭되면 계속.")
    for k,(O,N) in enumerate(HUNKS):
        c=src.count(O)
        if c!=1: print(f"[중단] 훅{k} OLD 매칭 {c}회(1이어야). 취소."); sys.exit(2)
    out=src
    for O,N in HUNKS: out=out.replace(O,N,1)
    after=md5(out); ts=time.strftime("%Y%m%d_%H%M%S"); bak=TARGET+".bak_v1334_"+ts
    open(bak,"w",encoding="utf-8").write(src); open(TARGET,"w",encoding="utf-8").write(out)
    ok=(after==EXPECT_AFTER)
    print("  ✓ 훅",len(HUNKS),"개 적용","\n백업:",bak,"\n적용후 md5:",after,"\n기대   md5:",EXPECT_AFTER," →","일치 ✅" if ok else "불일치 ❌")
    try: py_compile.compile(TARGET,doraise=True); print("py_compile: OK")
    except py_compile.PyCompileError as e: print("[중단] 문법오류:",e); sys.exit(3)
    print("최종 판정:", "성공 ✅" if ok else "확인 필요 ❌"); print("\n완료. uvicorn 재시작 후 스모크.")
if __name__=="__main__": main()
